# AGENTS.md

本文件供 Codex / AI 编程助手使用。进入本仓库后，必须先阅读本文件，再阅读 `docs/新对话交接文档.md`、`docs/开发状态.md` 和任务相关专题文档。所有判断以当前代码和当前项目文档为准，不依赖旧聊天记录。

## 项目与正式范围

项目名称：电信云平台项目现场综合管理系统。

当前正式产品是 Web 与微信小程序共用后端、数据库和文件存储的多项目现场管理平台。正式权限目录只包括：

- 场内管理：Web 在同一预约列表创建 `SINGLE` 单次外访或 `MEETING` 共享会议邀请，并可查看、纠错、作废和按人员导出。单次预约保持一次提交锁定整条邀请；会议分别使用提前登记码和会场签到码，预约人员按原微信身份逐人签到，未预约人员可现场补录并签到，工作人员可受控补签、撤销和更正。访客免注册，可在微信静默身份下复用当前项目的常用人员与车辆资料；截止或整场作废后不再返回凭证、导航或新的签到会话。
- 资料管理：目录、资料、版本、预览下载、归档、回收站，图纸/技术文件收文、发放、扫码签收与版本追溯，以及项目级用印申请、用户直指派审批、盖章件回传和用印台账。
- 巡检管理：进入后分为“电箱巡检”和“临边巡检”两个独立专区。电箱继续使用台账、六项每日巡检、专用月表、统一二维码和原整改流程；临边只使用系统内置的 8 类固定检查表，项目只维护点位和一套周期/人员设置，不提供用户模板、自定义类别、二维码或公开月表。Web 临边记录支持后台异步生成含图 Excel；临边不再设置项目试点开关。
- 质量周检：每项目每自然周一份共享草稿，可一次整理多个独立质量问题后原子提交；支持无问题周检留档。提交后各问题独立整改、复查和操作留痕，历史独立问题继续兼容。
- 系统管理：注册审核、用户、角色权限、菜单功能、用印审批、微信绑定和操作日志。项目与角色只在注册审核或用户管理中分配；用印审批按项目和具体印章直接选择业务审批用户，不向单个用户写入角色权限。
- 项目信息：Web 顶部当前项目选择器旁进入独立档案页，项目有效成员可查看，平台管理员可编辑；小程序从“我的 → 施工区域”进入同一档案的只读页。该入口是项目基础能力，不新增菜单或普通角色权限码。

地图、历史“项目概况”（视频/设备综合页）、人员、安全总览、摄像头、设备和视频等旧代码可能仍在仓库中，但当前入口已隐藏，不属于本期菜单和权限目录。独立“项目信息”档案页不等于恢复历史“项目概况”；除非任务明确要求，不得恢复旧入口，也不得把历史文档中的旧页面当成当前产品。

## 用印附件与盖章件要求（2026-09-11）

- 用印申请的待盖章附件选填；事由及至少一项文件名称/份数仍必填，Web、小程序和后端一致。
- 当前明确指派的审批人在同意时选择 `stampedResultRequired`，默认 false（按需上传）；true 要求用印后补传盖章件。
  选择与审批结果同事务保存，不改变原批准后上传/归档权限或申请状态，也不根据项目经理角色名推导审批资格。
- 详情、PDF、台账和审批留痕展示回传要求。旧申请默认按需上传；原本地库已备份并双跑
  `20260911_seal_optional_attachments.sql`，仍为 106 表。详见 `docs/用印附件与盖章件要求.md`。

## 会议详情与资料留档（2026-09-11）

- 小程序会议页的访客导航和会议资料由基本信息下方按钮打开同页居中弹窗；登记表单保留。
  导航地图按需挂载，资料关闭/预览返回保留筛选、页码和滚动位置；关闭停止播放及未完成下载，
  原生文档查看期间保留临时文件。到期关闭导航，作废关闭全部对外内容，公开资料不要求先登记。
- MEETING 详情使用系统主内容区完整页面，白蓝主题，预约登记/会场签到/会议资料/操作记录四页签，默认会场签到。
  返回保留列表上下文，从详情进入大屏后返回原会议及页签；普通登录仍进入个人待办。
- 会议资料按文件控制可见范围，新文件/新版本默认内部，明确发布最新版本后才替换旧公开版本。
  会后可继续维护/公开资料，作废只保留内部档案；公开读取不能恢复预约、放行或签到。
- 单文件 1GiB、8MiB 分片、24 小时续传仅用于会议资料。复用文件策略和存储，原文件版本不可变，
  预览异步转换且限制资源/网络；内部内容使用原登录绑定的短 HttpOnly Cookie，每次重新鉴权。
- 资料使用 site_access.view/manage，永久删除仍走平台管理员影响预览。资料、版本、任务及其原文件/副本
  均纳入会议删除；通用文件接口不得绕过会议权限。详见 docs/会议完整详情与资料留档.md。
- 本地原 dianxinyun 在备份后已双跑 20260911_meeting_materials.sql，当前 106 表；本轮按用户要求
  直接更新原 8080/3002 和小程序开发包，不另建演示库。生产发布仍按正式手册。

## 技术栈与目录

后端：

- Java 17、Spring Boot 3.5.14
- MyBatis-Plus 3.5.6、MySQL 8.x
- Redis、JWT、BCrypt
- Knife4j / OpenAPI、Lombok

客户端：

- Web：React 18、Vite 8、axios（Node.js 20.19+ 或 22.12+）
- 小程序：uni-app、Vue 3、TypeScript

主要目录：

```text
backend/
  src/main/java/com/example/siteplatform/
    auth/          账号登录、微信绑定、会话、Web 扫码登录
    registration/  Web/小程序统一注册申请与审批
    system/        菜单、操作权限、角色和系统管理
    project/       项目、项目成员、项目范围和兼容巡检权限
    siteaccess/    单次/会议外访邀请、微信身份登记、常用访客资料、人员明细、加密审计和导出
    seal/          用印配置、扫码申请、直接用户审批、抄送、台账和申请单 PDF
    document/      工程资料目录、资料版本、图纸收发签收追溯和回收站
    electricbox/   电箱台账和二维码
    inspection/    电箱巡检，以及内部 general 数据域承载的固定式临边巡检
    quality/       质量周检共享草稿、质量问题、整改和复查
    file/          通用文件与存储
    common/        统一响应、异常和限流
    config/        JWT、Redis、跨域、拦截器和接口文档
  src/main/resources/sql/
    init.sql
    migrations/

frontend/
  src/App.jsx
  src/pages/Login/
  src/pages/DocumentManagement/
  src/pages/ProjectInformation/
  src/pages/QualityManagement/
  src/pages/SystemManagement/
  src/services/
  src/utils/permissions.js

wechat-miniprogram/site-platform-miniprogram/
  src/pages/
  src/api/
  src/stores/auth.ts
  src/utils/navigation.ts

docs/
  当前项目文档和历史兼容说明
```

本地默认地址：

- Web：`http://localhost:3002`
- 后端：`http://localhost:8080`
- Knife4j：`http://localhost:8080/doc.html`

## 认证、注册与微信登录

- 密码必须使用 BCrypt 校验和保存。非 BCrypt 历史密码禁止登录，并标记为待管理员重置。
- 系统不提供默认测试账号或明文密码。平台管理员密码只能通过系统管理，或一次性环境变量 `ADMIN_RESET_USERNAME`、`ADMIN_RESET_PASSWORD` 显式重置。
- JWT 与 Redis 会话有效期统一读取 `JWT_EXPIRATION_MILLIS`，默认 7 天。
- JWT 带凭证版本和独立会话标识。同一账号可保留多个会话；普通退出只注销当前会话，改密、停用、微信解绑和关键授权变化必须注销该用户全部会话。
- Web 与小程序新用户统一写入 `registration_application`。提交申请时不得创建 `sys_user`；只有审批通过后才能在事务内创建账号、分配角色和项目范围、建立申请中的微信绑定并写审计日志。
- 新注册账号的登录名固定为已核验手机号；Web、小程序和后端不得再让申请人自定义独立用户名。兼容客户端若仍传 `username`，该值必须与手机号完全一致。
- 小程序微信快捷注册使用 `WECHAT_QUICK`：仅微信授权手机号和真实姓名必填，手机号由服务端解析并作为登录账号，不保存申请密码。审批后账号必须先通过微信登录设置初始密码；完成前只能读取本人状态、设置密码或登出，不能访问业务、解绑微信或使用 Web 扫码登录。
- 小程序标准手工注册继续保留为备用方式，仍要求手机号和符合强度规则的密码；Web 注册流程保持图形验证码与人工手机号核验。
- 标准微信注册的一次性 Redis 临时身份必须与申请数据库事务联动：提交成功后清理临时占用标记，事务回滚后恢复身份会话及剩余重试窗口并释放占用；并发请求仍只能成功消费一次。
- `wechat_access_application` 只用于已存在系统账号申请项目访问，不得创建微信专用随机账号。
- 微信绑定在同一 AppID 下严格保持有效用户、OpenID、UnionID 一对一。手机号相同只能用于提示下一步，绝不能自动绑定账号。
- 小程序绑定已有账号必须同时验证账号密码和新的 `uni.login` code。
- Web 扫码登录使用 Redis challenge、浏览器私有校验密钥和一次性交换码；二维码中不得携带 JWT 或浏览器密钥。
- Web 账号密码登录、Web 微信扫码登录、小程序账号/微信登录及已登录会话恢复后，统一进入当前用户的个人待办工作台。小程序首次设置密码和显式扫码业务续跳优先于默认首页，完成后再回到个人待办；不得用首个业务菜单覆盖这一默认落点。
- 微信 mock 默认关闭，只允许在显式 `dev`、`local` 或 `test` Profile 下启用。非开发环境缺少正式 AppID、AppSecret、合法 HTTPS 域名或回跳地址时必须拒绝启动微信能力。
- Knife4j、Swagger UI 和 OpenAPI JSON 生产默认关闭，只有显式开发 Profile 才能开启。生产还必须使用非 root 的独立 MySQL 账号、TLS `DB_URL`、非空数据库/Redis密码和独立 JWT 密钥。

## RBAC 与项目隔离

当前权限由以下部分共同决定：

- 平台角色：`sys_role.scope_type=PLATFORM`。
- 当前项目的项目角色：`sys_role.scope_type=PROJECT` 与 `sys_user_project_role` 多对多关系；旧 `sys_user_project.project_role_code` 只作历史兼容。
- 角色菜单：`sys_menu`、`sys_role_menu`。
- 业务模块：`sys_role_business_module` 中的 `SITE_ACCESS / DOCUMENT / INSPECTION / QUALITY`。后三项原业务模块继续覆盖 Web/内部小程序入口；`SITE_ACCESS` V1 只控制 Web 内部管理入口，访客公开填报不要求业务角色。
- 操作权限：`sys_permission`、`sys_role_permission`。
- 项目数据范围：`sys_user_project.status=ACTIVE`。

必须遵守：

- `PLATFORM_ADMIN` 只通过角色判断，不允许使用固定用户 ID 兜底。
- `PLATFORM_ADMIN` 对全部当前启用、可见的正式菜单、全部启用操作权限、`SITE_ACCESS / DOCUMENT / INSPECTION / QUALITY` 四个业务模块以及所有现有/新建项目无条件放行；不得要求管理员写入项目成员、项目角色或重复授权关系。停用、删除或隐藏的历史目录仍不得恢复。
- 只有 `PLATFORM_ADMIN` 是平台级全局资格；其他业务角色均为项目角色。用户管理仅分配项目与项目角色，受保护的平台全局身份不作为普通业务角色展示或分配。
- 同一成员可在同一项目拥有多个角色，菜单和操作权限按并集计算；`sys_user_project.status=ACTIVE` 是项目角色生效前提。
- 用印审批是业务任务指派，不是用户级 RBAC 覆盖：`seal.view/manage/export` 只控制项目级查询、管理和台账导出；本人申请、当前审批任务和明确抄送记录使用记录级 ACL。平台管理员可以配置和转办，但未被明确指派时不能直接审批。
- 用印申请的候选审批人、默认抄送人和临时抄送人必须是目标项目的有效成员。每枚启用印章拥有独立不透明小程序码，扫码绑定项目和具体印章，申请人不能改章；提交时快照配置版本和候选用户，多人模式为首个有效同意或驳回决定生效。
- 用户管理采用“项目 → 角色”树，项目成员管理采用“用户 → 角色”树；父节点只代表成员关系，首次勾选后必须再明确勾选至少一个项目角色，不得自动补默认角色。
- 跨项目或同项目的多项成员变更必须通过批量接口一次提交并在同一事务内完成；单项校验失败时整批回滚。暂停/恢复访问仍为独立状态操作，不能被角色保存隐式改变。
- `PROJECT_ADMIN` 正式展示为受保护的“项目经理”。只有系统管理员能授予、撤销或调整该角色；项目经理只可管理自己项目的普通成员，不能处理任何项目经理角色、其他项目或平台权限。
- 平台级系统管理接口只认可平台角色提供的平台权限；项目角色不能借聚合权限调用注册、全量用户、角色、菜单、微信全局管理或审计接口。
- 项目角色权限只在目标 `projectId` 的有效成员关系内生效。用户在 A 项目有写权限，不代表能写 B 项目。
- `/api/v1/auth/user-info` 是 Web 和小程序菜单、平台权限码、项目角色及项目权限的统一来源。
- 资料、巡检、质量必须通过一个模块开关统一控制两端入口。场内管理的模块开关只控制 Web 内部管理；小程序 `pages/public/visitor-invite`、`pages/public/meeting-invite` 和 `pages/public/meeting-check-in` 是持不透明 scene 的免登录公开页，不进入内部菜单。关闭模块时保留细分操作权限配置，但对应内部入口和后端项目鉴权必须拒绝该模块。
- 临边巡检不使用项目级试点或启停开关。所有接口校验有效项目范围、`INSPECTION` 模块和对应操作权限；查看、配置/取消/改派、执行、整改、复查和含图导出分别使用 `EDGE_INSPECTION_VIEW / EDGE_INSPECTION_MANAGE / EDGE_INSPECTION_SUBMIT / EDGE_INSPECTION_RECTIFY / EDGE_INSPECTION_REVIEW / EDGE_INSPECTION_EXPORT`。临边权限不得依赖或自动补入电箱的 `inspection.* / BOX_* / INSPECTION_* / SUMMARY_*` 权限；执行、整改和复查还必须是当前明确指派人，平台管理员未被明确指派时不得代替业务人员复查。
- 临边含图 Excel 导出要求 `EDGE_INSPECTION_VIEW + EDGE_INSPECTION_EXPORT`；普通用户只可查看、下载本人创建的任务，平台管理员可查看目标项目全部导出任务。单次最长 31 天、500 个任务、800 张照片和 120MB 原始照片，生成文件七天后过期。电箱统计与导出继续独立使用 `inspection.view / inspection.export / SUMMARY_VIEW / SUMMARY_EXPORT`，不得与临边互相代替。
- 质量周检草稿由目标项目具备 `quality.manage` 的人员共享编辑，整份草稿使用 `expectedVersion` 防并发覆盖；同项目同一自然周只能保留一份草稿或正式周检。未来周不得创建，往期允许补录并标记实际提交时间。提交必须在单一事务中生成全部独立质量问题，任一问题、整改人或附件校验失败时整批回滚。
- 新质量问题只能由周检提交生成；旧 `POST /api/v1/quality/issues` 返回 HTTP `410`，历史 `weekly_inspection_id` 为空的问题仍可查询、整改、复查、改派、作废和管理员确认删除。无问题周检必须填写检查结论并上传至少一张周检现场照片。
- 质量问题使用不可变 `record_date` 业务日期：周检问题取检查日期，历史独立问题按原创建日期回填。
  Web 日期筛选必须成对提交 `startDate/endDate`，闭区间最长 366 天且结束日期不得晚于 Asia/Shanghai
  当天。含图 Excel 只通过后台导出任务生成，普通用户只能访问本人任务，复用 `quality.view`；单次
  最多 300 个问题、800 张照片和 120MB 原始照片数据，文件七天后先标记再延迟物理清理。
- 质量周检、电箱日检和固定临边巡检的未提交提醒只写入现有 `user_notification`，不得创建伪待办、
  调用微信订阅消息或恢复历史外部提醒 Outbox。既有项目规则默认关闭，启用或重新启用后只处理生效
  边界之后的新周期；质量按项目周次、电箱按箱日、临边按任务使用确定性 `dedup_key`，每周期只提醒
  一次。调度执行时必须重新校验项目、模块、责任人和提交状态；责任失效时跳过，不得广播管理员。
- 菜单决定入口可见性，操作权限决定页面内动作；前端显隐只用于体验，不能替代后端鉴权。
- 个人待办是所有已登录用户的基础工作台，不授予任何项目业务权限；业务页面失权时回落个人待办，未登录或会话失效时回登录页。
- 项目信息读取只要求目标项目有效范围；编辑、图片暂存上传和保存只允许 `PLATFORM_ADMIN`。平台管理员仍通过角色编码识别，Web 的 `canEdit` 显示不能替代后端鉴权；小程序不得出现编辑、上传或删除控件。
- 项目地址与导航点必须通过独立定位编辑器和 `PUT /api/v1/projects/{projectId}/location` 原子维护；
  地址字段可填写真实门牌，也可在无门牌项目中填写“附近地标 + 到访说明”，不得为了通过校验伪造
  门牌号。管理员可通过地址解析、地图点击/拖动，或直接录入带来源坐标系的经纬度建立候选点；无论
  来源如何，都必须明确确认后才能保存。普通项目档案保存和通用项目更新不得单独改变地址、经纬度
  或坐标系，防止说明与导航点失配。
- 项目到访路线图必须使用独立的 `PROJECT_ROUTE_IMAGE_PENDING / PROJECT_ROUTE_IMAGE` 文件类型，
  不得混入项目效果图。每个项目最多保留一张当前有效路线图；预约公开页只返回是否可用，不返回
  文件 ID，并且只能通过请求体中的有效邀请令牌读取二进制。路线图读取失败不得阻断坐标导航。
- 角色基本信息、菜单和操作权限必须分开保存；未传授权字段的角色基本信息修改不得清空现有授权。
- 有效角色名称全局唯一；新增和改名都必须忽略首尾空格及英文大小写检查重名，冲突返回 HTTP `409`。角色定义写入使用平台管理员角色行串行化，不能让并发请求绕过名称检查。
- Web 资料管理一级专区固定为工程资料/用印管理/图纸收发；用印管理内部使用用印申请/用印台账二级页签，权限和数据库菜单仍复用 `DOCUMENT_SEAL`，不得拆成两套授权。小程序资料首页同步展示三个业务专区，用印管理内部按本人记录 ACL 和 `seal.view/manage/export` 展示申请/台账；页签细分不得将小程序拆成另一套业务授权。巡检页签固定为电箱台账/电箱巡检记录/电箱整改闭环/临边巡检，质量页签固定为周检记录/质量整改闭环/质量资料。
- 角色“分配菜单”中的巡检目录必须按“电箱巡检 / 临边巡检”两个逻辑专区展示：电箱专区批量控制
  `INSPECTION_LEDGER / INSPECTION_RECORDS / INSPECTION_RECTIFICATIONS`，临边专区控制
  `INSPECTION_EDGE`。逻辑专区不是数据库菜单，保存仍提交原四个真实页面 ID；部分电箱页必须显示
  半选，保留任一专区时不得撤销 `INSPECTION` 模块或两端巡检根菜单。
- 新的角色管理 Web 只调用 `/system/roles/{id}/menus` 和
  `/system/roles/{id}/operation-permissions`；旧 `/permissions` 组合接口仅作兼容，不删除。
- 所有业务接口必须以后端 JWT 用户为准，不得信任前端传入的 `userId`、`username`、角色、上传人或操作人。
- 列表接口必须按有权项目过滤；详情、更新和删除接口必须先查业务记录，再按记录真实 `projectId` 校验。
- 业务硬删除只允许 `PLATFORM_ADMIN` 执行。用户、项目、角色、注册申请、场内外访邀请、资料目录/资料永久清理、质量资料、电箱、巡检记录和质量问题必须先调用删除影响预览，使用 5 分钟内的一次性确认令牌执行；管理员勾选“已核对影响”后即可执行，不再输入目标名称。提交时必须锁定目标并重新计算影响，变化时返回 HTTP `409`。
- `PLATFORM_ADMIN` 角色、当前登录账号、最后一个可恢复的平台管理员及系统操作日志不得删除。删除注册申请只删除 `registration_application` 记录，不得联动删除已创建用户、项目角色、微信绑定或审核操作日志；待审核申请删除后应释放账号、手机号和微信待审核占用。删除其他用户时必须注销全部会话、清除平台/项目授权、微信绑定与订阅状态，并把仍有效的电箱、巡检和质量责任转为待重新分配；历史创建人、审核人、业务记录及操作日志保留。其他角色包括历史内置 `USER` 均可由平台管理员在预览影响后删除；失去最后一个角色的成员同时移出项目。项目删除时只调整待处理注册申请，不联动删除已处理申请。
- 强制删除必须在单一数据库事务中按依赖顺序级联；文件元数据先标记 `PENDING_DELETE`，事务提交后再删除物理文件，失败标记 `DELETE_FAILED` 供重试。普通用户只可删除本人未绑定业务的暂存附件。
- 场内外访邀请删除必须在同一事务中先删除会议签到、会议登记人员、会议审计、会场签到码和会议登记组，再删除单次人员明细、场内业务审计和邀请主记录；不级联删除独立的常用资料，`sys_operation_log` 不级联，并新增本次管理员删除日志。项目包含邀请、会议登记、签到码、签到、常用资料或其审计时均不得直接物理删除。
- 用户撤销项目或角色权限前必须预览责任影响；管理员确认后清空失去处理能力的电箱责任人、未完成巡检复核/整改负责人和未关闭质量整改负责人，业务记录、状态、创建人、操作日志及姓名快照继续保留并显示“待重新分配”。授权事务提交前后都要清权限缓存和全部 Web/小程序会话。
- 巡检汇总的应检、已检、漏检和异常必须按该日期生效的历史巡检范围与到期日期计算，并按箱日去重；建箱前、非巡检范围和未来日期不得计入。
- 电箱所属项目创建后不可通过编辑迁移；`status`、`qrStatus` 只能由停用、拆除或二维码
  生命周期接口修改。传入责任人用户 ID 时必须校验有效系统账号并使用服务端姓名，禁止
  让无效用户 ID 触发项目成员自动补授权；负责人还必须已由管理员分配该项目及至少一个角色。
- 电箱新增、普通编辑、导入、停用和拆除使用 `BOX_MANAGE`；二维码生成、换绑、打印及
  日志查看使用 `BOX_QR_MANAGE`；公开扫码启停使用 `BOX_PUBLIC_ACCESS`。普通编辑如果
  同时修改二维码或公开访问状态，还必须追加对应细分权限，不能借 `BOX_MANAGE` 绕过。
- 公开近 30 天摘要和逐日月表必须一次加载目标日期范围的巡检范围历史，再在内存中解析
  每日应检状态；不得恢复为按自然日逐次查询数据库。
- 文件上传必须调用服务端 `FileUploadPolicy` 校验大小、扩展名和文件头；巡检/质量流程附件只接受真实栅格图片。HTML、SVG、脚本和可执行文件不得上传，非安全预览格式必须强制下载并返回 `nosniff`。
- 本地文件存储的相对键和历史绝对键都必须在配置的上传根目录内；下载、覆盖、回滚清理和永久删除不得接受根目录外路径。
- 永久删除文件时不得在数据库事务提交前删除物理对象；事务回滚必须保留原文件，提交后的物理清理失败必须保留可追踪元数据。
- 通用附件上传的新文件必须在事务回滚时清理；内容替换和删除只能在元数据提交成功后清理旧物理文件。
- 工程资料目录、文件元数据、资料主记录、版本、当前版本指针、归档/回收站状态和资料
  操作日志的单记录写入必须校验影响行数；任一步未生效返回 HTTP `409` 并回滚。资料
  编号最长 100 字符，资料备注和版本说明最长 500 字符，必须在物理文件落盘前校验。
- 图纸收发只管理迁移上线后的 `DRAWING / TECHNICAL_DOCUMENT` 正式资料，历史资料保持
  `GENERAL` 且不补批次。收文发布必须锁定涉及资料并在一个事务中完成版本新增、当前指针切换、
  旧版 `SUPERSEDED`、发放快照、接收人、通知和结构化追溯；任一步失败整批回滚。纸质及
  `BOTH` 领取只能由名单内有效成员在小程序提交批次随机 scene，scene 不得包含业务主键。
- 图纸专用分片上传固定单片 8MB、单文件上限 200MB、会话 24 小时；完整合并后服务端重新计算
  SHA-256、校验大小与真实文件特征。IFC/RVT/DGN 只允许下载并返回 `nosniff`，不得在线预览。
- 无权限访问必须返回真实 HTTP `403`。
- 场内邀请令牌只能通过请求体传输，不得进入 URL、SQL 参数日志、异常信息或普通操作日志。访客手机号、历史身份证号和令牌明文必须使用独立 `VISITOR_DATA_ENCRYPTION_KEY` 做 AES-GCM 加密；只保存令牌摘要用于检索。同行人员单位、姓名、手机号均为选填，完全空白同行项不得写库或计入人数，填写手机号时仍必须校验格式。公开解析与提交必须先按受信 IP 限流；单次预约通过邀请行锁保证整条只成功一次，会议登记还必须使用有效 `uni.login` 微信会话，并通过“邀请 + AppID + 用途隔离身份摘要”唯一约束保证每个身份只有一组有效登记。数据库不得保存明文 OpenID，身份失败不得降级为匿名登记。
- 会议邀请父记录只使用 `OPEN / VOIDED`，到期状态按最新截止时间派生；会议类型创建后不可切换。开放期内可修改时间且新截止必须晚于当前时间，现有凭证实时采用最新截止时间。会议登记组使用 `REGISTERED / VOIDED`，后台只在开放期纠错或作废，截止后仅查看和导出；整场作废立即使全部登记凭证失效，但历史登记和审计保留。
- 每场会议创建时同时生成独立 `MC:` 会场签到码，scene 只包含不透明随机令牌。签到码默认在会议开始前 60 分钟至会议结束启用，每场只允许一个当前码，管理员可调整、停用或轮换；停用、轮换、签到窗口外或会议作废后拒绝新的公开签到。
- 会议时间变化必须在同一事务内同步默认签到窗口，并把自定义结束时间收敛到最新会议结束时间；公开
  签到还必须再次校验会议最新结束时间。读取未配置签到码的历史会议不得在 GET 请求中隐式建码。
- 预约人员只能由原会议登记微信身份逐人勾选签到；未预约人员可在会场码内原子完成 `WALK_IN` 登记和签到。同行人单独到场、人工补录、补签、撤销和更正只由 `site_access.manage` 工作人员执行并强制填写原因。已有有效签到时禁止整组纠错或作废，必须先撤销相关签到。
- 会议签到按人员唯一保存当前 `CHECKED_IN / REVOKED` 状态，多人提交必须在单一事务内完成并对重试、重复扫码和并发提交幂等。微信签到身份摘要使用独立用途隔离；访客原始经纬度不得落库或进入普通日志，只保存范围判断、距离、精度和项目定位版本。定位异常仅提示和留痕，不阻断签到。
- 预约邀请公开解析只可在 `PENDING`，或 `SUBMITTED` 且未到计划离场时间时返回项目导航
  `projectLocation`；坐标由后端按 `BD09 / GCJ02 / WGS84` 统一转换成 `GCJ02`，不得返回项目 ID、
  内部版本或地图 AK。小程序只展示项目标记，不读取访客当前位置；优先使用
  `MapContext.openMapApp({ preferApplication: "baidu" })`，不可用时降级 `openLocation`。门卫固定
  登记码不复用本项改动，除非后续任务明确扩展。

## 访客资料复用与门卫预约匹配（2026-09-10）

- 同一项目、同一 AppID 与微信身份的本人快捷资料由服务端维护；首次明确同意，成功提交后更新，
  关闭记住后不再自动复用或回退命名资料。自动填入单位、姓名、手机号码、出行方式和车牌；同行人
  每次填写。单次预约提交也必须有有效微信短会话，历史无微信归属记录不按手机号认领。
- 单次、会议和门卫的业务身份摘要继续按用途隔离；本人快捷资料使用独立 personal-profile 用途。
  手机号码及资料审计加密，禁止保存原始 OpenID。异步预填不得覆盖用户已编辑的字段。
- 门卫码匹配当前微信身份在预约开始至结束之间有效的单次、会议登记，并保留 24 小时现场登记。
  无有效登记时可多选本项目今天至后六天尚未结束的会议；选择凭证只能绑定本次短会话。门卫登记、
  各会议预约及关联、快捷资料与审计在同一事务完成。已有会议预约复用，门卫预约不产生会场签到。
- 会议大屏由 Web 主入口 meetingScreen 展示分支进入，普通登录仍进入个人待办。名单复用
  site_access.view，签到码复用 site_access.manage；专用响应不得包含手机号、车辆和定位明细。
  每页 24 人、10 秒翻页、5 秒刷新；权限失效清空名单，会议结束隐藏码，二维码按版本缓存。
- 迁移顺序：20260902_site_access_meeting_checkin.sql → 20260910_visitor_reuse_guard_meeting.sql。
  后者增加单次微信归属、本人快捷资料/加密审计和门卫会议关联三表。项目删除影响必须包括三表，
  邀请删除先清理门卫会议关联，保留独立快捷资料。详见 docs/访客复用与门卫会议联动.md。

## 接口与错误处理

- 后端接口统一放在 `/api/v1/**`，Web 通过 Vite `/api` 代理调用。
- 普通 JSON 接口返回 `Result<T>`；文件流接口可返回 `ResponseEntity<Resource>`。
- 生产反向代理必须配置 `FORWARD_HEADERS_STRATEGY=NATIVE`，Nginx 规范设置
  `X-Forwarded-For` 和 `X-Forwarded-Proto`，后端端口不得直接暴露公网。业务代码只能
  使用容器校验后的 `request.getRemoteAddr()`，不得直接信任客户端请求头中的 IP。
- `BusinessException` 由 `GlobalExceptionHandler` 映射为真实 HTTP `400 / 401 / 403 / 404 / 409` 等状态，同时保留响应体业务 `code`。
- 客户端必须同时处理 HTTP 状态和响应体业务消息。
- 新增公共登录、注册、扫码或状态查询接口时，必须补字段校验、Redis 限流和统一错误提示。
- 免登录公开业务读取也必须在数据库查询前执行基于受信客户端 IP 的 Redis 限流；限流
  主体不得以明文写入 Redis，超限使用真实 HTTP `429`。
- 统一扫码调用者没有目标项目巡检权限且电箱公开访问关闭时，必须在巡检范围和内部记录
  查询前返回 HTTP `403`，不得返回电箱编号、名称、位置等元数据；匿名公开扫码不得
  查询或返回当天内部记录。
- 业务字段写入打印页、下载 HTML 或 `document.write` 前必须做 HTML 文本/属性转义；
  二维码图像只接受受控 Data URL，打印文档保留 CSP 且新窗口切断 `opener`。
- 账号密码类入口必须同时按真实客户端 IP 和规范化账号做独立限流，账号维度不得拼接 IP；
  Redis 限流键不得保存明文用户名、手机号、IP、状态令牌或扫码 challenge。
- 当前账号微信绑定、解绑和项目访问申请也必须同时按受信 IP 与规范化账号限流；兼容
  接口共享业务额度，微信解绑的密码校验必须在限流通过后执行。
- 重要写操作必须记录审计日志；授权、账号状态或微信绑定在事务内变化时，必须在提交前
  立即清理权限缓存和相应会话，并在提交后重复清理，防止并发旧请求回写旧授权。首次
  密码设置会在同一事务签发新令牌，不得使用提交后全量注销。
- 密码、账号状态、微信绑定、成员授权、角色、菜单和权限模板等敏感单记录写入必须校验
  数据库影响行数为 1；写入未生效时返回 HTTP `409` 并回滚，不能继续注销会话、替换
  权限或写入成功审计。
- Web 系统管理中的密码重置等敏感表单必须使用系统内受控弹窗，包含明确字段校验、提交状态和结果反馈；不得使用浏览器 `window.prompt` 收集密码，也不得用浏览器 `alert` 展示密码重置结果。
- 电箱主记录和二维码操作日志的单记录写入也必须校验数据库影响行数为 1；写入未生效
  时返回 HTTP `409` 并回滚，不能继续补项目成员授权或返回成功。
- 新建电箱日检时，服务端固定使用 `ELECTRIC_BOX_DAILY / ELECTRICIAN_DAILY`，并锁定
  目标电箱后检查同箱同日唯一性。外观照片和内部照片必须各上传至少 1 张且全部绑定成功；
  请求必须恰好包含六个不重复的规范检查项编码，名称由服务端写入，异常项必须填写说明。
  巡检记录、检查项和巡检设置的单记录写入必须校验影响行数为 1，失败返回 HTTP `409` 并回滚。
- 摄像头和设备的所属项目、主键、删除标记及创建时间只能以后端原记录为准；名称必填，
  字符串边界必须在写库前校验，摄像头在线状态只允许 `0 / 1`。新增、更新和删除影响
  行数不是 1 时返回 HTTP `409`，不得把并发消失的目标报告为操作成功。
- 人员新增和编辑只复制明确允许的业务字段，主键、项目、状态、删除标记和创建时间不得
  由请求体覆盖；状态流转只走进退场/安全教育流程。人员、进退场流水、证件和附件绑定
  写入必须校验影响行数。未绑定证件附件只能由上传人本人绑定，且业务类型和项目必须匹配。
- 安全教育创建必须先完整校验批次字段、去重后的同项目人员和本人刚上传的未绑定培训资料，
  再开始写入。批次、人员关联、人员教育状态和附件绑定必须核对影响行数；完成或删除任一
  步骤未完整生效时返回 HTTP `409` 并回滚，不得留下部分完成的培训批次。
- 项目新增和基础资料编辑只能复制允许的业务字段，主键、删除标记及创建时间由服务端控制；
  项目状态、日期、坐标和字段长度必须先校验。项目定位主记录与审计日志在同一事务写入，
  任一单记录影响行数不是 1 时返回 HTTP `409` 并回滚。
- 项目定位更新必须提交当前 `profile_version` 作为 `expectedVersion`，锁定项目行并重新校验版本；
  地址、经纬度、坐标系、版本递增和定位审计在同一事务完成，冲突返回 HTTP `409`。
- 项目档案 `phase` 表示工程阶段，`project_status` 只表示系统正常/预警/异常/停工状态，不得混用；
  `building_area` 保存数值并同步兼容文本 `area`。保存必须锁定项目并校验 `profile_version`，
  版本冲突返回 HTTP `409`。效果图必须先以 `PROJECT_PROFILE_IMAGE_PENDING` 暂存，保存后才绑定为
  `PROJECT_PROFILE_IMAGE + projectId`；仅支持 JPEG/PNG/WebP、单张 15MB、最多 20 张，移除只归档，
  取消编辑清理本次暂存，超过 24 小时的遗留暂存由定时任务清理。
- 变更电箱巡检范围前必须锁定目标电箱，同箱并发变更串行执行；关闭旧开放区间和新增历史
  区间都必须影响恰好 1 行，失败返回 HTTP `409` 并回滚。原因最长 300 字符，操作人姓名
  使用服务端登录态且最长 50 字符。

## 数据库与迁移

- 2026-08-10 当前本地 `dianxinyun` 曾处于页签菜单部分迁移状态：后续
  `DOCUMENT_SEAL / INSPECTION_RECTIFICATIONS` 已存在，但 6 条基础页签和迁移标记缺失。
  已在完整开发库快照后补执行 `20260803_role_menu_permission_hierarchy.sql`，当前 6 条页签、父子关系、
  角色菜单回填和 `20260803_ROLE_MENU_PERMISSION_HIERARCHY_V1` 均已核对；脚本修正后可无告警幂等复跑。
  前端仍必须保留部分迁移目录防错，不能把单个缺失页签伪装成旧库兼容页。
- 当前有数据的本地 `dianxinyun` 已完成统一注册、RBAC、微信快捷登录、项目多角色、
  跨端业务模块、质量并发、巡检整改、场内外访、常用访客资料、门卫登记、用印一期、质量周检、图纸收发、旧通用巡检、固定式临边转换、临边含图导出、质量问题按日导出、角色授权目录一致性、共享会议邀请和周检/巡检未提交提醒迁移，2026-08-31 核对为
  98 表。项目 3 已通过本地幂等脚本写入带固定演示标记的临边点位、计划、任务、整改和图片；历史 `general_inspection_project_setting`
  仅保留兼容数据，新代码不读取它进行临边鉴权或任务生成。只有从迁移前
  39 表旧副本升级时，
  才按顺序使用以下增量迁移；不得在当前库盲目重跑或改用 `init.sql`：

```text
backend/src/main/resources/sql/migrations/20260728_unified_registration_rbac_wechat_login.sql
backend/src/main/resources/sql/migrations/20260729_project_multi_role_member_management.sql
backend/src/main/resources/sql/migrations/20260729_project_multi_role_permission_cleanup.sql
backend/src/main/resources/sql/migrations/20260729_shared_business_module_access.sql
backend/src/main/resources/sql/migrations/20260729_wechat_quick_registration_initial_password.sql
backend/src/main/resources/sql/migrations/20260730_quality_issue_resilience.sql
backend/src/main/resources/sql/migrations/20260801_inspection_template_item_code_alignment.sql
backend/src/main/resources/sql/migrations/20260803_role_menu_permission_hierarchy.sql
backend/src/main/resources/sql/migrations/20260807_retire_project_member_management_page.sql
backend/src/main/resources/sql/migrations/20260807_site_access_visitor_invitation.sql
backend/src/main/resources/sql/migrations/20260808_seal_application_workflow.sql
backend/src/main/resources/sql/migrations/20260809_project_profile.sql
backend/src/main/resources/sql/migrations/20260810_site_access_reusable_visitor_profile.sql
backend/src/main/resources/sql/migrations/20260810_site_access_visitor_security_hardening.sql
backend/src/main/resources/sql/migrations/20260814_site_access_remove_id_card_requirement.sql
backend/src/main/resources/sql/migrations/20260814_site_access_companion_optional_contact.sql
backend/src/main/resources/sql/migrations/20260814_site_access_guard_visitor_registration.sql
backend/src/main/resources/sql/migrations/20260826_quality_weekly_inspection.sql
backend/src/main/resources/sql/migrations/20260826_document_circulation.sql
backend/src/main/resources/sql/migrations/20260826_general_inspection.sql
backend/src/main/resources/sql/migrations/20260826_general_inspection_fixed_edge.sql
backend/src/main/resources/sql/migrations/20260826_site_access_meeting_invitation.sql
backend/src/main/resources/sql/migrations/20260828_quality_issue_daily_export.sql
backend/src/main/resources/sql/migrations/20260828_edge_inspection_export.sql
backend/src/main/resources/sql/migrations/20260829_role_authorization_consistency.sql
backend/src/main/resources/sql/migrations/20260829_inspection_permission_separation.sql
backend/src/main/resources/sql/migrations/20260829_inspection_submission_reminder.sql
```

`20260803_role_menu_permission_hierarchy.sql` 只增加 6 条页签菜单并一次性回填角色菜单关联，
不增加表或字段。已在隔离副本双跑，但未经备份和明确升级授权不得直接应用到长期本地库或生产库。
`20260807_retire_project_member_management_page.sql` 只撤销并软退役重复的项目成员菜单和权限，
同样不得未经备份和明确升级授权直接应用。
`20260807_site_access_visitor_invitation.sql` 新增 3 张场内管理表及 `SITE_ACCESS` 菜单、权限种子；
当前本地库已于 2026-08-07 应用，表数当时由 47 增至 50；生产库仍须先备份、预检并明确授权。
`20260808_seal_application_workflow.sql` 新增 11 张用印、直接用户审批、抄送和通知表；当前本地库
已于 2026-08-08 在用户明确授权跳过本地备份后应用，表数由 50 增至 61，脚本幂等重跑通过，
迁移标记、2 个菜单、5 项权限及平台管理员关联均已核验。生产库尚未应用，仍须完整备份和预检。
`20260809_project_profile.sql` 幂等扩展 `project_info` 项目档案字段、`profile_version` 和效果图索引，
不新增业务表、菜单或权限种子；当前本地库已在数据库与上传目录备份、隔离库双跑后应用，生产库
尚未应用。真实项目字段和效果图只能通过受控导入进入目标环境，不得写入迁移、测试、文档或 Git。
`20260810_site_access_reusable_visitor_profile.sql` 新增 3 张同项目常用访客资料表，并为邀请增加
`source_profile_id`；当前本地库已在 SQL 快照后应用，表数由 61 增至 64。资料更新/停用不得回写
历史邀请快照，生产环境仍须先备份、预检并单独执行迁移。
`20260810_site_access_visitor_security_hardening.sql` 不增加表或字段，只退役历史人员表中可离线
枚举的未加钥身份证 SHA-256 摘要；新应用统一写入带用途隔离的 HMAC-SHA256。该脚本必须在新
应用部署前执行，当前长期本地库和生产库均不得未经备份、预检及明确升级授权直接应用。
`20260814_site_access_remove_id_card_requirement.sql` 仅将两张人员表的历史身份证列改为可空；
`20260814_site_access_companion_optional_contact.sql` 将人员姓名改为可空并新增单位、加密手机号列。
两者均不删除历史数据，必须在新版应用前执行；当前长期本地库已于 2026-08-14 备份、预检、应用并
复跑幂等，生产库尚未应用，仍须备份、预检、隔离副本双跑及明确升级授权。
`20260814_site_access_guard_visitor_registration.sql` 新增 4 张门卫固定码与登记业务表；当前长期本地库
已于 2026-08-14 在完整备份和隔离库双跑后应用，表数由 64 增至 68，生产库尚未应用。
`20260826_quality_weekly_inspection.sql` 新增质量周检及草稿问题 2 张表，并为 `quality_issue`
增加可空周检归属与条目顺序；历史独立问题不回填虚构批次。当前长期本地库已存在迁移标记和两张表；
生产库状态仍需在发布前单独核对，
不得未经数据库与上传目录备份、预检、隔离副本双跑及明确升级授权应用到长期本地库或生产库。
`20260826_document_circulation.sql` 扩展资料正式类型和版本状态，新增收文、发放、接收人、逐文件
份数及结构化事件共 7 张表，并只为平台管理员补“图纸收发”页签和 4 项操作权限。历史资料只回填
`GENERAL`，不生成收发记录；当前只随代码准备，未经备份、预检、隔离副本双跑及明确授权不得执行。
`20260826_general_inspection.sql` 是已应用的旧开放式通用巡检基线，当前配置库已于 2026-08-26 13:42
写入 `20260826_GENERAL_INSPECTION_V1`；现有 14 张内部表，历史项目开关有 1 条记录，但点位、计划、任务和整改均为 0 条。
不得重写、替换或在当前库重跑该历史脚本。`20260826_general_inspection_fixed_edge.sql` 在其之后幂等转换为
8 类固定临边检查表、`INSPECTION_EDGE` 菜单和 5 项 `EDGE_INSPECTION_*` 权限；当前长期本地库已于
2026-08-27 按用户明确授权跳过备份后直接应用，核对 8 个字段、8 类、8 套版本、40 项、五项权限、
菜单和唯一迁移标记均正常。旧项目开关表继续留作历史兼容，但不再控制临边入口、接口、附件或调度。生产库仍须备份、预检、隔离副本双跑及明确授权后应用。
`20260826_site_access_meeting_invitation.sql` 为 `site_visit_invitation` 增加默认并回填为 `SINGLE` 的
不可变类型，并新增会议登记组、人员和审计 3 张表；不新增菜单或权限。脚本已在隔离临时库连续执行
两次；2026-08-31 又按用户明确授权跳过本地备份，直接在长期本地开发库幂等执行两次。该迁移完成时
本地库为 97 表，6 条旧邀请均回填为 `SINGLE`，唯一索引和迁移标记唯一；后续提醒迁移使当前本地库为
98 表。生产库仍未执行，必须先备份、预检并取得明确升级授权。
`20260902_site_access_meeting_checkin.sql` 为会议登记组补 `INVITATION / WALK_IN` 来源，扩展会议审计
的人员和签到码关联，并新增会场签到码、逐人签到 2 张表；现有会议只回填登记来源，不补签到记录。
脚本及空库基线已在临时隔离库完成“空库基线 + 增量连续两次”验证，表、字段、索引和迁移标记保持
唯一；2026-09-10 按用户本地展示要求，已在备份后应用本地开发库，并顺序应用访客复用迁移，
当前为 103 表。后续开发直接使用原 8080/3002 与本地库，不再另建演示环境。生产尚未应用。
`20260828_quality_issue_daily_export.sql` 为 `quality_issue` 增加不可空 `record_date` 和项目日期状态
索引，新增导出任务及问题快照 2 张表；周检问题按检查日期回填，历史独立问题按创建日期回填。
当前长期本地库已于 2026-08-28 按用户明确授权跳过备份后直接执行，表数由 92 增至 94；已核对
不可空字段、索引、两张任务表、唯一迁移标记及 12 条历史问题日期回填。生产库仍须先备份、预检、
隔离副本双跑并取得明确升级授权。
`20260828_edge_inspection_export.sql` 扩展既有通用巡检导出任务表，增加临边导出类型、统计快照和
多实例领取索引，并新增任务 ID 快照表。当前长期本地库已在数据库与上传目录备份后幂等执行两次，
表数由 91 增至 92；生产仍须按升级手册备份、预检、迁移和验收。
`20260829_role_authorization_consistency.sql` 不新增表、字段、菜单、权限或角色操作授权，只按已有启用
权限补业务模块、跨端根菜单和唯一业务子页面，并清理失效关联、继续退役旧项目成员入口。隔离副本
双跑结果一致；按用户明确要求不做本地备份后已直接应用到长期本地库，表数仍为 94，电工和安全员
操作权限数量未增加。生产仍须按升级手册备份、预检、迁移和验收。
`20260829_inspection_permission_separation.sql` 新增 `EDGE_INSPECTION_EXPORT`，并把旧通用巡检权限字典
明确为电箱口径；只按已有能力和菜单等价迁移、保守清理临边角色的旧技术依赖，禁止按岗位或角色名
重置权限。隔离副本已连续双跑；按用户明确要求跳过本地备份后，迁移已直接应用并幂等复跑长期本地
库，表数仍为 94，角色授权和目录关系已通过运行态验收。生产库尚未执行，仍必须先备份、预检、核对
角色授权快照并取得明确升级授权。
`20260829_inspection_submission_reminder.sql` 新增项目级质量周检提醒设置，并为电箱项目设置增加提醒开关、
生效时间、乐观锁版本和调度索引；临边开关继续保存在版本化计划 JSON。所有既有项目默认关闭。该迁移
已于 2026-08-31 按用户明确授权跳过本地备份后直接应用到长期本地开发库，表数由 97 增至 98；迁移标记、
质量提醒表、电箱三项字段和调度索引均已核对，现有提醒仍全部关闭，电箱页面及下一分钟调度运行正常。
生产库尚未执行，仍须受控预检、迁移和验收。

- `backend/src/main/resources/sql/init.sql` 是禁用兼容入口，直接执行必须失败。全新空库只能
  使用 `scripts/init-empty-database.sh`；工具要求精确确认并拒绝已有任何表的目标库。
- 空库基线模板不包含 `DROP TABLE / DROP DATABASE`。不得绕过工具直接执行模板，不得用
  空库初始化代替增量迁移，也不得为了方便测试重建用户现有数据库。
- `backend/src/main/resources/sql/migrations/` 只允许保存可审计的正式增量迁移，禁止放入
  清库、重置或演示数据脚本。破坏性本地测试夹具只能放在
  `backend/src/test/resources/sql/fixtures/`，且不得对生产库或长期数据环境执行。
- 执行迁移前必须备份数据库和相关文件，检查重复手机号、旧角色、微信绑定、项目授权和至少一个可恢复的平台管理员。
- 迁移必须可重复检查；重复执行不得恢复管理员已经停用的菜单或撤销的角色授权。
- 数据库字段变更必须同步 SQL、Entity、DTO/VO、Service/Controller、前端表单与展示、`docs/数据库结构.md` 和 `docs/接口规则.md`。

## 开发规范

- 修改前先用 `rg`、`rg --files` 和 `sed` 确认真实入口与调用链。
- 当前 Web 根流程仍主要由 `frontend/src/App.jsx` 组织；独立业务页在 `frontend/src/pages/`。不要误改未挂载的旧拆分页。
- 小程序导航和页面守卫必须继续由 `/auth/user-info` 返回的菜单与项目权限驱动，不得恢复固定原生权限入口。
- 新增前端 API 方法放在相应 `services` / `api` 文件，不在页面中重复拼请求。
- 保持现有模块化单体结构和 MyBatis-Plus 写法，修改应小步、聚焦。
- 工作区可能已有用户或其他工具的未提交变更；不得回退、覆盖或清理无关修改。
- 不要读取或修改 `node_modules/`、`dist/`、`backend/target/`、日志、构建产物和无关大文件。
- 不要把真实 AppSecret、数据库密码、JWT、百度地图 AK、生产 token 或用户业务数据写入源码和文档。
- 百度地图只用于 Web 管理端地址解析与人工选点，浏览器端 AK 仅通过
  `VITE_BAIDU_MAP_AK` 注入并配置 Referer 白名单；AK 不得进入小程序包、后端接口或公开响应。
- 不做只有前端显隐、没有后端权限校验的功能。
- 不恢复已隐藏旧模块，不恢复巡检管理中的“用户与权限”双入口。

## 开发前必须阅读

最少阅读：

1. `AGENTS.md`
2. `docs/项目总览.md`
3. `docs/新对话交接文档.md`
4. `docs/开发状态.md`

按任务补充阅读：

- 模块：`docs/模块清单.md`
- 接口：`docs/接口规则.md`
- 数据库：`docs/数据库结构.md`
- 权限：`docs/权限规则.md`
- 小程序与巡检：`docs/小程序需求说明书.md`、`docs/小程序现场检查与电箱检查规格.md`

## 修改后验证与文档同步

- 后端：`cd backend && mvn test`
- Web：`cd frontend && npm run check && npm run audit`（全量源码 ESLint、权限/项目列表核心 JS 类型检查、Node 单测、生产构建和完整依赖审计）
- 小程序：在 `wechat-miniprogram/site-platform-miniprogram` 执行 `npm run type-check`、`npm run build:h5`、`npm run build:mp-weixin`
- 提交前执行 `git diff --check`。

小程序当前 DCloud 编译套件仍声明精确 peer Vite 5.2.8；仓库通过 `.npmrc` 保留已完成 H5、微信及
真实生产构建验证的 Vite 6.4.3 安全兼容例外。由 esbuild 上卷的中风险项属于上游编译链，不能通过
强装 Vite 8、降级回已知高风险版本或执行 `npm audit fix --force` 处理。开发服务器只允许 localhost/私有局域网 Origin，禁止
暴露到公网；DCloud 正式支持安全版 Vite 后再做整套升级。

同步规则：

- 新增或修改模块：更新 `docs/模块清单.md`
- 新增或修改接口：更新 `docs/接口规则.md`
- 新增或修改表字段：更新 `docs/数据库结构.md`
- 修改权限：更新 `docs/权限规则.md`
- 推进重要功能或完成验收：更新 `docs/开发状态.md`
- 改变交接、启动或部署方式：更新 `docs/新对话交接文档.md` 和相关 README
- 生产升级或回滚：必须遵循 `docs/生产升级与回滚手册.md`，先确认授权、可追溯源码、
  数据库/文件/JAR/配置备份、目标库迁移状态和回滚负责人；不得从无法归属的脏工作树
  直接发布，也不得以关闭安全门禁代替正确配置。
