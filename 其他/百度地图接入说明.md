可以。下面这份就是你可以 **按步骤交给 Claude Code 执行的实施清单**。
建议不要一次性全丢进去，而是 **一段一段复制给 Claude Code**，每完成一步你先运行、检查，再继续下一步。

以下默认你的系统是：

```text
后端：Spring Boot 3.x + MySQL + MyBatis-Plus
前端：Vue 3 / Vite
地图：百度地图 JavaScript API / JavaScript API GL
```

------

# 一、Claude Code 实施总原则

你先告诉 Claude Code：

```text
本次只是在现有系统中增加“百度地图项目定位能力”，不要重新设计页面，不要重构已有系统，不要改动已有业务逻辑。

目标是在现有页面的地图区域中加载百度地图，并实现：
1. 后端提供项目地图点位接口；
2. 前端加载百度地图；
3. 用户点击左侧项目时，地图跳转到该项目位置；
4. 地图展示项目 Marker；
5. 点击 Marker 显示项目信息窗口；
6. 所有 projectId 接口必须做项目权限校验；
7. 百度地图 AK 通过环境变量配置，不能写死。
```

------

# 二、第一步：让 Claude Code 先检查项目结构

先不要让它写代码，先让它读项目。

你给 Claude Code 发：

```text
请先检查当前项目结构，不要修改任何代码。

请输出：
1. 后端项目结构；
2. 前端项目结构；
3. 当前项目管理相关代码位置；
4. 当前 project_info 表或项目实体类位置；
5. 当前项目详情接口位置；
6. 当前权限校验逻辑位置；
7. 当前前端项目概况页面位置；
8. 当前左侧项目列表组件位置；
9. 当前是否已经有地图容器或预留区域。

检查完后，只输出分析结果和实施计划，不要开始编码。
```

你要看它有没有找到这些内容：

```text
后端：
project_info 实体类
ProjectController
ProjectService
用户项目权限校验逻辑
数据库 SQL / migration 文件位置

前端：
项目概况页面
左侧项目列表组件
API 请求封装
环境变量配置
```

------

# 三、第二步：后端增加项目定位字段

确认结构后，让它先改数据库和实体。

给 Claude Code 发：

```text
现在开始第一阶段开发：后端增加项目定位字段。

本次只做以下内容：
1. 在 project_info 表中增加字段：
   - longitude 经度
   - latitude 纬度
   - province 省
   - city 市
   - district 区县
   - address 详细地址
   - coordinate_type 坐标系类型，默认 BD09

2. 修改 ProjectInfo 实体类，增加对应字段。

3. 如果项目中有 DTO / VO，也同步增加地图定位相关字段。

4. 新增或补充数据库 SQL 文件，不能直接删除原表。

5. 不要实现地图接口，不要改前端。

字段 SQL 参考：

ALTER TABLE project_info
ADD COLUMN longitude DECIMAL(10,6) COMMENT '经度',
ADD COLUMN latitude DECIMAL(10,6) COMMENT '纬度',
ADD COLUMN province VARCHAR(64) COMMENT '省',
ADD COLUMN city VARCHAR(64) COMMENT '市',
ADD COLUMN district VARCHAR(64) COMMENT '区县',
ADD COLUMN address VARCHAR(255) COMMENT '详细地址',
ADD COLUMN coordinate_type VARCHAR(32) DEFAULT 'BD09' COMMENT '坐标系类型';

完成后请输出：
1. 修改了哪些文件；
2. 新增了哪些字段；
3. 如何执行 SQL；
4. 是否影响已有接口。
```

你检查重点：

```text
不要让它重建 project_info 表
不要让它删旧字段
不要让它直接乱改业务接口
```

------

# 四、第三步：后端新增地图点位接口

确认字段没问题后，再让它加接口。

给 Claude Code 发：

```text
现在开始第二阶段开发：后端新增项目地图接口。

本次只实现以下接口：

1. GET /api/v1/projects/map-points
   功能：返回当前登录用户有权限访问的项目地图点位列表。

2. GET /api/v1/projects/{projectId}/map-detail
   功能：返回单个项目地图详情。

3. PUT /api/v1/projects/{projectId}/location
   功能：更新项目定位信息，仅管理员或项目管理员可操作。

要求：
1. 所有接口必须校验当前用户项目权限。
2. map-points 只返回当前用户有权限的项目。
3. 经纬度为空的项目可以返回给左侧列表，但字段中标记 hasLocation=false；前端不生成 Marker。
4. 坐标系默认 BD09。
5. 更新定位时校验：
   - longitude 范围 -180 到 180；
   - latitude 范围 -90 到 90；
   - coordinateType 必须是 BD09、GCJ02、WGS84 之一。
6. 更新定位后写入操作日志。
7. 不要修改前端。
8. 不要改动其他业务模块。

返回字段参考：
{
  "projectId": 1,
  "projectName": "项目名称",
  "projectStatus": "NORMAL",
  "longitude": 121.453200,
  "latitude": 31.229700,
  "coordinateType": "BD09",
  "province": "上海市",
  "city": "上海市",
  "district": "静安区",
  "address": "上海市静安区xxx路",
  "hasLocation": true,
  "cameraTotal": 32,
  "onlineCameraCount": 28,
  "deviceTotal": 10,
  "alarmDeviceCount": 1,
  "fileTotal": 120,
  "lastUpdateTime": "2026-05-06 18:30:00"
}

完成后请输出：
1. Controller 文件；
2. Service 文件；
3. DTO/VO 文件；
4. 权限校验位置；
5. Swagger 接口说明；
6. 测试请求示例。
```

你检查重点：

```text
map-points 必须按当前用户权限过滤
map-detail 必须校验 projectId 权限
location 更新接口不能让普通用户随便改
```

------

# 五、第四步：后端加测试数据

地图功能没有测试数据不好调。让 Claude Code 加一批项目经纬度。

给它发：

```text
现在生成一份项目地图测试数据 SQL。

要求：
1. 基于已有 project_info 表，生成 UPDATE SQL，不要重建表。
2. 给至少 5 个项目补充经纬度、省、市、区、地址、coordinate_type。
3. 坐标系统一使用 BD09。
4. 测试数据可以使用上海、南京、杭州、苏州、合肥等城市的示例坐标。
5. 只输出 SQL 文件，不要改业务代码。
```

它应该生成类似：

```sql
UPDATE project_info
SET longitude = 121.473701,
    latitude = 31.230416,
    province = '上海市',
    city = '上海市',
    district = '黄浦区',
    address = '上海市黄浦区示例项目',
    coordinate_type = 'BD09'
WHERE id = 1;
```

------

# 六、第五步：前端增加百度地图 AK 配置

后端接口完成后，再做前端。

给 Claude Code 发：

```text
现在开始前端第一阶段开发：增加百度地图 AK 环境变量配置。

本次只做：
1. 在前端环境变量文件中增加：
   VITE_BAIDU_MAP_AK=你的百度地图AK

2. 不要把 AK 写死在组件里。
3. 增加一个地图配置文件或工具方法，用于读取百度地图 AK。
4. 不要开发地图组件，不要改页面布局。

完成后输出：
1. 修改了哪些 env 文件；
2. 地图 AK 从哪里读取；
3. 如果 AK 未配置，前端如何提示。
```

注意：你自己的真实 AK 不要发给 Claude Code，先用占位：

```text
VITE_BAIDU_MAP_AK=REPLACE_WITH_YOUR_BAIDU_MAP_AK
```

------

# 七、第六步：前端封装百度地图加载工具

这一步不要直接写进页面，先封装工具。

给 Claude Code 发：

```text
现在前端第二阶段开发：封装百度地图 JS API 异步加载工具。

本次只做：
1. 新增一个工具文件，例如 src/utils/loadBaiduMap.ts。
2. 通过环境变量读取 VITE_BAIDU_MAP_AK。
3. 动态插入百度地图 script。
4. 避免重复加载 script。
5. 返回 Promise，地图加载成功后 resolve。
6. AK 未配置时 reject，并返回明确错误。
7. 地图加载失败时 reject，并返回明确错误。

不要改页面，不要新增 UI。

工具函数示例：
loadBaiduMap(): Promise<void>

如果项目采用 BMapGL，则加载完成后需要确保 window.BMapGL 可用。
如果项目采用 BMap，则确保 window.BMap 可用。

完成后输出：
1. 新增文件路径；
2. 使用方式示例；
3. 错误处理说明。
```

你检查重点：

```text
不能重复插入多个百度地图 script
AK 不能硬编码
必须能处理加载失败
```

------

# 八、第七步：前端新增地图组件

接下来再加地图组件，但不要大改页面。

给 Claude Code 发：

```text
现在前端第三阶段开发：新增百度地图项目定位组件。

本次新增组件：
src/components/ProjectMap/ProjectMap.vue

组件功能：
1. 接收项目点位数组 props。
2. 初始化百度地图。
3. 根据项目经纬度生成 Marker。
4. 经纬度为空或 hasLocation=false 的项目不生成 Marker。
5. Marker 根据 projectStatus 显示不同颜色。
6. 点击 Marker 后弹出信息窗口。
7. 暴露 locateProject(projectId) 方法，供父组件调用。
8. 地图加载失败时显示错误提示。
9. 不改变现有页面整体布局。

项目点位字段：
projectId
projectName
projectStatus
longitude
latitude
coordinateType
address
hasLocation
cameraTotal
onlineCameraCount
deviceTotal
alarmDeviceCount

状态颜色：
NORMAL：蓝色或绿色
WARNING：橙色
ALARM：红色
DISABLED：灰色

完成后输出：
1. 新增组件文件；
2. 组件 props；
3. 组件暴露方法；
4. 父组件调用示例。
```

如果你的前端是 React，把 `ProjectMap.vue` 改成：

```text
src/components/ProjectMap/ProjectMap.tsx
```

------

# 九、第八步：前端接入项目地图接口

组件有了之后，再接 API。

给 Claude Code 发：

```text
现在前端第四阶段开发：新增项目地图相关 API 请求方法。

本次只做：
1. 在现有 API 目录中增加项目地图接口方法。
2. 实现：
   - getProjectMapPoints(params)
   - getProjectMapDetail(projectId)
   - updateProjectLocation(projectId, data)

接口地址：
GET /api/v1/projects/map-points
GET /api/v1/projects/{projectId}/map-detail
PUT /api/v1/projects/{projectId}/location

要求：
1. 复用项目现有 request 封装。
2. 不直接在组件里写 axios。
3. 保持接口返回类型清晰。
4. 不改页面布局。
```

------

# 十、第九步：把地图组件嵌入现有页面地图区域

这一步才开始改现有页面。

给 Claude Code 发：

```text
现在前端第五阶段开发：把 ProjectMap 组件接入现有项目概况页面的地图区域。

重要要求：
1. 不重新设计页面。
2. 不改变现有整体布局。
3. 只把原先预留的地图区域替换为 ProjectMap 组件。
4. 页面加载时调用 getProjectMapPoints 接口。
5. 左侧项目列表点击项目时，调用 ProjectMap 的 locateProject(projectId) 方法。
6. 如果项目没有经纬度，提示“该项目暂未配置地图定位信息”。
7. 点击地图 Marker 时，右侧或现有详情区域同步显示项目摘要。
8. 地图加载失败时，在地图区域显示失败占位，不影响其他页面功能。

完成后输出：
1. 修改了哪个页面；
2. 如何加载项目点位；
3. 左侧项目点击如何联动地图；
4. 无经纬度项目如何处理。
```

你检查重点：

```text
不能把现有项目概况页面重做一遍
只能嵌入地图区域
左侧列表逻辑不能被破坏
```

------

# 十一、第十步：增加项目定位维护功能，先后端后前端

如果你现在不急着让用户维护定位，可以先跳过。
如果要做，按这个给 Claude Code：

```text
现在增加项目定位维护功能。

本次只在现有项目详情或项目编辑页面中增加定位字段，不新建页面。

需要增加字段：
longitude
latitude
province
city
district
address
coordinateType

要求：
1. 只有平台管理员或项目管理员能编辑。
2. 经纬度必须校验范围。
3. coordinateType 可选值：BD09、GCJ02、WGS84，默认 BD09。
4. 保存时调用 PUT /api/v1/projects/{projectId}/location。
5. 保存成功后刷新地图点位。
6. 不重新设计页面，只在现有表单中增加字段。
```

------

# 十二、第十一步：加 Redis 缓存，最后做

前面功能跑通后，再让 Claude Code 做缓存，不要一开始就做。

给它发：

```text
现在为项目地图接口增加 Redis 缓存。

本次只优化以下接口：
GET /api/v1/projects/map-points
GET /api/v1/projects/{projectId}/map-detail

缓存 Key：
project:map-points:{userId}
project:map-detail:{projectId}

缓存时间：
map-points：5分钟
map-detail：1分钟

以下操作后清理缓存：
1. 更新项目定位信息；
2. 修改项目状态；
3. 修改用户项目权限；
4. 新增或删除项目。

要求：
1. 缓存不能绕过权限校验。
2. map-points 缓存必须按 userId 区分。
3. 不要缓存未授权数据。
4. 保留无缓存时的正常查询逻辑。
```

------

# 十三、你每一步都要让 Claude Code 自检

每次任务最后，你都补一句：

```text
完成后请自检：
1. 是否改动了无关文件；
2. 是否破坏已有接口；
3. 是否遗漏 projectId 权限校验；
4. 是否能正常编译；
5. 是否有清晰的测试方法；
6. 请列出本次修改文件清单。
```

这句话很重要。

------

# 十四、推荐你的执行顺序总结

你实际就按这个顺序来：

```text
1. 检查项目结构，不改代码
2. 后端加地图字段
3. 后端加地图接口
4. 后端加测试数据
5. 前端加百度地图 AK 配置
6. 前端封装百度地图加载工具
7. 前端新增 ProjectMap 组件
8. 前端新增地图 API 方法
9. 把地图组件嵌入现有页面
10. 做项目定位维护功能，可选
11. 加 Redis 缓存，可最后做
12. 联调测试
```

------

# 十五、最终你可以先给 Claude Code 的第一段完整指令

你现在第一步可以直接发这个：

```text
请先检查当前项目结构，不要修改任何代码。

当前需求是在现有系统中增加“百度地图项目定位能力”，不是重新设计页面。

功能目标：
1. 在现有页面地图区域中加载百度地图；
2. 后端提供项目地图点位接口；
3. 左侧项目列表点击项目后，中间地图跳转到该项目位置；
4. 地图展示项目 Marker；
5. 点击 Marker 展示项目信息窗口；
6. 所有 projectId 接口必须校验当前用户项目权限；
7. 百度地图 AK 通过环境变量配置，不能写死。

请先输出：
1. 当前后端项目结构；
2. 当前前端项目结构；
3. 项目表、项目实体、项目接口所在位置；
4. 当前权限校验逻辑所在位置；
5. 项目概况页面和左侧项目列表所在位置；
6. 你建议本功能分几步开发；
7. 每一步会新增或修改哪些文件。

注意：本次不要开始编码，只做分析和实施计划。
```

等它分析完，你再从“第二步：后端增加项目定位字段”开始。