你可以把这件事理解成：

**现在不是马上接视频流，而是先把“摄像头档案信息”补完整。**

也就是说，你要先让系统能记录每个摄像头的：

- 装在哪里
- 用什么协议接入
- 接入哪个平台
- 国标设备 ID 是多少
- 是否支持云台、巡航、夜视
- 视频编码、分辨率、帧率是否符合要求
- 视频存储多少天
- 在线率是否达标

你上传的文件里明确提到：工地视频基础点位要覆盖人员出入口、塔吊/制高点、基坑及危大工程作业区；摄像机应支持 GB/T28181-2016、H.264 编码、高清画面、云台、巡航、夜视等要求；视频存储一般项目 1 个月、市管项目 3 个月，设备在线率不得低于应运行天数的 80%。所以我们才要补这些字段。

------

# 一、你现在到底要做什么

你现在要做的是这个小模块：

```text
摄像头资源管理字段增强
```

不是接海康视频流，不是写播放器，也不是调海康接口。

它属于后端和前端的基础数据补充。

完整步骤是：

```text
第1步：明确摄像头需要记录哪些信息
第2步：数据库 camera_resource 表增加字段
第3步：后端实体类增加字段
第4步：后端新增/编辑/详情接口支持这些字段
第5步：前端摄像头新增/编辑表单增加这些字段
第6步：前端摄像头详情页展示这些字段
第7步：录入测试数据
第8步：验证接口和页面是否正常
```

------

# 二、第一步：先确定这些字段是放在哪里

这些字段不是放在项目表，也不是放在视频播放表，而是放在：

```text
camera_resource 摄像头资源表
```

因为它们描述的是“某一个摄像头本身的信息”。

你可以这样理解：

```text
project_info
用于记录项目

camera_resource
用于记录项目下的摄像头

video_layout_config
用于记录视频窗口布局

video_access_log
用于记录谁看了视频
```

这批字段全部属于：

```text
camera_resource
```

------

# 三、第二步：数据库增加字段

你要让 Claude Code 或 OpenClaw 做的第一件事就是：

**在 camera_resource 表里增加这些字段。**

你可以给它这个任务：

```text
现在只修改数据库，不写业务逻辑。

目标：
在 camera_resource 表中增加摄像头接入和设备参数字段。

新增字段：
gb_device_id
install_position
access_protocol
access_platform
storage_days
bandwidth_mbps
online_rate
video_resolution
video_codec
fps
ptz_supported
cruise_supported
night_vision_supported

要求：
1. 只输出 ALTER TABLE SQL。
2. 不要删除原有字段。
3. 不要重建 camera_resource 表。
4. 不要修改其他表。
```

SQL 可以这样写：

```sql
ALTER TABLE camera_resource
ADD COLUMN gb_device_id VARCHAR(128) COMMENT '国标设备ID',
ADD COLUMN install_position VARCHAR(64) COMMENT '安装位置：人员出入口/塔吊制高点/危大工程作业区等',
ADD COLUMN access_protocol VARCHAR(64) DEFAULT 'GB28181' COMMENT '接入协议：GB28181/RTSP/HIKCENTRAL/HIKCONNECT/FLV/HLS/WEBRTC/MOCK',
ADD COLUMN access_platform VARCHAR(128) COMMENT '接入平台：上海市建筑工地安全监管平台/天翼视联/海康平台/流媒体平台等',
ADD COLUMN storage_days INT COMMENT '视频存储天数，如30天/90天',
ADD COLUMN bandwidth_mbps DECIMAL(8,2) DEFAULT 4.00 COMMENT '预估带宽，单位Mbps',
ADD COLUMN online_rate DECIMAL(5,2) COMMENT '在线率，百分比',
ADD COLUMN video_resolution VARCHAR(64) DEFAULT '1920x1080' COMMENT '视频分辨率',
ADD COLUMN video_codec VARCHAR(32) DEFAULT 'H.264' COMMENT '视频编码格式',
ADD COLUMN fps INT DEFAULT 25 COMMENT '视频帧率',
ADD COLUMN ptz_supported TINYINT DEFAULT 0 COMMENT '是否支持云台：1支持，0不支持',
ADD COLUMN cruise_supported TINYINT DEFAULT 0 COMMENT '是否支持巡航：1支持，0不支持',
ADD COLUMN night_vision_supported TINYINT DEFAULT 0 COMMENT '是否支持夜视：1支持，0不支持';
```

这一步完成后，你的数据库才能保存这些信息。

------

# 四、第三步：后端实体类增加字段

数据库字段加完以后，后端代码里也要同步。

也就是找到你的摄像头实体类，通常叫：

```text
CameraResource.java
```

然后增加这些属性。

你可以让 Claude Code 这样做：

```text
现在修改后端 CameraResource 实体类。

要求：
1. 根据 camera_resource 表新增字段，补充实体类属性。
2. 字段命名使用驼峰格式。
3. 不要修改已有字段。
4. 不要改业务逻辑。

新增属性：
gbDeviceId
installPosition
accessProtocol
accessPlatform
storageDays
bandwidthMbps
onlineRate
videoResolution
videoCodec
fps
ptzSupported
cruiseSupported
nightVisionSupported
```

实体类大概会是这样：

```java
private String gbDeviceId;
private String installPosition;
private String accessProtocol;
private String accessPlatform;
private Integer storageDays;
private BigDecimal bandwidthMbps;
private BigDecimal onlineRate;
private String videoResolution;
private String videoCodec;
private Integer fps;
private Boolean ptzSupported;
private Boolean cruiseSupported;
private Boolean nightVisionSupported;
```

注意：数据库里是 `tinyint`，后端可以转成 `Boolean`。

------

# 五、第四步：后端 DTO / VO 也要同步

光改实体类还不够。

因为前端新增、编辑、查看摄像头时，一般不会直接用 Entity，而是用：

```text
CameraCreateDTO
CameraUpdateDTO
CameraDetailVO
CameraListVO
```

你要让 Claude Code 检查当前项目里有没有这些类。

然后这样要求它：

```text
现在同步修改摄像头新增、编辑、详情相关 DTO / VO。

要求：
1. 新增摄像头 DTO 增加这些字段。
2. 编辑摄像头 DTO 增加这些字段。
3. 摄像头详情 VO 增加这些字段。
4. 摄像头列表 VO 可以只展示部分字段。
5. 不要影响播放地址接口。

字段包括：
gbDeviceId
installPosition
accessProtocol
accessPlatform
storageDays
bandwidthMbps
onlineRate
videoResolution
videoCodec
fps
ptzSupported
cruiseSupported
nightVisionSupported
```

建议列表页只显示这些：

```text
摄像头名称
安装位置
接入协议
国标设备ID
在线状态
在线率
存储天数
```

详情页再显示全部字段。

------

# 六、第五步：增加枚举值，避免乱填

安装位置和接入协议不要让用户随便乱写。

建议做成下拉选项。

## 安装位置 install_position

```text
PERSON_ENTRANCE    人员出入口
TOWER_CRANE        塔吊/制高点
DANGER_AREA        基坑/危大工程作业区
LIVING_AREA        生活区
MATERIAL_AREA      材料区
OTHER              其他
```

## 接入协议 access_protocol

```text
GB28181       国标协议接入
RTSP          RTSP流接入
HIKCENTRAL    海康综合平台接入
HIKCONNECT    海康云接入
FLV           FLV播放地址接入
HLS           HLS播放地址接入
WEBRTC        WebRTC播放接入
MOCK          模拟测试接入
```

你可以让 Claude Code 这样做：

```text
现在新增摄像头相关枚举。

要求：
1. 新增 InstallPositionEnum。
2. 新增 AccessProtocolEnum。
3. 新增字段校验，installPosition 和 accessProtocol 只能使用枚举值。
4. 不要影响已有接口。
```

------

# 七、第六步：后端接口要支持新增字段

如果你已有摄像头新增/编辑接口：

```text
POST /api/v1/cameras
PUT /api/v1/cameras/{cameraId}
GET /api/v1/cameras/{cameraId}
GET /api/v1/cameras
```

那现在这些接口要支持新字段。

你给 Claude Code 的任务可以这样写：

```text
现在修改摄像头管理接口，使其支持新增字段。

接口范围：
1. 新增摄像头
2. 编辑摄像头
3. 摄像头详情
4. 摄像头列表

要求：
1. 新增接口可以保存 gbDeviceId、installPosition、accessProtocol 等字段。
2. 编辑接口可以修改这些字段。
3. 详情接口返回全部字段。
4. 列表接口返回核心字段。
5. 播放地址接口 /play-url 不要改。
6. 所有接口继续保持 projectId 权限校验。
```

这里一定注意：

```text
不要改 /api/v1/cameras/{cameraId}/play-url
```

因为播放地址是另外一个逻辑。

这次只是补摄像头档案字段。

------

# 八、第七步：前端新增/编辑摄像头表单增加字段

后端支持后，前端表单也要有地方填写。

你前端如果有摄像头管理页面，就在“新增摄像头 / 编辑摄像头”表单里增加这些字段。

建议分组：

```text
基础信息
├── 摄像头名称
├── 摄像头编号
├── 国标设备ID
├── 所属项目
├── 所属区域
└── 安装位置

接入信息
├── 接入协议
├── 接入平台
├── 视频流类型
├── 视频流地址/平台标识
├── 存储天数
└── 预估带宽

设备参数
├── 分辨率
├── 编码格式
├── 帧率
├── 是否支持云台
├── 是否支持巡航
└── 是否支持夜视

运行状态
├── 在线状态
├── 在线率
├── 最近在线时间
└── 备注
```

给 Claude Code 的任务：

```text
现在修改前端摄像头新增/编辑表单。

要求：
1. 不重新设计页面。
2. 只在现有摄像头表单中增加字段。
3. 安装位置使用下拉框。
4. 接入协议使用下拉框。
5. 存储天数使用下拉框：30、90。
6. 是否支持云台、巡航、夜视使用开关。
7. 分辨率默认 1920x1080。
8. 编码格式默认 H.264。
9. 帧率默认 25。
10. 提交时把新增字段一起传给后端。
```

------

# 九、第八步：摄像头详情页展示这些字段

新增/编辑能录入后，详情页要能看。

你可以让 Claude Code 做：

```text
现在修改摄像头详情展示。

要求：
1. 摄像头详情页展示新增字段。
2. 字段需要显示中文名称。
3. 枚举字段显示中文，不显示编码。
4. 布尔字段显示“支持/不支持”。
5. 不改视频播放逻辑。
```

显示效果类似：

```text
国标设备ID：31011536001320000326
安装位置：人员出入口
接入协议：GB28181
接入平台：天翼视联
存储天数：30天
预估带宽：4Mbps
在线率：96.5%
分辨率：1920x1080
编码格式：H.264
帧率：25fps
云台：支持
巡航：支持
夜视：支持
```

------

# 十、第九步：录入一条测试数据

你可以先不用真实摄像头，录一条测试数据。

示例：

```text
摄像头名称：1号人员出入口摄像头
摄像头编号：CAM001
国标设备ID：31011536001320000326
安装位置：人员出入口
接入协议：GB28181
接入平台：天翼视联
存储天数：30
预估带宽：4
在线率：95
分辨率：1920x1080
编码格式：H.264
帧率：25
支持云台：是
支持巡航：是
支持夜视：是
```

测试 SQL：

```sql
UPDATE camera_resource
SET gb_device_id = '31011536001320000326',
    install_position = 'PERSON_ENTRANCE',
    access_protocol = 'GB28181',
    access_platform = '天翼视联',
    storage_days = 30,
    bandwidth_mbps = 4.00,
    online_rate = 95.00,
    video_resolution = '1920x1080',
    video_codec = 'H.264',
    fps = 25,
    ptz_supported = 1,
    cruise_supported = 1,
    night_vision_supported = 1
WHERE id = 1;
```

------

# 十一、第十步：测试接口

你需要测试这几个点：

## 1. 摄像头列表

看看是否能返回新增字段：

```text
GET /api/v1/cameras?projectId=1
```

检查是否有：

```text
gbDeviceId
installPosition
accessProtocol
storageDays
onlineRate
```

## 2. 摄像头详情

```text
GET /api/v1/cameras/1
```

检查是否返回全部字段。

## 3. 编辑摄像头

```text
PUT /api/v1/cameras/1
```

检查是否能保存新字段。

## 4. 播放地址接口

```text
GET /api/v1/cameras/1/play-url
```

检查这个接口有没有被影响。

这一步很重要，因为这次字段增强不应该破坏视频播放接口。

------

# 十二、你实际交给 Claude Code 的分步任务

你不要一次性全丢。按下面 6 次来。

------

## 任务 1：数据库字段

```text
本次只修改数据库 SQL。

请在 camera_resource 表中增加摄像头接入和设备参数字段：
gb_device_id、install_position、access_protocol、access_platform、storage_days、bandwidth_mbps、online_rate、video_resolution、video_codec、fps、ptz_supported、cruise_supported、night_vision_supported。

要求：
1. 只输出 ALTER TABLE SQL。
2. 不要重建表。
3. 不要删除原字段。
4. 不要修改业务代码。
```

------

## 任务 2：后端实体和 DTO

```text
本次只修改后端摄像头实体类、DTO、VO。

要求：
1. CameraResource 实体类增加新增字段。
2. 新增/编辑摄像头 DTO 增加新增字段。
3. 摄像头详情 VO 增加新增字段。
4. 摄像头列表 VO 增加核心字段。
5. 不要修改 Controller 逻辑。
6. 不要修改视频播放接口。
```

------

## 任务 3：后端接口支持字段保存和返回

```text
本次修改摄像头管理接口，让新增字段可以保存和返回。

要求：
1. 新增摄像头接口支持新增字段。
2. 编辑摄像头接口支持新增字段。
3. 摄像头详情接口返回全部新增字段。
4. 摄像头列表接口返回核心新增字段。
5. 所有接口继续保持 projectId 权限校验。
6. 不要修改 /play-url 播放地址接口。
```

------

## 任务 4：前端表单

```text
本次修改前端摄像头新增/编辑表单。

要求：
1. 增加国标设备ID、安装位置、接入协议、接入平台、存储天数、预估带宽、分辨率、编码格式、帧率、是否支持云台、是否支持巡航、是否支持夜视字段。
2. 安装位置和接入协议用下拉框。
3. 是否支持云台、巡航、夜视用开关。
4. 默认值：
   accessProtocol=GB28181
   storageDays=30
   bandwidthMbps=4
   videoResolution=1920x1080
   videoCodec=H.264
   fps=25
5. 不重新设计页面。
```

------

## 任务 5：前端详情展示

```text
本次修改前端摄像头详情展示。

要求：
1. 展示新增字段。
2. 枚举值显示中文。
3. 布尔值显示“支持/不支持”。
4. 不修改视频播放组件。
```

------

## 任务 6：测试数据和自检

```text
本次生成测试数据和自检清单。

要求：
1. 生成一条摄像头测试数据 SQL。
2. 测试数据包含国标设备ID、安装位置、接入协议、平台、存储天数、分辨率、编码格式、帧率、云台、巡航、夜视。
3. 给出接口测试步骤。
4. 确认 /play-url 播放地址接口未被影响。
```

------

# 十三、你要记住一句话

这一步做的是：

```text
摄像头基础档案增强
```

不是：

```text
真实视频接入
```

不是：

```text
海康接口联调
```

不是：

```text
播放器开发
```

你现在只要先做到：

```text
系统能完整记录和展示摄像头接入参数、设备参数、国标ID、安装位置、存储天数、在线率
```

然后下一步才能做：

```text
根据 gbDeviceId / accessProtocol / accessPlatform 去获取真实视频播放地址
```

这样顺序才是对的。