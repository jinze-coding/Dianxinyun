# 新对话交接文档

## 2026-09-11 小程序会议页展示调整

会议页已将访客导航和会议资料收为基本信息下方的两个按钮，点击进入同页居中悬浮窗。
登记表单保留；弹窗关闭和文件预览返回保留资料筛选与列表位置，过期/作废继续实时校验。
原 dist/dev/mp-weixin 已热更新；专项使用 npm run test:meeting-dialogs，类型、扫码回归及两端构建已通过。
无数据库/接口变更，直接使用当前 8080/3002 和小程序开发版；真机导航/文档返回仍需设备验收。

## 2026-09-11 会议完整详情与资料留档

会议改为系统白蓝主题完整页面，默认会场签到，下设预约登记/会场签到/会议资料/操作记录。
资料按文件控制内部或访客可见，支持 1GB 分片续传、不可变版本、页内预览及会后公开资料读取。
原本地库已备份并幂等应用 20260911 迁移两遍，当前 106 表，8080/3002 直接使用当前源码。
当前日期演示主会议为项目 3、会议 29；旧日期演示记录保留。小程序开发包仍是原 dist/dev/mp-weixin。
设计、接口、转换器、迁移、实测与设备验收边界见 [会议完整详情与资料留档](会议完整详情与资料留档.md)。

> 2026-09-10 大屏 UI 修正：大屏统一使用系统白色主题，预约列表操作按钮统一样式及对齐。
> “返回上一页”恢复来源项目和列表筛选/页码/滚动位置；直接打开的会议大屏回该项目邀请列表。
> 普通登录仍进入个人待办。Web check 160 项全绿、审计 0 漏洞，修改已由本地 3002 提供。

> 2026-09-10 本地展示更新：用户明确要求直接使用现有开发系统，不再维护独立演示入口。
> 原 `dianxinyun` 已备份并应用会议签到和访客复用两项迁移，当前 103 表；8080 后端已重启。
> 3002 当前项目（本地项目 3）新增 10 条测试邀请，今日可见 6 条；主会议 ID 19，预约 40、已签 35、
> 待签 10，另有 2 组门卫登记。原 15 条邀请及门卫码保留。详见 [本地测试数据](访客与会议演示数据.md)。
> 后续开发直接更新当前代码、8080/3002 服务与本地库，不再另建演示环境；生产未发布。

> 2026-09-10 访客复用与门卫会议联动：三种访客入口已共享同项目微信本人的快捷资料，首次同意后
> 成功提交更新；自动预填保护已编辑字段。单次预约增加微信归属，门卫码匹配预约有效时段并支持
> 现场多选近期会议原子报名，门卫放行与会场逐人签到分开。会议列表新增签到码和独立签到大屏。
> 实现及复跑方法见 [访客复用与门卫会议联动](访客复用与门卫会议联动.md)。
> 本轮已备份本地数据库/上传目录，在隔离副本及空库验证迁移双跑；历史副本 103 表，空库补认证
> 基础后 101 表（不含两张历史微信消息兼容表）。后端 908 项、Web 156 项及构建审计、小程序检查
> 和双端构建均通过；单独导出本次提交树后，后端 908 项、Web 146 项再次通过。隔离浏览器已核验
> 大屏翻页、断网恢复、结束与登录失效处理。
> 本地开发库已在后续展示更新中升级到 103 表；生产未部署，微信真机与现场投屏仍待验收。
> 原固化 11 项/98 表发布包不能直接用于本版，正式组包须加入两项新增迁移并重新验证发布门禁。


> 2026-09-02 共享会议现场签到交接（最新）：`MEETING` 创建现同时生成 `M:` 邀请登记码和独立
> `MC:` 会场签到码。小程序新增免系统账号签到页：预约微信身份只读取本人登记组并逐人、分批签到，
> 未预约人员可原子完成现场补录和签到；定位异常只提示，服务端仅保存范围结果、距离、精度和项目定位
> 版本，不保存原始经纬度。
>
> Web 会议详情新增“预约登记管理 / 会场签到管理”，签到工作台可设置、停用、轮换和下载会场码，
> 查看预约/已签/未签/现场补录/签到率，按来源、状态和定位筛选，并受控执行人工补录、补签、撤销、
> 更正及两 Sheet 导出。权限继续复用 `site_access.view/manage/export`；已有有效签到的登记组必须先
> 撤销签到，才能整组纠错或作废。
>
> 新迁移 `20260902_site_access_meeting_checkin.sql` 新增签到码和逐人签到两表，补登记来源及审计关联；
> 空库基线已同步，并在临时隔离库完成基线后增量连续两次验证，历史数据不补签到记录。长期本地库和
> 生产库均未执行；不要因页面已编译完成就直接在现有库试用新接口。下一步必须先取得迁移授权，再在
> 微信开发者工具和真机完成定位授权、多人分批签到、现场补录和 Excel 对账。
> 最终自动回归为后端 885 项全绿、Web 142 项及 lint/类型检查/生产构建全绿、小程序类型检查、扫码
> 契约与微信构建全绿，`git diff --check` 通过；这些结果不代表长期本地库已升级或真实跨端验收完成。

> 2026-09-01 图纸收发 Web 任务化交接（最新）：图纸收发默认进入工作台，五阶段流程、五类状态统计和
> 异议/逾期优先待处理列表可直接说明完整闭环；收文改为四步向导，发放详情改为“下一步 + 签收进度 +
> 精确版本 + 逐人状态 + 受控更多操作”。现有后端、数据库、RBAC、发布事务、二维码和台账接口未改。
> 新增 3 项页面契约后，Web 共 139 项测试以及 lint、类型检查、生产构建通过；本地浏览器已只读验收
> 工作台和异议详情，并保存既有演示草稿验证第 4 步候选人加载，未正式发布或发送通知。

> 2026-09-01 正式快照兼容交接（最新）：正式备份已完整覆盖本地开发库与附件，覆盖前本地数据另有
> 检查点。68 表、13 个标记的正式快照已顺序补齐 11 项迁移到 98 表、24 个原迁移标记；安全复跑、
> 行数/语义摘要、逻辑关联、239 份附件及核心 API/Web 页面均已对账。
>
> 快照中 434 个访客密文有 7 个由早期开发默认密钥生成，原来会令两条邀请详情返回 500。当前代码
> 新增只在专用 Profile 与显式开关同时启用时存在的离线 `VERIFY / APPLY` 工具；两次从原检查点开始
> 的演练均只修复同一 7 个密文，修复后 434/434 当前密钥可解、15/15 邀请详情成功，并写入
> `20260901_SITE_ACCESS_REENCRYPT_LEGACY_DEVELOPMENT_KEY_V1`。禁止把开发密钥设为生产密钥或加入
> 在线回退；正式库必须在停机窗口重新 `VERIFY`，不能复用本地计数或候选指纹。
>
> Web 顶部导航的 1280px 负溢出已修复并实测；小程序下一正式候选身份为 `0.1.8 / 108 /
> 0.1.8-20260901-prod-compat`，增强校验器覆盖 43 页和当前关键业务链路，正式构建已显式关闭 DCloud
> `uniStatistics`。旧 Web 会调用已由新后端返回 410 的独立质量创建接口，因此正式 Web 已拆为
> `transition` 与 `final`：停服期间先切关闭会议创建的 transition，再启动新后端；通过旧正式小程序
> 兼容停点并确认 0.1.8 用户端生效后才切 final。生产脚本已锁定 68 表/13 标记基线、11 项迁移、同点
> 备份/回滚和上述阶段证明。正式环境尚未部署，仍需同源不可变产物封存、图纸 scene 独立密钥、微信
> 平台合法域名/隐私和双端真机证据。

> 2026-08-31 共享会议邀请码本地收口（最新）：用户明确授权跳过本地备份和隔离库后，长期本地
> `dianxinyun` 已连续两次执行 `20260826_site_access_meeting_invitation.sql`，当时由 94 表增至 97 表。
> 6 条旧邀请全部保持原状态并回填为 `SINGLE`，三张会议表、有效身份唯一索引和迁移标记均唯一。
>
> 本地已登录 Web 创建 `LOCAL-MEETING-QA-20260831` 会议并以 A/B/C 三个微信 mock 身份走真实数据库
> 链路：A 登记复扫只见 A，B 首扫仍见表单；同身份并发两次只生成一组；单组作废后可重登；截止
> 调整实时作用于凭证；整场作废后不返回凭证、导航且会话为 HTTP 410。验收会议最终保留为
> `VOIDED`，历史登记和审计未删除。
>
> 本轮同时修复微信换码期间作废/到期竞态、空白 OpenID、会议导出及二维码权限映射、Web 跨项目和
> 筛选竞态、截止后只读弹窗，以及小程序回前台后未刷新单组/整场/最新截止的问题。后端全量 847 项、
> Web 126 项及生产构建、小程序类型检查/完整扫码脚本/微信构建全部通过，四项开发服务已重启。
> 生产库仍未迁移，正式小程序仍未发布；发布顺序和正式双微信验收要求保持不变。

> 2026-08-29 周检与巡检未提交站内提醒交接（最新）：质量周检、电箱日检、固定临边巡检均已接入
> 模块内 Web 规则配置和现有个人消息中心。质量配置唯一责任人及星期/时间；电箱复用日检截止时间并
> 展示责任电工异常；临边复用任务截止时间。所有既有项目默认关闭，每个周次、箱日或任务使用固定
> `dedup_key` 只提醒一次，完成业务不会自动把历史通知改为已读。
>
> Web 和小程序共用现有 `/me/inbox`、未读红点及受控路由；新增消息分组
> `businessGroup=QUALITY|INSPECTION` 和质量路由 `QUALITY_WEEKLY_INSPECTION_WEEK`。小程序不提供
> 规则配置。本期没有微信、短信、系统弹窗或外部 Outbox。迁移
> `20260829_inspection_submission_reminder.sql` 已于 2026-08-31 按用户明确授权跳过本地备份后直接应用
> 到长期本地库，表数由 97 增至 98；唯一标记、质量提醒表、电箱三项字段和调度索引均已核对，现有
> 提醒仍全部关闭。项目 3 电箱台账已恢复显示 1 个电箱、今日应检 1、已检 0、未检 1，巡检设置弹窗
> 正常加载；跨过下一分钟调度边界后未再出现缺表或缺字段错误。生产库仍未应用，上线前继续执行受控
> 预检、责任配置检查、迁移和运行态验收。
>
> 调度候选已预先排除已通知、已正式提交和非应检数据，并在锁内重新校验；Web 设置页使用项目请求
> 序号隔离迟到响应。账号停用前提供责任影响预览并要求确认；平台管理员身份撤销、角色权限收缩或
> 全局停用权限若会使现有提醒责任失去处理能力，则返回 HTTP 409，要求先改派，不自动清空责任。

> 2026-08-29 巡检菜单专区交接（最新）：角色“分配菜单”中的巡检目录已增加前端逻辑层，显示为
> `巡检管理 → 电箱巡检 / 临边巡检`。电箱专区展开原电箱台账、巡检记录、整改闭环三个真实菜单，
> 临边专区独立控制 `INSPECTION_EDGE`；逻辑专区不新增数据库菜单、菜单 ID、接口或迁移。分组全选、
> 半选、逐页取消和跨专区保留规则均有自动化测试，并已在项目总工程师的已登录弹窗中只读验收；未保存
> 角色变更。Web 当前为 108 项测试通过，最新本地服务已重启。

> 2026-08-29 巡检权限分离交接（最新）：新增
> `20260829_inspection_permission_separation.sql` 和 `EDGE_INSPECTION_EXPORT`。电箱专区继续使用
> `inspection.* / BOX_* / INSPECTION_* / SUMMARY_*`，临边专区只使用六项
> `EDGE_INSPECTION_*`；临边含图导出改为 `EDGE_INSPECTION_VIEW + EDGE_INSPECTION_EXPORT`，不再
> 依赖电箱汇总或导出权限。
>
> 迁移先把已有 `EDGE_INSPECTION_VIEW + SUMMARY_EXPORT + inspection.export` 角色等价转换为新临边
> 导出权限，再只对持临边权限的角色按真实电箱能力和电箱页面清理旧技术依赖。它不删除电箱细分
> 权限、不按岗位或角色名重置权限，并为任一临边权限角色补齐 `INSPECTION` 模块、两端根菜单和
> `INSPECTION_EDGE`。空库基线、演示安全员导出授权及契约测试已同步。
>
> 迁移已在完整 94 表隔离副本连续执行两次；按用户明确要求跳过本地备份后，已直接应用并幂等复跑
> 长期本地 `dianxinyun`。表数仍为 94，迁移标记唯一，六项临边权限启用，临边角色菜单/模块缺口为
> 0。已登录 Chrome 实测权限弹窗只保留“电箱巡检 / 临边巡检”两组：电工 2+3 项，安全员 4+4 项，
> 原“巡检记录 / 共用统计与导出 / 整改闭环”模糊分组已消失。生产库仍未执行，部署前必须先备份、
> 预检、迁移并对账执行前后的角色权限、菜单和模块。

> 2026-08-29 角色授权目录一致性交接：新增幂等迁移
> `20260829_role_authorization_consistency.sql`。它不建表、不新增菜单或权限、不写
> `sys_role_permission`，只从角色已有启用权限补正式业务模块、Web/小程序根菜单和能够唯一确定的
> 子页面。临边 `EDGE_INSPECTION_*` 固定对应 `INSPECTION_EDGE`；图纸收发、用印、`BOX_*`、电箱
> 日检/记录、巡检整改和质量整改也使用明确页面映射。共享多页权限只补模块与根菜单，不自动扩页。
>
> 迁移会清理指向停用、删除或不存在菜单/权限的关联，`visible=0` 不单独触发清理，并兼容遗漏
> `20260807_RETIRE_PROJECT_MEMBER_MANAGEMENT_PAGE_V1` 的环境：继续停用
> `SYSTEM_PROJECT / project.member.manage` 并补旧标记，不恢复旧项目成员范围。演示脚本在补电工、
> 安全员既定临边权限后同步 `INSPECTION` 模块、两端根菜单和临边页面，56 项任务口径不变。
> 隔离副本已连续执行两次且结果一致；按用户此前明确要求不做本地备份，迁移已直接应用到长期本地
> `dianxinyun`，表数仍为 94，迁移标记唯一，失效菜单/权限关系均为 0。电工保持 7 项操作权限并显示
> 临边查看、执行、整改；安全员保持 10 项并显示临边查看、管理、复查。后端 787 项测试、Web lint、
> TypeScript、104 项测试和生产构建通过，服务已重启；Chrome 已登录页面实测权限弹窗、菜单弹窗和
> 目录一致性“完整（26/26 菜单、43/43 标准权限）”。生产库仍须按正式升级流程单独备份、预检和执行。

> 2026-08-29 质量含图导出布局交接：新生成的质量报表由三 Sheet 收敛为“按日汇总 / 问题明细”两
> Sheet。“问题明细”保留 A:T 20 列，整改前照片直接放在 S 列、最新一轮整改后照片直接放在 T 列；
> 多张照片在各自列内按原附件顺序逐行排列，A:R 的同一问题信息纵向合并覆盖照片行，下一问题紧接，
> 不表示一一对应。S/T 图片以紧凑缩略图展示，完整原图仍嵌入工作簿，可在 WPS/Excel 选中图片后
> 使用图片工具放大查看。明细页取消 AutoFilter，继续
> 保留冻结首行、横向打印、适宽一页、隐藏网格线和重复表头。JPEG/PNG 原字节、缺图失败、300 问题、
> 800 照片、120MB、七天有效期及接口权限均不变；历史成功文件不回写。长期本地库已应用质量按日
> 导出迁移并为 94 表，生产库仍须受控升级。后端 787 项测试、Web 104 项测试与构建、小程序三项检查
> 均通过；最新服务已重启。真实任务 `3` 已生成 9 问题、14 原图、约 12.1MB 的两 Sheet 文件，WPS
> 已核对 S/T 缩略图、缺图提示及图片选中后“图片工具”放大入口。

> 2026-08-28 质量本地迁移与小程序修复交接：微信开发者工具质量首页的 HTTP `500` 已定位为最新后端
> 查询 `quality_issue.record_date`，但长期本地库仍停留在迁移前结构。用户明确授权跳过备份后，已直接
> 执行 `20260828_quality_issue_daily_export.sql`，本地库由 92 表增至 94 表；不可空字段、索引、两张
> 导出任务表、迁移标记及 12 条现有问题日期回填均已核对。当前工作区四项本地服务已重启，微信开发者
> 工具实测质量首页、12 条整改列表和问题详情恢复，清空旧控制台后未再出现 HTTP `500`。生产库未迁移，
> 仍须按正式升级流程备份、预检和单独授权。

## Git 远程提交约定

- 本机已配置可用的 Git SSH 密钥，本项目远程仓库为
  `git@github.com:jinze-coding/Dianxinyun.git`。
- 后续提交统一使用本机 `git` 通过 SSH 执行状态核对、暂存、提交和
  `git push`；不得把 GitHub CLI `gh` 的安装、登录或认证作为提交前提。
- 除非用户明确要求创建 Pull Request，否则完成 SSH 推送并核对远程提交即可，
  不额外走 GitHub CLI 或网页 PR 流程。
- 只提交可公开复现的配置模板、初始化脚本和脱敏开发数据；
  `.env.local`、`.env.mp-real`、密钥、密码和真实业务数据不得强制加入版本库。

> 2026-08-28 临边巡检业务口径与两端优化交接（最新，替代下方旧的 32/232 任务展示说明）：临边
> “8”表示项目当前 8 个启用点位，不是累计待办数。系统按“启用点位 × 计划日期”生成任务，因此每日
> 周期下每天 8 项、最近 7 天共 56 项；未来任务可以在数据库提前生成，但 `availableTime` 到达前不进入
> 页面和个人待办，历史逾期仍保留补检入口。
>
> 新增 `GET /api/v1/edge-inspections/projects/{projectId}/workspace-summary`，按上海业务时间返回
> `enabledPointCount` 及本人的今日总数/待巡检/已提交/取消、逾期、待整改、待复查。今日总数包含已
> 提交和取消；整改、复查按整改单 `taskId` 去重且只计算明确指派本人，`UNASSIGNED` 不算本人。接口
> 要求有效项目范围、`INSPECTION` 模块及任一临边权限，平台管理员沿用现有全局放行规则。
>
> 小程序首页主数字已改为“启用点位”，另列四项“我的”数量；“我的临边巡检”改为“今日巡检 /
> 逾期补检 / 巡检记录”三页，整改仍从独立入口处理。任务卡按点位、类型、位置、日期时段、状态和操作
> 纵向分行，并增加流程条和底部说明；首页、任务、表单、整改及个人待办统一隐藏演示 marker。Web
> 保持“待处理 / 巡检记录 / 基础设置”三页，在顶部增加五步流程条和说明抽屉，并明确项目范围与本人
> 范围数字。
>
> 项目 3 已用最新幂等脚本连续写入两次并核对：8 个点位、最近 7 天 56 项任务，状态为 `CANCELLED 1 /
> CLOSED 1 / COMPLETED 42 / PENDING 10 / RECTIFICATION_PENDING 2`；其中今天 8 项待巡检、历史
> 逾期未检 2，另有逾期补检 2、取消 1、待整改 1、待复查 1、已闭环 1、普通按时完成 40。整改单为
> `CLOSED / COMPLETED / PENDING` 各 1；非演示项目 3、项目 4 和电箱计数前后未变。调度器后续可以
> 独立预生成未来任务，但两端不会提前展示。
>
> 后端全量 774 项测试通过；小程序展示规则测试、TypeScript、H5 和微信构建通过；演示脚本语法、
> 双次幂等、状态分布、照片完整性和清理边界已核对。生产不得携带本地演示数据。

> 2026-08-28 临边巡检 Web 三页式简化交接：巡检管理顶部只保留紧凑“电箱巡检 / 临边巡检”切换，
> 临边内部改为“待处理 / 巡检记录 / 基础设置”。默认待处理仅按需加载逾期任务和未闭环整改单；
> 整改并入待处理，统计并入记录右侧抽屉，点位与单一周期设置合并到基础设置。记录筛选继续常驻，
> 批量选择按需开启，异步含图导出保持原接口和弹窗。小程序、后端、数据库和权限码未改动。
>
> 新增 `/api/v1/edge-inspections/export-jobs` 创建、列表、进度和受权下载接口。任务创建时冻结任务 ID，
> 后台行锁领取；限制 31 天、500 个任务、800 张照片、120MB 和每人两个活动任务。工作簿包含巡检汇总、
> 八个逐点检查记录、照片明细和整改明细，损坏图片只在单元格标记，成功文件七天有效。
>
> 当前长期本地库已备份数据库和上传目录，并幂等执行 `20260828_edge_inspection_export.sql` 两次，当时为
> 92 表；随后已应用质量按日导出迁移，当前为 94 表。项目 3 已用 `scripts/edge-inspection-demo-data.sh` 写入 8 类点位、一套每日设置、截至当天 224
> 个任务、31 个异常明细和 244 条图片元数据；调度另生成次日 8 个待办，当前共 232 个任务。脚本的
> `cleanup` 仅删除固定演示标记数据，清理后二次写入及电箱、项目 4 对账均通过。
>
> 独立质量导出迁移在本段临边专项验收时尚未应用；当前状态以上方“质量本地迁移与小程序修复交接”
> 为准。
>
> 后端 767 项、Web 90 项及生产构建、小程序类型检查和微信构建均通过；浏览器在 1366/1440/1920
> 三档核对无整页横向滚动。真实异步任务导出 224 个任务并成功生成 1,540,867 字节 XLSX，POI 回读
> 为 11 张工作表和 434 个嵌入图片对象。生产库仍须按升级手册独立备份、预检和授权，不得把本地演示
> 数据带入正式环境。

> 2026-08-28 质量问题按日筛选与导出交接：质量问题新增稳定 `record_date`，周检问题取周检检查日期，
> 历史独立问题取原创建日期。Web“整改闭环”增加问题日期、可清空的开始/结束日期和“导出汇总”；
> 默认列表仍查询全部时间。日期必须成对提交，闭区间最长 366 天，结束日期不得晚于上海当天。
>
> 新增 `/api/v1/quality/issues/export-jobs` 创建、列表、进度和下载接口。任务创建时事务内冻结问题 ID，
> 当时后台生成三 Sheet XLSX：按日汇总、问题明细、照片对比；嵌入全部整改前原图和最新一轮全部整改后
> 原图。2026-08-29 起新导出已改为上方两 Sheet、S/T 列内逐行紧凑缩略图且 A:R 问题信息纵向合并的
> 布局；完整原图仍嵌入，WPS/Excel 选中图片后可用图片工具放大查看，历史文件不重写。JPEG/PNG
> 不重编码，缺图或
> 解码失败会让任务失败。任务复用 `quality.view`，普通用户仅访问
> 本人任务，平台管理员全局访问；限制为每人两个活动任务、300 问题、800 照片、120MB，文件七天
> 后先转数据库删除状态，再进入统一物理清理重试。
>
> 幂等迁移 `20260828_quality_issue_daily_export.sql` 增加 `record_date`、项目/日期/状态索引和两张导出
> 任务表。完整 91 表隔离副本双跑、历史/周检日期回填和 93 表结构均已核对，临时库已删除；该质量
> 专项收口时长期本地 `dianxinyun` 为 91 表且没有本次字段/表，随后仅应用临边导出迁移增至 92 表。
> 质量迁移随后已按用户明确授权跳过备份并应用到长期本地库；生产库仍未执行，后续必须先备份、预检
> 并单独授权升级。
> 自动验证为后端 761 项、Web 88 项/生产构建/0 漏洞和 `git diff --check` 全绿。隔离环境真实生成
> 12 问题/18 原图/21,613,318 字节报表，18 个工作簿媒体摘要与源照片逐一匹配，并完成 17 页打印
> 检查；浏览器验证日期筛选 12→9、9 问题/14 照片任务完成及 XLSX 下载 HTTP 200。小程序没有改动。

> 2026-08-27 固定式临边本地运行交接：用户明确要求跳过备份后，当前长期本地 91 表库已直接执行
> `20260826_general_inspection_fixed_edge.sql`，固定式标记为 1。已核对 8 个字段、8 类、8 套表单及
> 版本、40 项、五项权限和 `INSPECTION_EDGE`；尚未创建临边点位、计划、任务和整改。最新产品口径
> 已取消项目试点开关，历史 `general_inspection_project_setting` 不再参与鉴权、任务生成、附件或两端
> 展示；`GET/PUT /edge-inspections/projects/{projectId}/feature` 与 Web 试点按钮均已删除。生产库仍未
> 据此执行迁移，必须继续按生产升级手册单独备份、预检和授权。
> 最新本地自动验证为后端全量 753 项、编译和 Jar 打包通过；Web 临边定向 5 项、lint、类型检查和
> 生产构建通过；小程序类型检查、H5 和微信构建通过。Web 全量检查 86 项中仅既有质量周检日期用例
> 失败，与本次临边改动无关。最新后端、Web、扫码 H5 和小程序编译监听均已重启；未登录临边接口
> 返回 401，旧 `/feature` 返回 404，Web 返回 200。隔离浏览器无登录态，尚未完成登录后角色实操验收。

> 2026-08-26 共享会议邀请交接：现有预约邀请保留为 `SINGLE`，继续由首位访客提交后锁定整条邀请；
> 同一列表新增 `MEETING` 共享会议码。会议在截止前保持 `OPEN`，不同微信身份分别登记本人、同行人
> 和车辆；同一身份复扫只返回自己的登记组凭证，其他身份仍显示表单。管理员在开放期可纠错或作废
> 单组，作废后原身份可重新登记；截止或整场作废后所有人只见失效页，内部记录仅查看和导出。
>
> 后端新增强制 `uni.login` 会议会话、用途隔离身份摘要、有效登记唯一约束、分页管理、逐人 Excel、
> 删除影响和事务级联；Web 增加类型筛选、开放登记状态、组数/人数、共享码和登记组管理；小程序新增
> `pages/public/meeting-invite`，在身份状态确定前不显示表单。微信仅用于身份识别，不申请头像、昵称
> 或微信手机号，数据库不保存明文 OpenID。
>
> 幂等迁移 `20260826_site_access_meeting_invitation.sql` 增加 `invite_type` 并默认/回填 `SINGLE`，
> 新增会议登记组、人员和审计 3 张表；已在新建隔离库连续执行两次，核对旧状态不变、唯一约束、
> 作废后重登和单一迁移标记，临时库已删除。长期本地库后续已按本页 2026-08-31 最新记录直接执行，
> 生产库仍未执行。当时自动验证为后端 755 项、Web 85 项及生产构建、小程序类型检查/H5/微信构建和
> 扫码场景测试通过；尚未做正式环境真实微信双账号验收。
> 发布必须按“正式库备份与预检 → 会议迁移 → 后端 → 小程序审核发布 → Web”执行；小程序新版生效前
> 不得创建正式会议码。

> 2026-08-26 固定式临边巡检交接：开放式通用巡检产品方案已经停止。巡检管理分为“电箱巡检”和
> “临边巡检”两个独立专区，Web、小程序首页、个人待办和记录均不混排；电箱六项日检、统一二维码、
> 专用月表及原整改流程不变。临边专区只允许维护点位和一套项目巡检设置，不再提供模板、自定义类别、
> 多计划、多时段、逐点人员覆盖、临边二维码或匿名月表。当时尚无异步导出；该边界已由上方
> 2026-08-28 临边含图导出交接替代。
>
> 临边点位固定为 8 类，每类绑定一套系统只读的 5 项检查表。检查项只能选正常或异常；每任务至少
> 1 张全景照片，异常项必须说明并至少上传 1 张证据图。全部异常按任务聚合为一张整改单，整改人逐项
> 反馈并上传照片后整单进入待复查；复查只能全部通过关闭或填写原因退回。项目只设置一个周期、一个
> 时段以及巡检人、整改人、复查人和整改期限；每月可指定 1—31 日或月末。新建点位只纳入创建后的
> 任务，停用后不可恢复并自动取消未提交任务。
>
> 业务接口为 `/api/v1/edge-inspections/**`，菜单为 `INSPECTION_EDGE / 临边巡检`，权限固定为
> `EDGE_INSPECTION_VIEW / EDGE_INSPECTION_MANAGE / EDGE_INSPECTION_SUBMIT /
> EDGE_INSPECTION_RECTIFY / EDGE_INSPECTION_REVIEW`。后端还校验有效项目范围、`INSPECTION`
> 模块、五项临边权限及任务当前指派；平台管理员未被明确指派时不能直接复查。
>
> 长期本地库已经应用原 `20260826_general_inspection.sql`，真实总表数为 91。原脚本是已执行的历史
> 基线，必须保持不变；新增 `20260826_general_inspection_fixed_edge.sql` 作为 additive 转换迁移，
> 保留 14 张 `general_inspection_*` 表和历史数据，只归档开放配置、增加固定类型快照字段/索引并写入
> 8 类固定表单、五项权限和新菜单。完整 91 表隔离副本已完成双跑：首次生成 8 类、8 套表单、40 项、
> 5 项权限、新菜单和标记；模拟管理员停用菜单/权限、撤销角色关联并修改历史开关记录后复跑，选择均被保留，
> 临时库已清理。项目级试点开关已经取消，历史表仅作兼容保留。
> 仅应用本次临边补丁的干净工作区自动门禁为后端全量 728 项（临边定向 118 项）、Web 82 项/
> 生产构建/0 漏洞、小程序类型检查/H5/微信构建全部通过。
> 当前长期本地库已应用固定式转换迁移；生产仍必须先备份数据库及上传目录，取得明确授权后应用迁移，
> 再按管理者、巡检人、整改人、复查人完成一个真机闭环。没有临边匿名访客验收角色。

> 2026-08-26 图纸收发交接：资料管理已新增“图纸收发”，包括收文、发放和综合台账。历史资料
> 保持 `GENERAL`；正式图纸/技术文件发布在单事务中新增版本、把旧版标为 `SUPERSEDED`、切换
> 当前指针并生成发放快照、接收人、站内通知和追溯事件。纸质与 `BOTH` 必须在小程序扫描批次
> 随机 scene；名单、有效项目成员、资料模块和 `document.view` 在读取明细前校验，重复确认幂等。
>
> 新迁移 `20260826_document_circulation.sql` 新增 7 表、资料/版本字段、“图纸收发”页签和 4 项
> 权限，只自动关联平台管理员；迁移和空库基线已准备但未执行长期本地 68 表库或生产库。分片上传
> 为 8MB/片、200MB/文件、24 小时会话，IFC/RVT/DGN 只下载。最终自动验证为后端 710 项
> 全量测试通过；Web lint、类型检查、80 项测试及生产构建通过；小程序类型检查、H5 和
> 微信构建通过。未做现有库写入、真实浏览器、微信开发者工具、200MB 真文件或真机验收。

> 2026-08-26 质量周检交接：正式质量入口已改为“质量周检”。每项目每个自然周最多一份正式周检，
> 支持当前周及往期补录、拒绝未来周；Web 和小程序共用服务端共享草稿，以 `expectedVersion` 防止
> 相互覆盖。草稿可整批保存 0–50 个问题，每个问题独立配置整改人、期限和 1–20 张整改前照片；
> 零问题提交必须填写检查结论并上传 1–20 张现场照片。提交锁定周检并在单事务中完整校验、生成
> `PENDING` 问题、日志及正式附件，任一项失败整批回滚，重复提交幂等。
>
> 新问题只能由 `/api/v1/quality/weekly-inspections` 周检提交产生，旧
> `POST /api/v1/quality/issues` 返回 HTTP `410`；历史独立问题不迁移、不删除，仍可通过“整改闭环”
> 的“历史独立问题”筛选继续处理。提交后周检不可编辑，各问题继续独立改派、整改、退回、复查、
> 关闭或作废；详情固定展示原始问题照片和最新一轮整改照片，完整轮次保留在时间线。
>
> 迁移 `20260826_quality_weekly_inspection.sql` 新增两张周检表及 `quality_issue` 关联字段，已在临时
> 结构副本连续执行两次并核验同周唯一索引、迁移标记、安全菜单改名及停用菜单保护；临时库已删除，
> 长期本地库和生产库均未应用。自动验证为后端 677 项、Web 77 项/生产构建/0 漏洞、小程序类型
> 检查/H5/微信构建、种子脚本语法和 `git diff --check` 全部通过。隔离脚本还在 68 表临时副本上
> 以真实 HTTP/MySQL 验证同周创建唯一、并发提交幂等、整批事务回滚及恢复、放弃重建、零问题凭证、
> 未来周拒绝、50/51 项边界和旧接口 `410`，退出后临时资源已复核清理。下一步必须先备份、预检并在目标
> 环境执行迁移，再进行真实浏览器与微信开发者工具/真机的跨端草稿、批量拍照和照片对比验收。

> 2026-08-26 巡检兼容提交交接：`POST /api/v1/inspection/records/{id}/submit` 仅供旧客户端兼容。
> 当前接口事务内锁定目标记录，只允许历史 `DRAFT` 完成；`COMPLETED / RECTIFICATION_PENDING /
> CLOSED` 重复调用幂等返回，其他历史状态返回 HTTP `409`，不能覆盖异常整改或历史复核状态。
> 已补专项回归测试，并继续清理接口、权限、模块清单、需求/数据库说明和现场规格中的双码、
> 逐条复核、安全抽查及“整改接口退出”等残留旧口径；Web 详情显示真实状态，小程序项目工作台已
> 移除历史安全复核入口。最终验证为后端 643 项、Web 74 项及生产
> 构建/0 漏洞、小程序类型检查/H5/微信构建和 `git diff --check` 全部通过；本次无数据库或迁移变更，
> 未进行微信真机预览或上传。

> 2026-08-24 巡检文档口径交接：当前代码真实流程不是“所有新巡检直接完成、整改接口退出”。
> 外观照片和内部照片均为必填且每类至少 1 张，异常项说明必填，本轮不支持单项异常照片；正常
> 巡检为 `COMPLETED`，异常巡检为 `RECTIFICATION_PENDING`，多个异常项聚合为一条整改任务。
> `/api/v1/inspection/rectifications*` 仍是当前正式接口：指派项目电工提交说明和整改照片，项目
> 安全员关闭、退回或改派；退出的仅是历史逐条安全复核与现场安全抽查入口。相关模块清单、规格、
> 实现说明及联动测试计划已按当前代码修正；本轮未改业务代码、数据库或迁移。基线验证为后端
> 641 项、Web 74 项及生产构建、小程序类型检查、`git diff --check` 全部通过；未进行微信真机验收
> 或版本上传。

> 2026-08-15 小程序预览快捷登录交接：本次失败由电脑 DHCP 地址从 `192.168.0.108` 变化为
> `192.168.0.105`，而旧预览包仍请求旧地址引起。当前后端、Web、小程序编译监听和扫码 H5 已重启，
> 微信产物请求 `http://192.168.0.105:8080/api/v1`，局域网后端访问返回 HTTP 200。AppID、真实微信
> 模式、有效绑定及 AppID/AppSecret 配对均已核验正常。`scripts/dev-services.sh start` 现会比较当前
> LAN IP 与编译产物 API 地址，换址时自动重启 watcher 并重编译；`status` 会明确提示过期地址。
> 已重新生成开发者工具预览码但未上传微信版本。真机必须扫描最新预览码、与电脑处于同一 Wi-Fi，
> 并通过右上角“…”开启“开发调试”；若微信提示一次性 code 无效或已使用，应退出预览小程序后重新
> 扫码再登录。

> 2026-08-14 手工坐标与到访路线图交接：项目没有正式门牌地址时，Web 定位编辑器可填写“附近地标
> 及到访说明”，并直接录入经度、纬度与 `BD09 / GCJ02 / WGS84` 来源坐标系；地址解析只是可选辅助，
> 百度 AK 缺失时也可在严格校验后明确确认手工点位。新增项目专属到访路线图，使用独立
> `PROJECT_ROUTE_IMAGE_PENDING / PROJECT_ROUTE_IMAGE` 类型，支持 JPEG/PNG/WebP、15MB、预览、
> 替换、移除和暂存清理；定位版本、路线图绑定/归档及审计同事务，无数据库迁移。
>
> 预约邀请公开响应只暴露 `routeImageAvailable`，路线图通过请求体携带 `inviteToken` 的
> `POST /api/v1/public/site-access/project-location/route-image` 下载，不暴露文件 ID 或令牌，且仅
> `PENDING / SUBMITTED` 未到计划离场时间可访问。小程序待登记和绿色放行两态显示附近地标、原生
> 地图、百度优先导航及可放大的到访路线图；图片失败不阻断地图导航，临时文件在替换、迟到响应和
> 页面退出时清理；页面停留跨过计划离场时间时也会立即隐藏导航、清理路线图并关闭项目信息。
> BMapGL 地图点击必须读取 `event.latlng`，标记拖动优先读取标记当前位置，不得直接持久化可能为
> 米制墨卡托的 `event.point`；写入前还须通过经纬度范围校验。该回归已覆盖千万级墨卡托值拦截。
> 项目位置已取消独立编辑入口，顶部“编辑项目信息”统一开启档案、位置、坐标和路线图编辑；位置仍
> 单独保存以保留审计和版本边界，主档案保存会阻止未保存的位置变更并同步最新 `profileVersion`。
> 小程序提交后路线图曾因原始响应对象与 Vue 响应式代理恒不相等而被误删，现改为只按请求序号判断
> 迟到响应。运行态已实测邀请返回 `routeImageAvailable=true`，路线图接口返回 HTTP 200、有效 PNG
> 423037B，数据库、文件存储与公开接口正常。门卫页未改，旧预约码无需重生。最终验证为后端 640 项
> 及 JAR、Web 74 项/生产构建/0 漏洞、小程序扫码契约/类型检查/微信构建全绿。本地服务已启动且
> 浏览器端 AK 已配置；本轮未上传微信，真机须从当前开发者工具重新预览本地包后再扫预约邀请码。

> 2026-08-14 预约邀请项目导航交接：Web 独立“项目信息”页已增加项目地址与导航点编辑器，复用
> `project_info` 现有地址、经纬度和坐标系字段，无数据库迁移。BMapGL 地址解析只产生候选点，必须
> 由管理员点击地图、拖动标记或明确确认后保存；`VITE_BAIDU_MAP_AK` 仅为浏览器端配置并需 Referer
> 白名单，不进入小程序、API 或源码。定位保存提交 `expectedVersion`，锁项目行并递增
> `profile_version`，并发冲突返回 HTTP `409`，普通 profile 和通用项目更新均不得改变定位字段。
>
> 预约邀请公开响应现增加不含项目 ID、AK 或内部版本的 `projectLocation`，仅在待登记或已登记且未到
> 计划离场时间时返回；后端统一输出 `GCJ02`。小程序待登记和绿色放行两态均展示项目地址与原生地图，
> 百度地图优先导航失败后回退系统地图，且不读取访客位置。门卫长期登记码未改，现有预约码无需重生。
> 本地当前两个项目尚无完整导航坐标，须逐项目人工确认真实入口。后端 625 项测试与 JAR、Web
> 65 项/生产构建/0 漏洞、小程序扫码契约/类型检查/微信构建均通过；浏览器未配置有效
> `VITE_BAIDU_MAP_AK`，真实选点与真机地图 App 调起待人工验收，本轮未上传微信。

> 2026-08-14 预约邀请放行页交接：单次预约邀请提交后不再显示普通“信息已提交”卡片，而是无需
> 审批立即显示绿色“已登记 · 门卫放行”凭证；有效截止沿用邀请计划离场时间，不是滚动 24 小时。
> 同一专属邀请码在有效期内重扫继续显示原凭证，Web 已登记记录也可重复查看/下载同一码；到期、
> 作废后不可继续访问。公开凭证不包含手机号、同行明细或内部 ID。本次无数据库迁移、未上传微信；
> 后端 609 项、Web 60 项及构建、小程序类型检查/扫码契约/微信构建均通过。

> 2026-08-14 门卫固定登记码与外访字段迁移交接：Web 场内管理已增加“预约邀请 / 门卫登记”，
> 每项目门卫码默认不自动过期；访客无需系统账号，使用微信静默身份登记，免审批，单次放行自提交起
> 24 小时有效，登记页和放行页均可查看脱敏项目信息。门卫四表迁移已在完整备份后应用本地开发库。
> 真机首次提交曾因本地库漏执行同行单位/手机号及去身份证两个字段迁移而回滚，日志报缺少
> `site_visitor_profile_person.person_company`；2026-08-14 15:39 已再次完整备份，并依次应用
> `20260814_site_access_remove_id_card_requirement.sql` 与
> `20260814_site_access_companion_optional_contact.sql`，两份脚本复跑幂等、迁移标记各一条、现有记录
> 数量不变。本地 8080/3002/3003 与微信编译监听已重启；生产库仍未迁移，也未上传微信小程序。

> 2026-08-10 场内管理常用外访资料交接：外访人员仍无需注册系统账号。有效邀请公开页通过
> `uni.login` 换取 30 分钟短会话，只允许读取同项目、同 AppID、同微信身份的常用资料；失败退回
> 原手工填报。资料保存/更新必须单独勾选同意，本次邀请始终保留独立快照，资料更新或停用不回写历史。
> Web 已增加常用资料查询、完整详情、停用和邀请来源展示；小程序已增加历史选择、预填、保存、
> 更新和停用。迁移 `20260810_site_access_reusable_visitor_profile.sql` 在本地 SQL 快照后应用，新增
> 3 表和 `source_profile_id`，当时本地库 64 表；生产库尚未应用，正式微信仍需上传新构建并用新邀请
> 完成真机“保存 → 再次邀请复用 → Web 核对来源”验收。
>
> 2026-08-10 常用资料加固：公开场内接口已把受信 IP 限流和 64KB 请求体上限前移到 MVC 参数
> 绑定之前，缺会话、畸形 JSON、错误 Method/Content-Type 分别返回受控 `401/400/405/415`；微信
> 上游异常不再传播包含 AppSecret、code、access token 或 URI 的 cause。Web 列表/详情/停用均绑定
> 当前项目和 latest request，切换项目后丢弃旧响应；小程序邀请解析后立即显示手工表单，资料选择
> 只接受最后一次响应，会话失效时保留字段并经确认降级为本次手工提交。身份证摘要改为带用途隔离的
> HMAC-SHA256，新增迁移 `20260810_site_access_visitor_security_hardening.sql` 只退役历史未加钥
> 摘要，不改变表数；该迁移尚未应用长期本地库或生产库，部署时必须先迁移再切换新应用。
> 当前验证为后端 583 项、Web 60 项及构建/审计、小程序扫码与资料专项/类型检查/H5/微信正式构建
> 全绿；两份 SQL 已在隔离库双跑并删除临时库。本地 8080/3002/3003 与微信编译监听已重启，公开
> 异常请求状态实测符合接口文档。长期本地两张人员表当前均为 0 行，因此未执行仅写迁移标记的
> 安全加固脚本；正式环境仍须按迁移顺序执行并做真机闭环。

> 2026-08-09 项目信息档案交接：Web 顶部项目选择器右侧已新增独立“项目信息”，有效项目成员
> 可查看，`PLATFORM_ADMIN` 可原位编辑并管理效果图；小程序从“我的 → 施工区域 → 项目卡片 →
> 项目信息”进入同一接口的只读页，不显示编辑、上传或删除。该入口是项目基础能力，不恢复历史
> 带视频/设备的“项目概况”，也不新增菜单或普通角色权限码。
>
> 后端新增 `GET/PUT /api/v1/projects/{projectId}/profile`，使用项目行锁、`profile_version`、完整
> 事务和字段名审计；`phase` 与系统 `project_status` 分离，`building_area` 同步兼容 `area`。效果图
> 使用 `PROJECT_PROFILE_IMAGE_PENDING` 暂存和 `PROJECT_PROFILE_IMAGE + projectId` 最终绑定，
> 支持 JPEG/PNG/WebP、15MB、最多 20 张、移除归档、取消清理和 24 小时遗留暂存清理。
>
> 幂等迁移 `20260809_project_profile.sql` 已在隔离库连续执行两次，并在数据库 SQL 备份、上传目录
> 归档和 SHA-256 校验后应用本地开发库；表数仍为 61，空库基线已同步，生产库尚未应用。指定工作簿
> 已通过一次性平台管理员会话按项目全称唯一匹配导入，30 个非空源字段、31 个存储字段和两张效果图
> 的顺序、首图、预览与摘要全部核对通过；临时脚本、会话、实际字段值和图片副本未进入仓库。
> 自动回归为后端 564 项测试、Web 53 项测试/生产构建/0 漏洞审计、小程序类型检查/H5/微信构建
> 全部通过；仍需在实际浏览器和微信开发者工具登录目标构建完成人工页面交互检查。

> 2026-08-08 登录默认落点：Web 账号密码、微信扫码登录、已有会话恢复及业务失权回落统一进入“个人待办与消息”；小程序账号密码、微信快捷登录和有效会话冷启动恢复统一进入第一栏“待办”，底部顺序为“待办 / 资料 / 巡检 / 质量 / 我的”。小程序首次设置密码和显式扫码业务续跳继续优先。个人待办不扩大业务权限，本次无后端接口或数据库变更；Web 全量 check/42 项测试/生产构建/0 漏洞审计及小程序类型检查/H5/微信构建均通过。

> 2026-08-08 用印一期交接：当前代码已加入项目印章与独立不透明小程序码、扫码申请、源文件和
> 盖章件、用户直指派 `ANY_ONE` 审批、抄送、站内通知、带原因转办、盖章件复制归档、申请单
> PDF 和两 Sheet 用印台账。Web 已接入资料管理用印分栏、用印审批和个人消息，小程序已接入扫码
> 申请、审批详情及第一列“待办”主栏；个人工作中心通过稳定 `todoKey` 和受控
> `routeCode + routeParams` 聚合巡检、质量与用印。
>
> 权限边界必须保持：审批只认申请快照生成的 `workflow_approval_task.assignee_user_id`；
> `seal.manage` 负责项目级用印管理但不能转办，带原因转办只认 `system.approval.manage`；归档盖章件
> 必须另外具有 `document.upload`，审批管理员未被指派时不能代批。抄送人只读，用户停用或移出项目
> 后不能继续读取或处理记录。
>
> 新增迁移 `20260808_seal_application_workflow.sql` 已在隔离空库按完整迁移链执行，并重复执行用印
> 迁移两次通过；临时库已清理。2026-08-08 又按用户明确授权直接应用到本地开发库，本地表数由
> 50 增至 61，11 张业务表、迁移标记、2 个菜单、5 项权限和平台管理员关联核验通过；修正 MySQL
> 8.4 弃用提示后幂等重跑无重复授权。本次按用户要求未备份本地库、未执行 `init.sql`。生产库尚未
> 应用，生产必须单独注入至少 32 字节的
> `SEAL_SCENE_ENCRYPTION_KEY`。最终回归为后端 539 项测试及生产 JAR、Web 全量 check/42 项测试/
> 生产构建/0 漏洞审计、小程序类型检查/H5/微信构建全部通过。PDF A4 两页的中文精确抽取和分页
> 渲染已检查；Excel 双 Sheet、中文数据、冻结/筛选、A3 打印区、重复标题和公式注入防护已通过
> 结构测试及 LibreOffice 渲染。自动化环境无法控制 WPS，仍需实际 WPS 打开检查列宽、筛选和打印
> 预览；正式微信二维码还需真实配置与真机闭环。首期不发送微信订阅消息。
>
> 最终安全边界：草稿抄送人不能读取草稿或附件，重复创建/提交幂等，源附件变更与提交共用申请
> 行锁；正式 PDF 仅对 `APPROVED` 开放。配置保存按用户、成员、配置顺序锁定一致快照。账号停用、
> 成员移除或暂停会事务内清理审批配置和待办，无候选时通知管理员转办；旧
> `PUT /system/users/{id}/access` 已 fail-closed 返回 `410`。存在非草稿用印记录的项目不得永久删除。

> 2026-08-08 场内外访删除：Web 场内管理仅向平台管理员显示“删除”，任意状态邀请都复用统一影响预览、勾选确认、5 分钟令牌和目标行锁。事务按实名人员、场内业务审计、邀请主记录顺序物理删除，平台 `sys_operation_log` 保留并新增删除摘要；项目仍不能绕过逐条确认直接删除外访数据。本次无数据库迁移，后端 479 项测试与 Web 全量 check/37 项测试/生产构建通过，未对长期本地库做破坏性验收。

> 2026-08-08 密码重置交互：Web 用户管理不再通过浏览器 `prompt/alert` 收集或反馈密码，改为系统内弹窗；包含目标账号、新密码、确认密码、显示/隐藏、8–72 位且字母数字组合的实时校验、提交状态和成功/错误反馈。后端接口与会话注销逻辑不变，无数据库迁移；Web 全量 check/35 项测试/生产构建通过。

> 2026-08-08 注册审核删除：平台管理员可在 Web 注册审核列表删除任意状态的注册申请，统一使用影响预览、勾选确认、5 分钟一次性令牌和目标行锁。只删除 `registration_application` 当前行；已创建用户、项目角色、微信绑定和审核操作日志保留，待审核唯一占用随记录释放。本次无数据库迁移；后端 477 项测试、Web 全量 check/34 项测试/生产构建通过。

> 2026-08-08 角色名称唯一校验：有效角色名称全局唯一，新增与改名均忽略首尾空格和英文大小写检查；重名返回 HTTP `409`。角色定义写入通过平台管理员角色行串行化，避免并发创建绕过检查。Web 弹窗即时提示并禁用保存，后端是最终校验来源。本次无数据库迁移；后端 473 项测试、Web 全量 check/34 项测试/生产构建通过。

> 2026-08-07 场内管理 V1：Web 顶部已增加“场内管理”，可创建单次外访邀请、生成专属小程序码、查看/纠错/作废并按人员导出；小程序公开页 `pages/public/visitor-invite` 免登录一次性填报，提交后锁定。新增 `SITE_ACCESS / WEB_SITE_ACCESS / SITE_VISITOR`、`site_access.view/manage/export` 和 3 张业务表迁移。手机号、身份证、令牌及审计快照使用独立 AES-GCM 密钥；公开接口先限流，提交行锁防重。
>
> `20260807_site_access_visitor_invitation.sql` 已应用到本地开发库，表数当时由 47 增至 50；生产库尚未应用。非开发环境必须配置独立且至少 32 字节的 `VISITOR_DATA_ENCRYPTION_KEY`。当前后端 472 项测试与生产打包、Web 全量 check/33 项测试/生产构建/0 漏洞审计、小程序类型检查/电箱和外访场景解析测试/H5/微信构建通过；仍需正式微信真机扫码闭环和真实 Excel/WPS 打开检查。

更新时间：2026-08-09。

> 2026-08-07 管理员全权限与强制删除：`PLATFORM_ADMIN` 自动获得全部当前启用正式菜单、
> 操作权限、四个模块和全部项目，不写项目成员关系；Web 提供统一影响确认弹窗，小程序仅同步
> 全权限且不增加硬删除控件。用户、项目、角色、资料、电箱、巡检和质量硬删除必须使用 5 分钟
> 一次性令牌并在执行时重算影响。撤权后清当前责任人、保留任务和历史、注销目标用户全部会话。
> Web 用户管理仅向平台管理员显示删除入口；当前登录账号和最后一个可恢复管理员受保护，删除
> 其他用户时同步清授权、微信绑定与订阅状态并保留历史业务及操作日志。
> 本次无数据库迁移，破坏性验收只能使用隔离数据库副本和临时文件目录。当前回归已通过
> 后端 448 项测试、Web 27 项测试及生产构建/审计、小程序类型检查与 H5/微信构建；
> 微信开发者工具真实交互和隔离库破坏性验收尚未执行。

> 2026-08-07 注册项目选择：新申请必须通过公共搜索选择至少一个项目，可多选；Web 和小程序
> 都提交 `desiredProjectIds[]`，不再显示自由文本或项目 ID 输入。公共搜索至少两个字符、最多
> 20 条、只返回最小字段并排除停工项目。审核不再附加默认 `USER`，管理员必须明确勾选最终项目
> 和至少一个实际角色；服务端在提交与审批两处重新校验。历史内置 `USER` 也已放开管理员删除，不再自动回落或补角色。

> 2026-08-03 巡检报表导出规则：旧 `month/boxId` 月报继续兼容；Web 日期范围报表使用
> `startDate/endDate/boxIds`，结束日期不晚于当天且首尾最多 366 天。一个电箱允许
> `INSPECTION_DAILY_SUBMIT` 或 `SUMMARY_EXPORT`，多个或全部电箱必须 `SUMMARY_EXPORT`。
> 范围报表只应用日期和电箱条件，应检统计按历史巡检范围计算并批量加载检查项与范围历史。

> 2026-08-01 巡检范围写入规则：变更前锁定目标电箱；旧开放区间关闭和新范围历史新增
> 都必须影响 1 行，失败返回 HTTP `409` 并回滚。原因最长 300 字符，操作人来自登录态。

> 2026-08-01 项目写入规则：新增/编辑不能覆盖主键、删除标记和创建时间；字段长度、状态、
> 日期和坐标必须在写库前校验。项目新增、更新、定位和定位审计写入必须影响 1 行，定位
> 主记录与审计日志同事务，失败返回 HTTP `409` 并回滚。

> 2026-08-01 安全教育写入规则：创建前完整校验批次、去重人员和本人刚上传的未绑定培训
> 资料；所有人员必须同项目。批次、人员关联、人员教育状态和附件绑定写入数量不符时返回
> HTTP `409` 并回滚，批次不存在返回 HTTP `404`。

> 2026-08-01 人员写入规则：人员请求体不能覆盖主键、项目、状态、删除标记和创建时间；
> 状态只走进退场/安全教育流程。人员、流水、证件和附件绑定写入必须影响 1 行。证件附件
> 必须同项目、类型为 `PERSON_CERTIFICATE`，未绑定附件只能由上传人本人绑定。

> 2026-08-01 摄像头/设备写入规则：所属项目和持久化字段只以后端原记录为准；名称必填，
> 字段长度在写库前校验，摄像头在线状态只允许 `0 / 1`。新增、更新、删除影响行数不是
> 1 时返回 HTTP `409`；设备自定义扩展类型继续兼容。

> 2026-08-01 巡检写入规则：新建日检由服务端固定模板和来源，必须恰好包含六个不重复
> 的规范检查项，名称由服务端写入，异常项必须填写说明。服务端锁定目标电箱后检查同箱
> 同日唯一性；巡检记录、检查项和巡检设置写入影响行数不是 1 时返回 HTTP `409` 并回滚。
> 当前模板插座编码为 `SOCKET_220V / SOCKET_380V`，旧编码只能通过正式迁移对齐。

> 2026-08-01 工程资料写入规则：目录、文件元数据、资料、版本、当前版本指针、归档/
> 回收站状态、恢复、永久删除和操作日志的单记录写入必须校验影响行数为 1，失败返回
> HTTP `409` 并回滚。资料编号最长 100 字符，资料备注和版本说明最长 500 字符，必须
> 在物理文件落盘前校验。上传回滚清理新文件，永久删除只在数据库提交后清理旧文件。

> 2026-07-31 电箱写入规则：所属项目创建后不可编辑迁移，`status`、`qrStatus` 只能由
> 专用生命周期接口修改。传入责任人 ID 时必须校验启用账号并使用服务端姓名。停用/拆除
> 走 `BOX_MANAGE`，二维码换绑、打印和日志走 `BOX_QR_MANAGE`，公开访问启停走
> `BOX_PUBLIC_ACCESS`；普通编辑改变二维码或公开状态还需追加细分权限。主记录和二维码
> 日志写入影响行数不是 1 时返回 HTTP `409` 并回滚。

> 2026-07-31 当前账号微信操作限流：微信绑定、解绑和项目访问申请必须同时按受信 IP
> 与规范化账号限流；两套兼容解绑入口共享额度，密码校验前每账号 10 分钟最多 5 次。
> 不得只保留 IP 限流，Redis 键中不得出现明文账号或 IP。

> 2026-07-31 二维码标签 HTML 安全规则：电箱编号、名称、位置和场景码写入打印/下载
> HTML 前必须转义；二维码图像只接受服务端生成的 PNG Base64 或编码 SVG Data URL，
> 禁止恢复原始 SVG/任意 URL 直接拼接。打印文档保留 CSP，打印窗口必须切断 opener。

> 2026-07-31 公开巡检范围查询规则：近 30 天摘要和公开逐日月表必须一次加载目标日期
> 范围的巡检范围历史，再在内存中解析每日应检状态；不得恢复为按自然日逐次查询数据库。
> 历史范围、建箱日期、到期日期和箱日去重口径保持不变。

> 2026-07-31 统一扫码公开边界：调用者没有目标项目巡检权限且电箱公开访问关闭时，
> `/api/v1/scan/electric-boxes/{sceneCode}` 必须在巡检范围和当天记录查询前返回
> HTTP `403`，不得返回电箱编号、名称、位置等元数据。正常匿名公开扫码也不查询当天
> 内部巡检记录。

> 2026-07-31 敏感写入规则：密码、账号状态、微信绑定、项目成员授权、角色、菜单、
> 操作权限和巡检权限模板的单记录新增/更新/删除必须校验数据库影响行数为 1；写入未
> 生效时返回 HTTP `409` 并回滚，禁止继续注销会话、替换权限或记录成功结果。

> 2026-07-31 微信注册临时身份事务规则：标准微信注册对 Redis 临时身份的消费必须与
> 数据库事务结果联动；事务回滚后恢复原身份会话及 10 分钟有效期并释放占用标记，提交
> 成功后清理占用标记。并发请求仍只能成功占用一次，不得因业务校验或数据库回滚强迫
> 用户重新微信授权。

> 2026-07-31 公开巡检限流：免登录统一扫码解析和近 30 天摘要按真实客户端 IP 每
> 10 分钟最多 60 次，公开月表每 10 分钟最多 30 次；必须在可选登录态解析和业务查询
> 前执行，超限返回 HTTP `429`，Redis 限流键只保存 HMAC 摘要。

> 2026-07-31 权限失效顺序：项目成员、角色、菜单权限、巡检模板、账号状态或微信绑定
> 发生变更时，必须在数据库事务提交前立即清理项目缓存并注销会话，提交成功后再重复
> 清理一次，防止并发请求按旧数据回写缓存或补签会话。首次密码设置会签发新令牌，不得
> 使用提交后全量注销。

> 2026-07-31 巡检统计口径：公开近 30 天汇总、公开月表和内部汇总的应检、已检、
> 漏检、异常必须按该日期的历史巡检范围和到期日期计算，并按箱日去重；不得把建箱前、
> 非巡检范围、未来日期或同日重复记录计入统计。

> 2026-07-31 通用附件事务顺序：上传/替换的新物理文件在事务回滚时清理；替换和删除
> 只能在新元数据或逻辑删除提交后清理旧文件。元数据写入行数异常返回 HTTP `409`，
> 禁止忽略数据库失败后继续删除物理文件。

> 2026-07-31 资料永久删除顺序：工程资料先在事务内标记文件元数据并删除版本/资料关系，
> 数据库提交后才删除物理对象；回滚不触碰原文件。提交后物理清理失败时保留逻辑删除
> 元数据供排查和重试，不能改回“先删文件再提交数据库”。

> 2026-07-31 项目删除门禁：`DELETE /projects/{id}` 仅允许平台管理员删除完全空项目；
> 任一成员、角色、资料、巡检、质量、人员、设备、摄像头或微信申请记录仍存在时返回
> HTTP `409`。禁止删除项目主记录后遗留孤儿数据，也不通过该接口级联清库。

> 2026-07-31 登录限流：密码登录和微信绑定登录同时按真实客户端 IP、规范化账号独立
> 限流，账号额度不随 IP 改变而重置。所有 Redis 限流主体用当前环境 JWT 密钥生成
> HMAC-SHA256，键中不保留明文用户名、手机号、IP、状态令牌或扫码 challenge。

> 2026-07-31 反向代理 IP 门禁：生产必须配置 `FORWARD_HEADERS_STRATEGY=NATIVE`，并由
> Nginx 设置 `X-Forwarded-For`、`X-Forwarded-Proto`；否则后端拒绝启动。业务限流和
> 审计只读取 Tomcat 校验后的 `request.getRemoteAddr()`，不直接信任客户端请求头。
> 后端 8080 不得直接暴露公网。

> 2026-07-31 生产部署门禁：正式环境必须从部署平台注入非 root MySQL 账号、非空数据库
> 密码、启用 TLS 的 `DB_URL`、非空 `REDIS_PASSWORD` 和独立 JWT/微信配置；完整占位
> 模板见 `backend/.env.example`，Spring Boot 不会自动加载该文件。Knife4j、Swagger UI
> 与 OpenAPI JSON 生产固定关闭，误开会拒绝启动，路径级过滤同时确保 `/doc.html`、
> `/webjars/**`、`/swagger-ui/**`、`/v3/api-docs/**` 返回 `404`。本地统一开发脚本会
> 显式开启文档。springdoc 已按 Spring Boot 3.5 兼容线固定为 `2.8.17`。

> 2026-07-31 线上版本差异：`zhihuiyz.xyz` 当前仍运行缺少验证码、统一注册、Web 扫码
> 和微信快捷登录接口的旧后端，不能通过修改小程序 API 前缀解决。升级前必须按
> `docs/生产升级与回滚手册.md` 确认生产授权、备份、数据库迁移状态、服务路径和回滚
> 负责人；仓库没有生产 SSH、systemd/Nginx 路径或密钥，不得猜测后直接发布。

> 2026-07-31 真机接口构建门禁：`mp-real` 模式会强制校验
> `VITE_API_BASE_URL`，缺失、非 HTTPS、localhost、`127.0.0.1`、局域网地址、
> 非 `/api/v1` 前缀或启用 mock 都会直接拒绝构建。真机配置模板为
> `wechat-miniprogram/site-platform-miniprogram/.env.mp-real.example`，复制为本机
> `.env.mp-real` 后再执行 `npm run build:mp-weixin:real`。构建通过只代表代码包地址
> 合法；上传前仍必须确认线上 `/api/v1/auth/captcha` 返回业务 `code=200`。统一开发
> 脚本使用 `dev:mp-weixin` 连接本机 8080 后端，不读取生产 `mp-real` 地址。

> 2026-07-30 微信体验版联调：小程序真实环境使用
> `https://zhihuiyz.xyz/api/v1`，执行
> `npm run build:mp-weixin:real` 生成上传用代码包。不得使用默认
> `build:mp-weixin` 产物或局域网 `192.168.*:8080` 地址上传体验版；默认构建
> 未提供绝对地址时会回退到 `127.0.0.1`，真机无法访问。
> `scripts/dev-services.sh start/restart` 默认生成 `mp-local` 开发产物，并自动使用当前电脑
> 局域网 IPv4 的 `http://<LAN-IP>:8080/api/v1` 连接本机开发后端和开发库。该包只用于
> 微信开发者工具模拟器；手机预览必须重新切回 `mp-real` HTTPS 构建。

> 2026-07-30 开发巡检码联调：手机扫描开发 H5 码后，接口必须经 `http://<本机IP>:3003/api/v1` 同源代理访问后端。H5 请求封装默认使用 `/api/v1`，禁止回退到手机自身的 `127.0.0.1:8080`；测试时应关闭微信内旧页面后重新扫码。
>
> 2026-07-30 局域网联调：Web `:3002` 与小程序 H5 `:3003` 的 Vite 代理会把局域网浏览器 Origin 规范化为本机 8080 代理目标，修复通过 `http://<本机IP>:3002` 登录时的 `403 Invalid CORS request`。后端 CORS 白名单未放宽；其他设备只访问 3002/3003，不直接访问 8080。

> 2026-08-10 小程序依赖审计：Vite 已固定升级至安全维护版本 `6.4.3`，nanoid 固定为
> `3.3.18`，高风险项归零；仍有由 DCloud 内部 esbuild 重复上卷的 14 条中风险。
> DCloud 5.14 仍声明精确 peer `vite@5.2.8`，仓库通过 `.npmrc` 记录这一已完成 H5、
> 微信小程序和 mp-real 构建验证的兼容例外，不得执行 `npm audit fix --force`。
> 3002/3003 默认只监听 `127.0.0.1`；手机扫码联调须显式设置
> `DIANXINYUN_H5_HOST=<本机私有局域网IP>`，脚本拒绝 `0.0.0.0` 和公网绑定。

> 2026-07-31 后端依赖审计：Spring Boot 已从 `3.2.5` 升级到 Java 17 兼容维护线
> `3.5.14`，并固定 Spring Framework `6.2.19`、Tomcat `10.1.57`、Netty
> `4.1.136.Final`、Logback `1.5.34`、Jackson BOM `2.21.5`、Commons Lang
> `3.18.0`、POI `5.4.1` 和 Commons Compress `1.27.1`；MinIO 补充同版本要求的
> `okhttp-jvm:5.3.2`。396 项测试和生产打包通过；118 个运行时依赖的 OSV 精确版本
> 回扫命中为 0。

> 2026-07-31 生产密钥门禁：只有 `local/dev/test` Profile 可以使用仓库内开发 JWT
> 密钥。无开发 Profile 或启用 `prod/production` 时，必须注入至少 32 字节且不等于
> 开发默认值的 `JWT_SECRET`，否则后端启动失败。正式环境不得把实际密钥写入仓库。

> 2026-07-31 文件上传门禁：所有通用附件和工程资料由服务端校验大小、扩展名和文件头；
> 流程照片仅真实栅格图片且最大 15MB，工程资料最大 50MB，电箱导入仅 `.xlsx` 且最大
> 10MB。HTML、SVG、脚本和可执行文件禁止上传；只有 PDF/栅格图允许内联预览，其余强制
> 下载并返回 `nosniff`。文件 JSON 不返回服务器物理路径或对象存储键。
> 本地存储的相对键及历史绝对键还必须解析到配置的上传根目录内，根目录外读取、覆盖、
> 回滚清理和删除固定拒绝；根目录内绝对路径保持兼容。

新对话必须先阅读根目录 `AGENTS.md`，再阅读 `docs/项目总览.md`、`docs/开发状态.md` 和任务相关专题文档。所有判断以当前代码和这些现行文档为准，不依赖旧聊天记录。

## 项目位置与正式范围

项目路径：

```text
/Users/js/Documents/Jinze/Dianxinyun
```

项目是 Web 与微信小程序共用 Spring Boot、MySQL、Redis 和文件存储的多项目现场管理平台。

当前正式权限目录只有：

- 资料管理；
- 巡检管理；
- 质量管理；
- 系统管理。

Web 顶部正式业务入口为“场内管理 / 资料管理 / 巡检管理 / 质量管理”，并在原主题按钮位置按权限显示“系统管理”。时间保留，主题切换、主题面板和密度设置已删除，界面固定为白色舒适密度。

小程序一级入口为“待办 / 资料 / 巡检 / 质量 / 我的”，使用权限驱动的自定义导航和页面守卫。

地图、项目概况、人员、安全总览、摄像头、设备和视频等历史代码可能仍在仓库，但当前入口已隐藏，不属于本期 RBAC 目录。不要因历史源码或旧文档恢复这些入口。

## 本轮已实现

### 统一注册

- Web 和小程序共用独立 `registration_application`。
- 标准注册提交时只保存 BCrypt 摘要；小程序 `WECHAT_QUICK` 只保存微信身份和手机号，不保存申请密码，均不创建 `sys_user`。
- 状态固定为 `PENDING / APPROVED / REJECTED / CANCELLED`。
- 待审批账号、手机号和微信身份使用唯一约束防并发占用。
- 审批通过时在事务内创建用户、分配项目角色和项目范围、建立申请中的微信绑定并写审计日志；平台全局身份不在用户管理页分配，只能由系统初始化或受保护运维流程维护。
- 批准、驳回或申请人取消后清除申请表密码摘要。
- Web 使用图形验证码，手机号首版人工核验；申请状态只允许使用随机状态令牌查询或取消。
- 小程序微信快捷注册只要求微信手机号和真实姓名；审批通过后首次微信登录必须设置初始密码。完成前禁止进入业务、申请项目访问、自助解绑微信或使用 Web 扫码登录。

### 认证与微信快捷登录

- 密码只使用 BCrypt；历史非 BCrypt 凭证禁止登录并标记为待重置。
- JWT 与 Redis 会话默认 7 天，凭证版本变化、停用、改密、微信解绑和关键授权变化会注销全部会话。
- 系统不提供默认测试账号或明文密码；管理员恢复使用一次性 `ADMIN_RESET_USERNAME`、`ADMIN_RESET_PASSWORD`。
- 小程序支持账号密码、微信快捷登录、绑定已有账号和申请注册。
- 新注册申请统一使用手机号作为系统登录账号；Web 与小程序不再展示独立用户名输入，兼容请求若带 `username` 必须与手机号一致。
- 绑定已有账号必须同时验证账号密码和新的 `uni.login` code；手机号相同绝不能自动绑定。
- 同一 AppID 下有效系统用户、OpenID、UnionID 一对一。
- 已启用密码登录的用户重新验证密码后可自助解绑；仅微信登录账号由平台管理员处理。
- Web 扫码登录使用 2 分钟 Redis challenge、浏览器私有校验密钥、小程序明确确认/取消和一次性交换码。二维码不携带 JWT 或浏览器密钥。
- `/auth/user-info` 返回 `initialPasswordSetupRequired`，小程序页面守卫会将该账号强制导向 `pages/initial-password`；`POST /auth/initial-password` 成功后注销旧会话并签发新 JWT。
- 原电箱扫码申请只允许已有用户申请项目访问，不创建微信专用随机账号。

### RBAC 与系统管理

- `PLATFORM_ADMIN` 是唯一平台级全局资格，拥有注册审核、全量用户、角色、菜单、微信、审计和跨项目管理；其他业务角色均为项目角色。该受保护身份不在用户管理页展示或分配。
- 同一成员在同一项目可以拥有多个角色，关系保存在 `sys_user_project_role`，菜单和操作权限按并集合并；`sys_user_project.status=ACTIVE` 是生效前提。
- `PROJECT_ADMIN` 正式显示为“项目经理”且受保护，只有系统管理员能授予、撤销或调整；项目经理不进入系统管理或调整其他成员授权。
- 资料、巡检、质量在角色配置中是三个跨端统一模块，而不是重复的 Web/小程序菜单。关闭模块会同时隐藏两端入口并由后端按目标项目返回 `403`；细分操作权限保留，重新启用后继续生效。系统管理仍为独立 Web 平台目录。
- `/api/v1/auth/user-info` 返回 `menus`、平台 `permissionCodes`、每个项目的 `projectRoles[]`、菜单和有效权限码。项目权限按目标 `projectId` 精确计算；A 项目写权限不能用于 B 项目。
- 平台管理员可见注册审核、用户、角色与权限、菜单与功能、微信绑定和操作日志六个页签；项目经理和普通成员无系统管理入口。项目与角色只在注册审核或用户管理中分配。
- 独立“巡检权限角色模板”已删除；历史表/字段只保留追溯兼容，`/inspection-permission-templates` 返回 HTTP `410`。
- 前端菜单和按钮显隐只用于体验，资料、巡检、质量和系统管理服务端继续返回真实 HTTP 403。

## 数据库状态

`20260807_retire_project_member_management_page.sql` 为本轮新增的待应用迁移，只随代码提交；
未在当前长期本地库或生产库执行。应用前必须备份并明确授权。

2026-07-29 已对当前本地 `dianxinyun` 执行统一注册/RBAC、项目级多角色、跨端业务模块及微信快捷注册增量迁移，当时达到 47 张表并创建 `registration_application`、`sys_user_project_role`、`sys_role_business_module`，以及 `registration_application.registration_mode` 字段。此后场内外访增加 3 表、用印一期增加 11 表，当时共 61 表。微信快捷注册迁移前备份为 `backups/development/dianxinyun-before-wechat-quick-registration-20260729_150000.sql`，SHA-256 为 `b9750dd54c76ea3d2cbe5eebc3436ca1f0cc514f2986f6a9a6dd5452cb3c1c1c`。2026-07-30 又在备份 `backups/development/dianxinyun-before-quality-resilience-20260730.sql` 后执行质量并发与幂等迁移，备份 SHA-256 为 `4393d6d6812a382cd9f96af7830b079c856116832d1900824ba4498e50bdd054`；两份迁移均已连续双跑通过，未执行 `init.sql`。

上段 61 表是用印一期完成时的历史节点；2026-08-10 常用访客资料迁移新增 3 表达到 64 表，
2026-08-14 门卫登记迁移再新增 4 表，现行为 68 表。

当前本地库已执行以下增量脚本。从迁移前 39 表旧副本升级时才按顺序使用，当前 68 表
`dianxinyun` 不得把本清单当作待执行任务重复操作：

```text
backend/src/main/resources/sql/migrations/20260728_unified_registration_rbac_wechat_login.sql
backend/src/main/resources/sql/migrations/20260729_project_multi_role_member_management.sql
backend/src/main/resources/sql/migrations/20260729_project_multi_role_permission_cleanup.sql
backend/src/main/resources/sql/migrations/20260729_shared_business_module_access.sql
backend/src/main/resources/sql/migrations/20260729_wechat_quick_registration_initial_password.sql
backend/src/main/resources/sql/migrations/20260730_quality_issue_resilience.sql
backend/src/main/resources/sql/migrations/20260807_inspection_rectification_closure.sql
backend/src/main/resources/sql/migrations/20260807_site_access_visitor_invitation.sql
backend/src/main/resources/sql/migrations/20260808_seal_application_workflow.sql
backend/src/main/resources/sql/migrations/20260809_project_profile.sql
backend/src/main/resources/sql/migrations/20260810_site_access_reusable_visitor_profile.sql
backend/src/main/resources/sql/migrations/20260810_site_access_visitor_security_hardening.sql
backend/src/main/resources/sql/migrations/20260814_site_access_remove_id_card_requirement.sql
backend/src/main/resources/sql/migrations/20260814_site_access_companion_optional_contact.sql
backend/src/main/resources/sql/migrations/20260814_site_access_guard_visitor_registration.sql
```

统一注册脚本新增 6 张表，项目多角色脚本新增 `sys_user_project_role`；完成后再叠加工程资料、
微信快捷注册、跨端业务模块和质量迁移时达到 47 张表，场内邀请、用印与常用访客资料迁移后达到
64 张表，门卫登记迁移后当前为 68 张表。
项目多角色迁移已在隔离
副本 `dianxinyun_multi_role_verify_20260729` 连续双跑验证，并已应用当前本地库：

- 46 张表；
- `sys_user_project_role` 已按旧职责写入成员角色关联；
- 项目经理名称与受保护标记已存在；`project.member.manage` 将由 2026-08-07 增量迁移软退役；
- 非平台角色已从全局角色关系清除；
- 无法映射旧 `project_role_code` 时会在写入前中止；
- 重跑不会恢复管理员主动停用的菜单或撤销的角色授权。

其他仍停留在旧基线的环境迁移前必须：

1. 备份数据库和相关文件；
2. 执行 `docs/数据库结构.md` 中的重复手机号、有效微信绑定和平台管理员预检；
3. 记录旧角色、微信绑定、项目授权和关键业务表数量；
4. 只执行增量脚本；
5. 使用显式管理员密码重置确保至少一个可用 BCrypt 平台管理员；
6. 回归登录、权限和三大业务。

`backend/src/main/resources/sql/init.sql` 已改为直接中止的兼容入口。全新空库只能使用
`scripts/init-empty-database.sh`，并提供与目标库名一致的精确确认字符串；工具会拒绝
已有任何表的数据库，基线模板不包含 `DROP TABLE / DROP DATABASE`。

## 正式微信配置

微信 mock 默认关闭，只允许显式 `dev`、`local` 或 `test` Profile 开启。非开发环境缺少以下任一配置时必须 fail-closed：

- 正式 AppID；
- 正式 AppSecret；
- 合法 HTTPS 域名；
- 合法 HTTPS 扫码回跳地址。

正式接口已配置连接和读取超时。开发者工具 mock 不能作为上线验收。

## 已完成验证

- 后端 `mvn clean test`：396 项通过，0 失败、0 错误；生产打包通过。
- 后端 118 个运行时 Maven 坐标 OSV 精确版本扫描：0 命中。
- Web `npm run check`：lint、核心类型检查、6 项 Node 单测和生产构建通过；`npm run audit` 为 0 个漏洞。
- 小程序 `npm run type-check`：通过。
- 小程序 `npm run build:h5`：通过。
- 小程序 `npm run build:mp-weixin`：通过。
- 小程序 `npm run build:mp-weixin:real`：通过；DCloud 上游构建链仍有 1 高、14 中风险公告，开发服务不得暴露公网。
- 微信快捷注册针对性后端测试（申请、审批、首次密码、扫码拦截）：通过。
- 角色分配回归：注册审核、用户管理和项目成员角色均由现有角色卡片选择；用户管理以单个项目数量/状态按钮收纳跨项目授权，点击后在加宽、可搜索的“项目与角色详情”侧栏按项目加入、保存角色、暂停/恢复或移出项目，项目选择控件已加高并强化文字层级；不展示或分配平台全局身份。项目经理角色目录仅返回可授予的非项目经理角色及模块、权限名称摘要。
- 隔离接口回归：
  - 管理员登录及 `/auth/user-info` 正常；
  - 用户与注册申请详情接口正常；
  - Web 扫码 `WAITING → SCANNED → CONFIRMED → exchange` 正常；
  - 错误浏览器返回 403，重复消费返回 400；
  - 注册申请可查询、取消，文本项目意向正确保存，取消后密码摘要清除；
  - 普通用户访问全局用户管理返回 403；
  - 旧直接创建用户接口返回 410。

## 上线前仍需完成

- 对正式 Web 在 1280、1440、1920 宽度做顶部导航、登录页和系统管理页面视觉检查。
- 使用正式 AppID/AppSecret、合法 HTTPS 域名和微信真机完成：
  - Web/小程序注册；
  - 微信手机号授权；
  - 微信快捷注册审批后首次密码设置；
  - 首次绑定和微信快捷登录；
  - Web 扫码确认、取消、过期、截屏复用和网络中断；
  - 解绑；
  - 停用、撤权后的会话失效与页面回落。
- 上线前先备份生产候选库，再依据 `docs/数据库结构.md` 的版本预检，只执行该环境尚未
  应用且适用的增量脚本；当前本地 68 表库不得盲目重跑迁移，也不得使用空库初始化入口。
  不可把本地迁移结果直接视为生产验收。

## 常用验证命令

```bash
cd backend
mvn test

cd ../frontend
npm run check
npm run audit

cd ../wechat-miniprogram/site-platform-miniprogram
npm run type-check
npm run build:h5
npm run build:mp-weixin
npm run build:mp-weixin:real

cd ../..
git diff --check
```

## 相关文档

- `AGENTS.md`：仓库开发规则和当前架构。
- `docs/项目总览.md`：正式产品总览。
- `docs/开发状态.md`：当前实现与验证结果。
- `docs/接口规则.md`：认证、注册、扫码和系统管理接口。
- `docs/数据库结构.md`：增量迁移、预检和表结构。
- `docs/权限规则.md`：平台/项目 RBAC 与旧巡检模板兼容规则。
- `docs/模块清单.md`：正式模块与历史保留代码。
- `docs/小程序与PC管理后台交互测试计划.md`：双端和真机验收清单。
- `docs/生产升级与回滚手册.md`：线上版本升级、备份、迁移、验收和回滚硬门槛。
- `docs/系统全局检查修复状态.md`：BUG-001～BUG-031 状态、验证证据和剩余边界。
