# 新对话交接文档

## Git 远程提交约定

- 本机已配置可用的 Git SSH 密钥，本项目远程仓库为
  `git@github.com:jinze-coding/Dianxinyun.git`。
- 后续提交统一使用本机 `git` 通过 SSH 执行状态核对、暂存、提交和
  `git push`；不得把 GitHub CLI `gh` 的安装、登录或认证作为提交前提。
- 除非用户明确要求创建 Pull Request，否则完成 SSH 推送并核对远程提交即可，
  不额外走 GitHub CLI 或网页 PR 流程。
- 只提交可公开复现的配置模板、初始化脚本和脱敏开发数据；
  `.env.local`、`.env.mp-real`、密钥、密码和真实业务数据不得强制加入版本库。

> 2026-08-08 登录默认落点：Web 账号密码、微信扫码登录、已有会话恢复及业务失权回落统一进入“个人待办与消息”；小程序账号密码、微信快捷登录和有效会话冷启动恢复统一进入第一栏“待办”，底部顺序为“待办 / 资料 / 巡检 / 质量 / 我的”。小程序首次设置密码和显式扫码业务续跳继续优先。个人待办不扩大业务权限，本次无后端接口或数据库变更；Web 全量 check/42 项测试/生产构建/0 漏洞审计及小程序类型检查/H5/微信构建均通过。

> 2026-08-08 用印一期交接：当前代码已加入项目印章与独立不透明小程序码、扫码申请、源文件和
> 盖章件、用户直指派 `ANY_ONE` 审批、抄送、站内通知、带原因转办、盖章件复制归档、申请单
> PDF 和两 Sheet 用印台账。Web 已接入资料管理用印分栏、用印审批和个人消息，小程序已接入扫码
> 申请、审批详情及第一列“待办”主栏；个人工作中心通过稳定 `todoKey` 和受控
> `routeCode + routeParams` 聚合巡检、质量与用印。
>
> 权限边界必须保持：审批只认申请快照生成的 `workflow_approval_task.assignee_user_id`；
> `seal.manage` 负责项目级用印管理但不能转办，带原因转办只认 `system.approval.manage`；归档盖章件
> 必须另外具有 `document.upload`，审批管理员未被指派时不能代批。抄送人只读，用户停用或移出项目
> 后不能继续读取或处理记录。
>
> 新增迁移 `20260808_seal_application_workflow.sql` 已在隔离空库按完整迁移链执行，并重复执行用印
> 迁移两次通过；临时库已清理。2026-08-08 又按用户明确授权直接应用到本地开发库，本地表数由
> 50 增至 61，11 张业务表、迁移标记、2 个菜单、5 项权限和平台管理员关联核验通过；修正 MySQL
> 8.4 弃用提示后幂等重跑无重复授权。本次按用户要求未备份本地库、未执行 `init.sql`。生产库尚未
> 应用，生产必须单独注入至少 32 字节的
> `SEAL_SCENE_ENCRYPTION_KEY`。最终回归为后端 539 项测试及生产 JAR、Web 全量 check/42 项测试/
> 生产构建/0 漏洞审计、小程序类型检查/H5/微信构建全部通过。PDF A4 两页的中文精确抽取和分页
> 渲染已检查；Excel 双 Sheet、中文数据、冻结/筛选、A3 打印区、重复标题和公式注入防护已通过
> 结构测试及 LibreOffice 渲染。自动化环境无法控制 WPS，仍需实际 WPS 打开检查列宽、筛选和打印
> 预览；正式微信二维码还需真实配置与真机闭环。首期不发送微信订阅消息。
>
> 最终安全边界：草稿抄送人不能读取草稿或附件，重复创建/提交幂等，源附件变更与提交共用申请
> 行锁；正式 PDF 仅对 `APPROVED` 开放。配置保存按用户、成员、配置顺序锁定一致快照。账号停用、
> 成员移除或暂停会事务内清理审批配置和待办，无候选时通知管理员转办；旧
> `PUT /system/users/{id}/access` 已 fail-closed 返回 `410`。存在非草稿用印记录的项目不得永久删除。

> 2026-08-08 场内外访删除：Web 场内管理仅向平台管理员显示“删除”，任意状态邀请都复用统一影响预览、勾选确认、5 分钟令牌和目标行锁。事务按实名人员、场内业务审计、邀请主记录顺序物理删除，平台 `sys_operation_log` 保留并新增删除摘要；项目仍不能绕过逐条确认直接删除外访数据。本次无数据库迁移，后端 479 项测试与 Web 全量 check/37 项测试/生产构建通过，未对长期本地库做破坏性验收。

> 2026-08-08 密码重置交互：Web 用户管理不再通过浏览器 `prompt/alert` 收集或反馈密码，改为系统内弹窗；包含目标账号、新密码、确认密码、显示/隐藏、8–72 位且字母数字组合的实时校验、提交状态和成功/错误反馈。后端接口与会话注销逻辑不变，无数据库迁移；Web 全量 check/35 项测试/生产构建通过。

> 2026-08-08 注册审核删除：平台管理员可在 Web 注册审核列表删除任意状态的注册申请，统一使用影响预览、勾选确认、5 分钟一次性令牌和目标行锁。只删除 `registration_application` 当前行；已创建用户、项目角色、微信绑定和审核操作日志保留，待审核唯一占用随记录释放。本次无数据库迁移；后端 477 项测试、Web 全量 check/34 项测试/生产构建通过。

> 2026-08-08 角色名称唯一校验：有效角色名称全局唯一，新增与改名均忽略首尾空格和英文大小写检查；重名返回 HTTP `409`。角色定义写入通过平台管理员角色行串行化，避免并发创建绕过检查。Web 弹窗即时提示并禁用保存，后端是最终校验来源。本次无数据库迁移；后端 473 项测试、Web 全量 check/34 项测试/生产构建通过。

> 2026-08-07 场内管理 V1：Web 顶部已增加“场内管理”，可创建单次外访邀请、生成专属小程序码、查看/纠错/作废并按人员导出；小程序公开页 `pages/public/visitor-invite` 免登录一次性填报，提交后锁定。新增 `SITE_ACCESS / WEB_SITE_ACCESS / SITE_VISITOR`、`site_access.view/manage/export` 和 3 张业务表迁移。手机号、身份证、令牌及审计快照使用独立 AES-GCM 密钥；公开接口先限流，提交行锁防重。
>
> `20260807_site_access_visitor_invitation.sql` 已应用到本地开发库，表数当时由 47 增至 50；生产库尚未应用。非开发环境必须配置独立且至少 32 字节的 `VISITOR_DATA_ENCRYPTION_KEY`。当前后端 472 项测试与生产打包、Web 全量 check/33 项测试/生产构建/0 漏洞审计、小程序类型检查/电箱和外访场景解析测试/H5/微信构建通过；仍需正式微信真机扫码闭环和真实 Excel/WPS 打开检查。

更新时间：2026-08-08。

> 2026-08-07 管理员全权限与强制删除：`PLATFORM_ADMIN` 自动获得全部当前启用正式菜单、
> 操作权限、四个模块和全部项目，不写项目成员关系；Web 提供统一影响确认弹窗，小程序仅同步
> 全权限且不增加硬删除控件。用户、项目、角色、资料、电箱、巡检和质量硬删除必须使用 5 分钟
> 一次性令牌并在执行时重算影响。撤权后清当前责任人、保留任务和历史、注销目标用户全部会话。
> Web 用户管理仅向平台管理员显示删除入口；当前登录账号和最后一个可恢复管理员受保护，删除
> 其他用户时同步清授权、微信绑定与订阅状态并保留历史业务及操作日志。
> 本次无数据库迁移，破坏性验收只能使用隔离数据库副本和临时文件目录。当前回归已通过
> 后端 448 项测试、Web 27 项测试及生产构建/审计、小程序类型检查与 H5/微信构建；
> 微信开发者工具真实交互和隔离库破坏性验收尚未执行。

> 2026-08-07 注册项目选择：新申请必须通过公共搜索选择至少一个项目，可多选；Web 和小程序
> 都提交 `desiredProjectIds[]`，不再显示自由文本或项目 ID 输入。公共搜索至少两个字符、最多
> 20 条、只返回最小字段并排除停工项目。审核不再附加默认 `USER`，管理员必须明确勾选最终项目
> 和至少一个实际角色；服务端在提交与审批两处重新校验。历史内置 `USER` 也已放开管理员删除，不再自动回落或补角色。

> 2026-08-03 巡检报表导出规则：旧 `month/boxId` 月报继续兼容；Web 日期范围报表使用
> `startDate/endDate/boxIds`，结束日期不晚于当天且首尾最多 366 天。一个电箱允许
> `INSPECTION_DAILY_SUBMIT` 或 `SUMMARY_EXPORT`，多个或全部电箱必须 `SUMMARY_EXPORT`。
> 范围报表只应用日期和电箱条件，应检统计按历史巡检范围计算并批量加载检查项与范围历史。

> 2026-08-01 巡检范围写入规则：变更前锁定目标电箱；旧开放区间关闭和新范围历史新增
> 都必须影响 1 行，失败返回 HTTP `409` 并回滚。原因最长 300 字符，操作人来自登录态。

> 2026-08-01 项目写入规则：新增/编辑不能覆盖主键、删除标记和创建时间；字段长度、状态、
> 日期和坐标必须在写库前校验。项目新增、更新、定位和定位审计写入必须影响 1 行，定位
> 主记录与审计日志同事务，失败返回 HTTP `409` 并回滚。

> 2026-08-01 安全教育写入规则：创建前完整校验批次、去重人员和本人刚上传的未绑定培训
> 资料；所有人员必须同项目。批次、人员关联、人员教育状态和附件绑定写入数量不符时返回
> HTTP `409` 并回滚，批次不存在返回 HTTP `404`。

> 2026-08-01 人员写入规则：人员请求体不能覆盖主键、项目、状态、删除标记和创建时间；
> 状态只走进退场/安全教育流程。人员、流水、证件和附件绑定写入必须影响 1 行。证件附件
> 必须同项目、类型为 `PERSON_CERTIFICATE`，未绑定附件只能由上传人本人绑定。

> 2026-08-01 摄像头/设备写入规则：所属项目和持久化字段只以后端原记录为准；名称必填，
> 字段长度在写库前校验，摄像头在线状态只允许 `0 / 1`。新增、更新、删除影响行数不是
> 1 时返回 HTTP `409`；设备自定义扩展类型继续兼容。

> 2026-08-01 巡检写入规则：新建日检由服务端固定模板和来源，必须恰好包含六个不重复
> 的规范检查项，名称由服务端写入，异常项必须填写说明。服务端锁定目标电箱后检查同箱
> 同日唯一性；巡检记录、检查项和巡检设置写入影响行数不是 1 时返回 HTTP `409` 并回滚。
> 当前模板插座编码为 `SOCKET_220V / SOCKET_380V`，旧编码只能通过正式迁移对齐。

> 2026-08-01 工程资料写入规则：目录、文件元数据、资料、版本、当前版本指针、归档/
> 回收站状态、恢复、永久删除和操作日志的单记录写入必须校验影响行数为 1，失败返回
> HTTP `409` 并回滚。资料编号最长 100 字符，资料备注和版本说明最长 500 字符，必须
> 在物理文件落盘前校验。上传回滚清理新文件，永久删除只在数据库提交后清理旧文件。

> 2026-07-31 电箱写入规则：所属项目创建后不可编辑迁移，`status`、`qrStatus` 只能由
> 专用生命周期接口修改。传入责任人 ID 时必须校验启用账号并使用服务端姓名。停用/拆除
> 走 `BOX_MANAGE`，二维码换绑、打印和日志走 `BOX_QR_MANAGE`，公开访问启停走
> `BOX_PUBLIC_ACCESS`；普通编辑改变二维码或公开状态还需追加细分权限。主记录和二维码
> 日志写入影响行数不是 1 时返回 HTTP `409` 并回滚。

> 2026-07-31 当前账号微信操作限流：微信绑定、解绑和项目访问申请必须同时按受信 IP
> 与规范化账号限流；两套兼容解绑入口共享额度，密码校验前每账号 10 分钟最多 5 次。
> 不得只保留 IP 限流，Redis 键中不得出现明文账号或 IP。

> 2026-07-31 二维码标签 HTML 安全规则：电箱编号、名称、位置和场景码写入打印/下载
> HTML 前必须转义；二维码图像只接受服务端生成的 PNG Base64 或编码 SVG Data URL，
> 禁止恢复原始 SVG/任意 URL 直接拼接。打印文档保留 CSP，打印窗口必须切断 opener。

> 2026-07-31 公开巡检范围查询规则：近 30 天摘要和公开逐日月表必须一次加载目标日期
> 范围的巡检范围历史，再在内存中解析每日应检状态；不得恢复为按自然日逐次查询数据库。
> 历史范围、建箱日期、到期日期和箱日去重口径保持不变。

> 2026-07-31 统一扫码公开边界：调用者没有目标项目巡检权限且电箱公开访问关闭时，
> `/api/v1/scan/electric-boxes/{sceneCode}` 必须在巡检范围和当天记录查询前返回
> HTTP `403`，不得返回电箱编号、名称、位置等元数据。正常匿名公开扫码也不查询当天
> 内部巡检记录。

> 2026-07-31 敏感写入规则：密码、账号状态、微信绑定、项目成员授权、角色、菜单、
> 操作权限和巡检权限模板的单记录新增/更新/删除必须校验数据库影响行数为 1；写入未
> 生效时返回 HTTP `409` 并回滚，禁止继续注销会话、替换权限或记录成功结果。

> 2026-07-31 微信注册临时身份事务规则：标准微信注册对 Redis 临时身份的消费必须与
> 数据库事务结果联动；事务回滚后恢复原身份会话及 10 分钟有效期并释放占用标记，提交
> 成功后清理占用标记。并发请求仍只能成功占用一次，不得因业务校验或数据库回滚强迫
> 用户重新微信授权。

> 2026-07-31 公开巡检限流：免登录统一扫码解析和近 30 天摘要按真实客户端 IP 每
> 10 分钟最多 60 次，公开月表每 10 分钟最多 30 次；必须在可选登录态解析和业务查询
> 前执行，超限返回 HTTP `429`，Redis 限流键只保存 HMAC 摘要。

> 2026-07-31 权限失效顺序：项目成员、角色、菜单权限、巡检模板、账号状态或微信绑定
> 发生变更时，必须在数据库事务提交前立即清理项目缓存并注销会话，提交成功后再重复
> 清理一次，防止并发请求按旧数据回写缓存或补签会话。首次密码设置会签发新令牌，不得
> 使用提交后全量注销。

> 2026-07-31 巡检统计口径：公开近 30 天汇总、公开月表和内部汇总的应检、已检、
> 漏检、异常必须按该日期的历史巡检范围和到期日期计算，并按箱日去重；不得把建箱前、
> 非巡检范围、未来日期或同日重复记录计入统计。

> 2026-07-31 通用附件事务顺序：上传/替换的新物理文件在事务回滚时清理；替换和删除
> 只能在新元数据或逻辑删除提交后清理旧文件。元数据写入行数异常返回 HTTP `409`，
> 禁止忽略数据库失败后继续删除物理文件。

> 2026-07-31 资料永久删除顺序：工程资料先在事务内标记文件元数据并删除版本/资料关系，
> 数据库提交后才删除物理对象；回滚不触碰原文件。提交后物理清理失败时保留逻辑删除
> 元数据供排查和重试，不能改回“先删文件再提交数据库”。

> 2026-07-31 项目删除门禁：`DELETE /projects/{id}` 仅允许平台管理员删除完全空项目；
> 任一成员、角色、资料、巡检、质量、人员、设备、摄像头或微信申请记录仍存在时返回
> HTTP `409`。禁止删除项目主记录后遗留孤儿数据，也不通过该接口级联清库。

> 2026-07-31 登录限流：密码登录和微信绑定登录同时按真实客户端 IP、规范化账号独立
> 限流，账号额度不随 IP 改变而重置。所有 Redis 限流主体用当前环境 JWT 密钥生成
> HMAC-SHA256，键中不保留明文用户名、手机号、IP、状态令牌或扫码 challenge。

> 2026-07-31 反向代理 IP 门禁：生产必须配置 `FORWARD_HEADERS_STRATEGY=NATIVE`，并由
> Nginx 设置 `X-Forwarded-For`、`X-Forwarded-Proto`；否则后端拒绝启动。业务限流和
> 审计只读取 Tomcat 校验后的 `request.getRemoteAddr()`，不直接信任客户端请求头。
> 后端 8080 不得直接暴露公网。

> 2026-07-31 生产部署门禁：正式环境必须从部署平台注入非 root MySQL 账号、非空数据库
> 密码、启用 TLS 的 `DB_URL`、非空 `REDIS_PASSWORD` 和独立 JWT/微信配置；完整占位
> 模板见 `backend/.env.example`，Spring Boot 不会自动加载该文件。Knife4j、Swagger UI
> 与 OpenAPI JSON 生产固定关闭，误开会拒绝启动，路径级过滤同时确保 `/doc.html`、
> `/webjars/**`、`/swagger-ui/**`、`/v3/api-docs/**` 返回 `404`。本地统一开发脚本会
> 显式开启文档。springdoc 已按 Spring Boot 3.5 兼容线固定为 `2.8.17`。

> 2026-07-31 线上版本差异：`zhihuiyz.xyz` 当前仍运行缺少验证码、统一注册、Web 扫码
> 和微信快捷登录接口的旧后端，不能通过修改小程序 API 前缀解决。升级前必须按
> `docs/生产升级与回滚手册.md` 确认生产授权、备份、数据库迁移状态、服务路径和回滚
> 负责人；仓库没有生产 SSH、systemd/Nginx 路径或密钥，不得猜测后直接发布。

> 2026-07-31 真机接口构建门禁：`mp-real` 模式会强制校验
> `VITE_API_BASE_URL`，缺失、非 HTTPS、localhost、`127.0.0.1`、局域网地址、
> 非 `/api/v1` 前缀或启用 mock 都会直接拒绝构建。真机配置模板为
> `wechat-miniprogram/site-platform-miniprogram/.env.mp-real.example`，复制为本机
> `.env.mp-real` 后再执行 `npm run build:mp-weixin:real`。构建通过只代表代码包地址
> 合法；上传前仍必须确认线上 `/api/v1/auth/captcha` 返回业务 `code=200`。统一开发
> 脚本使用 `dev:mp-weixin` 连接本机 8080 后端，不读取生产 `mp-real` 地址。

> 2026-07-30 微信体验版联调：小程序真实环境使用
> `https://zhihuiyz.xyz/api/v1`，执行
> `npm run build:mp-weixin:real` 生成上传用代码包。不得使用默认
> `build:mp-weixin` 产物或局域网 `192.168.*:8080` 地址上传体验版；默认构建
> 未提供绝对地址时会回退到 `127.0.0.1`，真机无法访问。
> `scripts/dev-services.sh start/restart` 默认生成 `mp-local` 开发产物，并自动使用当前电脑
> 局域网 IPv4 的 `http://<LAN-IP>:8080/api/v1` 连接本机开发后端和开发库。该包只用于
> 微信开发者工具模拟器；手机预览必须重新切回 `mp-real` HTTPS 构建。

> 2026-07-30 开发巡检码联调：手机扫描开发 H5 码后，接口必须经 `http://<本机IP>:3003/api/v1` 同源代理访问后端。H5 请求封装默认使用 `/api/v1`，禁止回退到手机自身的 `127.0.0.1:8080`；测试时应关闭微信内旧页面后重新扫码。
>
> 2026-07-30 局域网联调：Web `:3002` 与小程序 H5 `:3003` 的 Vite 代理会把局域网浏览器 Origin 规范化为本机 8080 代理目标，修复通过 `http://<本机IP>:3002` 登录时的 `403 Invalid CORS request`。后端 CORS 白名单未放宽；其他设备只访问 3002/3003，不直接访问 8080。

> 2026-07-31 小程序依赖审计：已通过安全版本覆盖把 25 项公告降到 15 项，仅余 1 条
> Vite 5 高风险及由 esbuild 重复上卷的 14 条中风险。DCloud 当前套件仍锁定 Vite
> `5.2.8` / esbuild `0.20.2`，不得执行 `npm audit fix --force` 或强换 Vite 8。
> 3003 开发服务的 CORS 只允许 localhost 和私有局域网 Origin，禁止暴露公网；剩余项
> 等 DCloud 官方升级后再根治。

> 2026-07-31 后端依赖审计：Spring Boot 已从 `3.2.5` 升级到 Java 17 兼容维护线
> `3.5.14`，并固定 Spring Framework `6.2.19`、Tomcat `10.1.57`、Netty
> `4.1.136.Final`、Logback `1.5.34`、Jackson BOM `2.21.5`、Commons Lang
> `3.18.0`、POI `5.4.1` 和 Commons Compress `1.27.1`；MinIO 补充同版本要求的
> `okhttp-jvm:5.3.2`。396 项测试和生产打包通过；118 个运行时依赖的 OSV 精确版本
> 回扫命中为 0。

> 2026-07-31 生产密钥门禁：只有 `local/dev/test` Profile 可以使用仓库内开发 JWT
> 密钥。无开发 Profile 或启用 `prod/production` 时，必须注入至少 32 字节且不等于
> 开发默认值的 `JWT_SECRET`，否则后端启动失败。正式环境不得把实际密钥写入仓库。

> 2026-07-31 文件上传门禁：所有通用附件和工程资料由服务端校验大小、扩展名和文件头；
> 流程照片仅真实栅格图片且最大 15MB，工程资料最大 50MB，电箱导入仅 `.xlsx` 且最大
> 10MB。HTML、SVG、脚本和可执行文件禁止上传；只有 PDF/栅格图允许内联预览，其余强制
> 下载并返回 `nosniff`。文件 JSON 不返回服务器物理路径或对象存储键。
> 本地存储的相对键及历史绝对键还必须解析到配置的上传根目录内，根目录外读取、覆盖、
> 回滚清理和删除固定拒绝；根目录内绝对路径保持兼容。

新对话必须先阅读根目录 `AGENTS.md`，再阅读 `docs/项目总览.md`、`docs/开发状态.md` 和任务相关专题文档。所有判断以当前代码和这些现行文档为准，不依赖旧聊天记录。

## 项目位置与正式范围

项目路径：

```text
/Users/js/Documents/Jinze/Dianxinyun
```

项目是 Web 与微信小程序共用 Spring Boot、MySQL、Redis 和文件存储的多项目现场管理平台。

当前正式权限目录只有：

- 资料管理；
- 巡检管理；
- 质量管理；
- 系统管理。

Web 顶部正式业务入口为“场内管理 / 资料管理 / 巡检管理 / 质量管理”，并在原主题按钮位置按权限显示“系统管理”。时间保留，主题切换、主题面板和密度设置已删除，界面固定为白色舒适密度。

小程序一级入口为“待办 / 资料 / 巡检 / 质量 / 我的”，使用权限驱动的自定义导航和页面守卫。

地图、项目概况、人员、安全总览、摄像头、设备和视频等历史代码可能仍在仓库，但当前入口已隐藏，不属于本期 RBAC 目录。不要因历史源码或旧文档恢复这些入口。

## 本轮已实现

### 统一注册

- Web 和小程序共用独立 `registration_application`。
- 标准注册提交时只保存 BCrypt 摘要；小程序 `WECHAT_QUICK` 只保存微信身份和手机号，不保存申请密码，均不创建 `sys_user`。
- 状态固定为 `PENDING / APPROVED / REJECTED / CANCELLED`。
- 待审批账号、手机号和微信身份使用唯一约束防并发占用。
- 审批通过时在事务内创建用户、分配项目角色和项目范围、建立申请中的微信绑定并写审计日志；平台全局身份不在用户管理页分配，只能由系统初始化或受保护运维流程维护。
- 批准、驳回或申请人取消后清除申请表密码摘要。
- Web 使用图形验证码，手机号首版人工核验；申请状态只允许使用随机状态令牌查询或取消。
- 小程序微信快捷注册只要求微信手机号和真实姓名；审批通过后首次微信登录必须设置初始密码。完成前禁止进入业务、申请项目访问、自助解绑微信或使用 Web 扫码登录。

### 认证与微信快捷登录

- 密码只使用 BCrypt；历史非 BCrypt 凭证禁止登录并标记为待重置。
- JWT 与 Redis 会话默认 7 天，凭证版本变化、停用、改密、微信解绑和关键授权变化会注销全部会话。
- 系统不提供默认测试账号或明文密码；管理员恢复使用一次性 `ADMIN_RESET_USERNAME`、`ADMIN_RESET_PASSWORD`。
- 小程序支持账号密码、微信快捷登录、绑定已有账号和申请注册。
- 新注册申请统一使用手机号作为系统登录账号；Web 与小程序不再展示独立用户名输入，兼容请求若带 `username` 必须与手机号一致。
- 绑定已有账号必须同时验证账号密码和新的 `uni.login` code；手机号相同绝不能自动绑定。
- 同一 AppID 下有效系统用户、OpenID、UnionID 一对一。
- 已启用密码登录的用户重新验证密码后可自助解绑；仅微信登录账号由平台管理员处理。
- Web 扫码登录使用 2 分钟 Redis challenge、浏览器私有校验密钥、小程序明确确认/取消和一次性交换码。二维码不携带 JWT 或浏览器密钥。
- `/auth/user-info` 返回 `initialPasswordSetupRequired`，小程序页面守卫会将该账号强制导向 `pages/initial-password`；`POST /auth/initial-password` 成功后注销旧会话并签发新 JWT。
- 原电箱扫码申请只允许已有用户申请项目访问，不创建微信专用随机账号。

### RBAC 与系统管理

- `PLATFORM_ADMIN` 是唯一平台级全局资格，拥有注册审核、全量用户、角色、菜单、微信、审计和跨项目管理；其他业务角色均为项目角色。该受保护身份不在用户管理页展示或分配。
- 同一成员在同一项目可以拥有多个角色，关系保存在 `sys_user_project_role`，菜单和操作权限按并集合并；`sys_user_project.status=ACTIVE` 是生效前提。
- `PROJECT_ADMIN` 正式显示为“项目经理”且受保护，只有系统管理员能授予、撤销或调整；项目经理不进入系统管理或调整其他成员授权。
- 资料、巡检、质量在角色配置中是三个跨端统一模块，而不是重复的 Web/小程序菜单。关闭模块会同时隐藏两端入口并由后端按目标项目返回 `403`；细分操作权限保留，重新启用后继续生效。系统管理仍为独立 Web 平台目录。
- `/api/v1/auth/user-info` 返回 `menus`、平台 `permissionCodes`、每个项目的 `projectRoles[]`、菜单和有效权限码。项目权限按目标 `projectId` 精确计算；A 项目写权限不能用于 B 项目。
- 平台管理员可见注册审核、用户、角色与权限、菜单与功能、微信绑定和操作日志六个页签；项目经理和普通成员无系统管理入口。项目与角色只在注册审核或用户管理中分配。
- 独立“巡检权限角色模板”已删除；历史表/字段只保留追溯兼容，`/inspection-permission-templates` 返回 HTTP `410`。
- 前端菜单和按钮显隐只用于体验，资料、巡检、质量和系统管理服务端继续返回真实 HTTP 403。

## 数据库状态

`20260807_retire_project_member_management_page.sql` 为本轮新增的待应用迁移，只随代码提交；
未在当前长期本地库或生产库执行。应用前必须备份并明确授权。

2026-07-29 已对当前本地 `dianxinyun` 执行统一注册/RBAC、项目级多角色、跨端业务模块及微信快捷注册增量迁移，当时达到 47 张表并创建 `registration_application`、`sys_user_project_role`、`sys_role_business_module`，以及 `registration_application.registration_mode` 字段。此后场内外访增加 3 表、用印一期增加 11 表，当前共 61 表。微信快捷注册迁移前备份为 `backups/development/dianxinyun-before-wechat-quick-registration-20260729_150000.sql`，SHA-256 为 `b9750dd54c76ea3d2cbe5eebc3436ca1f0cc514f2986f6a9a6dd5452cb3c1c1c`。2026-07-30 又在备份 `backups/development/dianxinyun-before-quality-resilience-20260730.sql` 后执行质量并发与幂等迁移，备份 SHA-256 为 `4393d6d6812a382cd9f96af7830b079c856116832d1900824ba4498e50bdd054`；两份迁移均已连续双跑通过，未执行 `init.sql`。

当前本地库已执行以下增量脚本。从迁移前 39 表旧副本升级时才按顺序使用，当前 61 表
`dianxinyun` 不得把本清单当作待执行任务重复操作：

```text
backend/src/main/resources/sql/migrations/20260728_unified_registration_rbac_wechat_login.sql
backend/src/main/resources/sql/migrations/20260729_project_multi_role_member_management.sql
backend/src/main/resources/sql/migrations/20260729_project_multi_role_permission_cleanup.sql
backend/src/main/resources/sql/migrations/20260729_shared_business_module_access.sql
backend/src/main/resources/sql/migrations/20260729_wechat_quick_registration_initial_password.sql
backend/src/main/resources/sql/migrations/20260730_quality_issue_resilience.sql
backend/src/main/resources/sql/migrations/20260807_inspection_rectification_closure.sql
backend/src/main/resources/sql/migrations/20260807_site_access_visitor_invitation.sql
backend/src/main/resources/sql/migrations/20260808_seal_application_workflow.sql
```

统一注册脚本新增 6 张表，项目多角色脚本新增 `sys_user_project_role`；完成后再叠加工程资料、
微信快捷注册、跨端业务模块和质量迁移时达到 47 张表，场内外访与用印迁移后当前为 61 张表。
项目多角色迁移已在隔离
副本 `dianxinyun_multi_role_verify_20260729` 连续双跑验证，并已应用当前本地库：

- 46 张表；
- `sys_user_project_role` 已按旧职责写入成员角色关联；
- 项目经理名称与受保护标记已存在；`project.member.manage` 将由 2026-08-07 增量迁移软退役；
- 非平台角色已从全局角色关系清除；
- 无法映射旧 `project_role_code` 时会在写入前中止；
- 重跑不会恢复管理员主动停用的菜单或撤销的角色授权。

其他仍停留在旧基线的环境迁移前必须：

1. 备份数据库和相关文件；
2. 执行 `docs/数据库结构.md` 中的重复手机号、有效微信绑定和平台管理员预检；
3. 记录旧角色、微信绑定、项目授权和关键业务表数量；
4. 只执行增量脚本；
5. 使用显式管理员密码重置确保至少一个可用 BCrypt 平台管理员；
6. 回归登录、权限和三大业务。

`backend/src/main/resources/sql/init.sql` 已改为直接中止的兼容入口。全新空库只能使用
`scripts/init-empty-database.sh`，并提供与目标库名一致的精确确认字符串；工具会拒绝
已有任何表的数据库，基线模板不包含 `DROP TABLE / DROP DATABASE`。

## 正式微信配置

微信 mock 默认关闭，只允许显式 `dev`、`local` 或 `test` Profile 开启。非开发环境缺少以下任一配置时必须 fail-closed：

- 正式 AppID；
- 正式 AppSecret；
- 合法 HTTPS 域名；
- 合法 HTTPS 扫码回跳地址。

正式接口已配置连接和读取超时。开发者工具 mock 不能作为上线验收。

## 已完成验证

- 后端 `mvn clean test`：396 项通过，0 失败、0 错误；生产打包通过。
- 后端 118 个运行时 Maven 坐标 OSV 精确版本扫描：0 命中。
- Web `npm run check`：lint、核心类型检查、6 项 Node 单测和生产构建通过；`npm run audit` 为 0 个漏洞。
- 小程序 `npm run type-check`：通过。
- 小程序 `npm run build:h5`：通过。
- 小程序 `npm run build:mp-weixin`：通过。
- 小程序 `npm run build:mp-weixin:real`：通过；DCloud 上游构建链仍有 1 高、14 中风险公告，开发服务不得暴露公网。
- 微信快捷注册针对性后端测试（申请、审批、首次密码、扫码拦截）：通过。
- 角色分配回归：注册审核、用户管理和项目成员角色均由现有角色卡片选择；用户管理以单个项目数量/状态按钮收纳跨项目授权，点击后在加宽、可搜索的“项目与角色详情”侧栏按项目加入、保存角色、暂停/恢复或移出项目，项目选择控件已加高并强化文字层级；不展示或分配平台全局身份。项目经理角色目录仅返回可授予的非项目经理角色及模块、权限名称摘要。
- 隔离接口回归：
  - 管理员登录及 `/auth/user-info` 正常；
  - 用户与注册申请详情接口正常；
  - Web 扫码 `WAITING → SCANNED → CONFIRMED → exchange` 正常；
  - 错误浏览器返回 403，重复消费返回 400；
  - 注册申请可查询、取消，文本项目意向正确保存，取消后密码摘要清除；
  - 普通用户访问全局用户管理返回 403；
  - 旧直接创建用户接口返回 410。

## 上线前仍需完成

- 对正式 Web 在 1280、1440、1920 宽度做顶部导航、登录页和系统管理页面视觉检查。
- 使用正式 AppID/AppSecret、合法 HTTPS 域名和微信真机完成：
  - Web/小程序注册；
  - 微信手机号授权；
  - 微信快捷注册审批后首次密码设置；
  - 首次绑定和微信快捷登录；
  - Web 扫码确认、取消、过期、截屏复用和网络中断；
  - 解绑；
  - 停用、撤权后的会话失效与页面回落。
- 上线前先备份生产候选库，再依据 `docs/数据库结构.md` 的版本预检，只执行该环境尚未
  应用且适用的增量脚本；当前本地 61 表库不得盲目重跑迁移，也不得使用空库初始化入口。
  不可把本地迁移结果直接视为生产验收。

## 常用验证命令

```bash
cd backend
mvn test

cd ../frontend
npm run check
npm run audit

cd ../wechat-miniprogram/site-platform-miniprogram
npm run type-check
npm run build:h5
npm run build:mp-weixin
npm run build:mp-weixin:real

cd ../..
git diff --check
```

## 相关文档

- `AGENTS.md`：仓库开发规则和当前架构。
- `docs/项目总览.md`：正式产品总览。
- `docs/开发状态.md`：当前实现与验证结果。
- `docs/接口规则.md`：认证、注册、扫码和系统管理接口。
- `docs/数据库结构.md`：增量迁移、预检和表结构。
- `docs/权限规则.md`：平台/项目 RBAC 与旧巡检模板兼容规则。
- `docs/模块清单.md`：正式模块与历史保留代码。
- `docs/小程序与PC管理后台交互测试计划.md`：双端和真机验收清单。
- `docs/生产升级与回滚手册.md`：线上版本升级、备份、迁移、验收和回滚硬门槛。
- `docs/系统全局检查修复状态.md`：BUG-001～BUG-031 状态、验证证据和剩余边界。
