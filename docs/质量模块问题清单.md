# 质量模块问题清单

## 2026-09-17 已删除问题的周检允许退回

用户明确扩展规则：已提交周检的问题部分或全部被删除后，管理员仍可填写原因退回同周草稿。仅恢复现存问题、检查日期、结论和现存周检现场照片；已删除问题及附件不重建，原删除审计保留。所有现存问题仍须未整改、复查或作废；不允许超出原提交数量或已有异常草稿。版本、行锁及事务约束保持，系统审计记录原提交数、恢复数、已删除未恢复数与原因。已永久删除记录的细项历史不可重建，不对其宣称完成整改历史检查。

Web 确认框明确删除内容不恢复，详情区分提交时数量、当前保留和已删除数量；不再将删除后的空列表误写为“本周检查无质量问题”。无需 SQL 或配置变更，小程序沿用现有草稿接口。

验证：46 项相关后端单测、9 项独立合成 MySQL/Redis 集成、Web check（196 项、lint、类型、构建）和 audit（0 漏洞）通过。集成覆盖真实永久删除后回退、部分删除、回退后新增再提交、删除照片不复活、现场照片字节保留、现存整改问题拒绝、并发、重复请求及审计失败回滚。测试仅使用按当前本地库结构建立的合成库及临时附件，不改写真实业务。最初空库模板因缺少历史账号字段未启动测试，改用当前结构后通过；未将测试初始化失败算为通过。

补丁准备以已上线 20260917-1330 为基线，仅后端/Web；正式上传执行由用户完成，未提交 Git。

## 2026-09-17 质量问题上传入口

小程序质量首页「最近周检」、周检记录列表和 Web 周检记录中的「共享草稿」标签改为蓝色「＋ 质量问题上传」小按钮；点击进入对应周次的问题编辑页。列表筛选与相关入口说明同步，只有草稿且具备原 `quality.manage` 权限时显示上传按钮；只读记录保留状态和查看入口。

仅调整客户端入口及文案，原共享草稿、提交巡检问题、结束本周巡检与整改流程保持，无后端、数据库、配置或依赖变化。已在原微信开发者工具核对首页、列表和按钮跳转，在原 3002 Web 核对按钮及对应周次编辑弹窗，未保存或结束实际记录。Web check（196 项测试、lint、类型、构建）与 audit（0 漏洞）、小程序类型检查、H5/微信构建及访客样式校验通过。本地开发环境已生效；既有封存发布包不含本次变更，未正式发布或提交 Git。

## 2026-09-17 提交巡检问题按钮

小程序和 Web 编辑页的「保存草稿」统一改名为「提交巡检问题」，处理中显示「正在提交...」，删除问题提示同步引用新按钮名称。仅更改文案，实际仍保存共享草稿，可继续编辑；问题进入整改闭环仍由「结束本周巡检」执行。两端原页面已核对新按钮，未点击保存或结束实际记录；Web check（196 项）及 audit 0 漏洞、小程序类型检查和 H5/微信构建通过。原开发环境已生效，未正式发布或提交 Git。

## 2026-09-17 结束本周巡检文案

小程序编辑页的「提交周检」和 Web 编辑弹窗的「整批提交」统一改为「结束本周巡检」，保留问题数量；确认提示、处理中、成功及失败提示同步。小程序首页引导、草稿详情说明及 Web 退回草稿后的操作说明采用相同称呼。

仅调整客户端文案，保存草稿、结束时校验并保存后整批提交、问题独立整改复查、原状态及权限规则保持；不增加后端或数据库变更。原微信开发者工具已显示新按钮，原 3002 Web 已核对按钮和确认后取消，草稿仍为原版本，未结束实际记录。Web check（196 项测试、lint、类型、构建）及 audit（0 漏洞）、小程序类型检查、H5/微信构建与访客样式校验通过。仅更新本地开发环境，既有封存发布包不含本次文案，未上传发布或提交 Git。

## 2026-09-16 管理员将误提交周检退回草稿

Web「周检记录」已提交行和详情增加「退回草稿」。仅系统管理员（`PLATFORM_ADMIN`）可操作，填写退回原因后确认；成功直接打开恢复的共享草稿，可继续修改、添加问题并重新提交。其他拥有 `quality.manage` 的项目成员可以按原权限继续编辑恢复的草稿，但不能执行退回。

按用户确认，只允许尚未发生整改或复查的整份周检退回。所有问题必须仍为 `PENDING`，无整改/复查字段或历史日志；即使复查驳回又变成待整改，也不能退回。作废、已删除问题或草稿/提交数量异常同样阻止，避免不完整恢复。无问题周检可退回并保留结论和现场照片。

回退保留检查日期、结论、问题内容、顺序、整改人、期限和照片。每个原问题保留记录和日志，标记 `WITHDRAWN/deleted=1` 后退出当前问题列表、统计及整改待办；重新提交生成新问题，旧问题不重新激活。周检提交编号、提交信息和提交数量清空，创建人保留、版本递增、最近编辑人更新。文件仅反向绑定到草稿，物理原件不变；问题、文件、周检和操作日志必须同事务成功或回滚。

已有 62 项相关后端单测通过，5 项独立合成库真实集成通过，覆盖回退后再提交、旧待办消失、照片保留、审计失败整笔回滚、已整改后驳回阻止、管理员/模块限制，以及整改与回退并发不能同时提交成功。Web `npm run check` 通过（196 项测试、lint、类型、构建）；浏览器与本地运行结果在交接文档记录。未对真实周检执行回退，无生产发布或 Git 提交。

本次只改后端/Web 和文档，没有 SQL、依赖、配置或小程序改动。0.1.12 小程序可沿用既有草稿接口继续编辑；1504 服务器更新包不包含本功能，后续正式更新需要重新封存包含本次代码的 JAR/Web。

更新时间：2026-09-16。

本文档依据当前 Web、小程序、后端、数据库结构和正式项目文档整理。正式模块已定义为“质量周检”：
先按项目和自然周整理共享草稿，再整批生成独立质量问题，之后分别完成“整改 → 复查 → 关闭/退回”。
历史独立问题不迁移、不删除，继续兼容处理；不另建与电箱日检重复的通用巡检系统。

## 2026-09-16 小程序整改人选择搜索

原滚轮选择改为共享 `QualityAssigneeSheet`：周检编辑和问题改派均支持部分姓名、账号即时搜索；显示账号以区分同名、当前选中标记、候选/匹配人数、清空及空结果提示。按用户 ID 选择，草稿按稳定问题键定位；取消、返回及页面隐藏保留原负责人，仍由原保存/提交操作写入业务数据。

候选继续来自质量专用接口，在客户端仅筛选已返回人员，不增加请求或权限；后端保存/提交时的资格校验保持。本次仅小程序和文档改动，之前后端/Web 服务器更新包无需重打。

验证：类型检查通过；原 H5 开发页面使用 85 名合成人员完成 8 组浏览器专项，覆盖姓名/账号搜索、同名区分、独立滚动、无结果、清空、取消保留、多问题负责人隔离及改派实际请求 ID；没有访问或改写真实业务数据。H5/正式微信构建、正式包检查与访客样式检查通过；两个微信页面均正确引用共享搜索组件。微信真机键盘、上传和发布尚未验收。

0.1.12（112，49 页）候选包已交付到 `/Users/js/Downloads/Dianxinyun-WeChat-0.1.12-search/`，可直接导入其中 `mp-weixin` 目录。归档为 `Dianxinyun-mini-0.1.12-prod-20260916-154651.tar.gz`，同目录有校验文件和使用说明；393 个产物文件逐一校验通过。构建自不可变源码快照 `Dianxinyun-source-20260916-154439-090c64b9721d.tar.gz`，源码清单 SHA-256 为 `9975a7c3f06721fe3e15a9de25b13ab7be173df627b78e3197f51b347f0417a2`。旧包保留，未自动上传发布、提交或推送 Git。

## 问题清单

| 编号 | 优先级 | 问题 | 验收标准 | 状态 |
| --- | --- | --- | --- | --- |
| Q-01 | P1 | 整改人候选接口权限不匹配，可能选到停用、无项目访问或无整改权限的人员 | 使用质量专用候选接口；前后端统一校验启用账号、ACTIVE 项目访问、质量模块及 `quality.view/quality.rectify`；未闭环任务负责人不可直接失效 | 已修复 |
| Q-02 | P1 | 整改、复查、改派缺少并发保护，创建重试可能重复 | 状态迁移使用原状态条件更新并在冲突时返回 HTTP 409；创建支持持久化幂等键；两端提交均有忙碌短路 | 已修复 |
| Q-03 | P1 | 通用文件接口存在目标项目鉴权和最终附件注入风险 | 质量文件按真实项目和阶段权限校验；禁止直接上传或通用维护最终质量附件；混合列表逐条过滤 | 已修复 |
| Q-04 | P1 | 无质量待办、通知和待复查逾期规则，任务可能长期停滞 | 创建、改派、整改、退回产生非阻断通知；质量待办进入现有四类待办；整改期限按整体闭环期限计算，待复查也能逾期 | 已修复 |
| Q-05 | P1 | Web 项目/筛选请求竞态，加载、搜索和资料请求耦合 | 旧请求不能覆盖新项目；加载和错误可见；搜索清空恢复全量；资料按需独立加载 | 已修复 |
| Q-06 | P1 | 小程序项目/筛选请求竞态，入口只判断本地当前项目 | 入口判断任一有效项目；自动定位有权项目；切换时清旧数据并校验响应上下文 | 已修复 |
| Q-07 | P1 | 小程序没有改派和期限调整，复查通过缺少安全确认 | 小程序可调用现有改派接口；负责人显式选择；可调整整体闭环期限；关闭前二次确认 | 已修复 |
| Q-08 | P2 | 质量资料有效/归档边界不清，小程序多格式打开不稳定 | 小程序只读展示 ACTIVE；Web 可区分有效与历史并恢复；PDF/Office 显式传文件类型；加载错误独立 | 已修复 |
| Q-09 | P1 | 多轮证据混合展示，附件失败被静默忽略 | 按创建、每轮整改、每次复查分组；显示失败数量；必要证据未完整加载时禁止静默通过 | 已修复 |
| Q-10 | P2 | 排序、统计语义、空状态和大列表性能不准确 | 未闭环/逾期/严重等级优先；“今日新增问题”语义准确；无数据闭环率不显示 100%；列表分页；搜索空态独立 | 已修复 |
| Q-11 | P2 | 缺少误建作废、严格字段校验及暂存附件回收 | 管理员可作废并留痕；非法状态/等级和超长字段明确拒绝；过期未绑定质量暂存附件自动清理；关闭后保持只读 | 已修复 |
| Q-12 | P1 | 自动化测试不足，无法证明完整闭环 | 覆盖创建、整改、退回、再次整改、关闭、改派、作废、权限隔离、附件、并发和统计；通过后端全测及三端构建 | 已修复 |
| Q-13 | P1 | 质量细分权限又叠加固定角色白名单，普通项目角色即使拥有 `quality.manage` 仍不能发起问题 | 所有动作统一校验目标项目 `ACTIVE`、`QUALITY` 模块和细分权限；发起/管理用 `quality.manage`，整改用 `quality.rectify` 且满足本人被指派或同时有 `quality.manage`，复查独立使用 `quality.review`；Web、小程序与后端结果一致 | 已修复 |
| Q-14 | P1 | 单问题创建不能表达一次周检发现多个问题，也无法统一留档 | 每项目每自然周唯一周检，支持 0–50 个问题整批原子提交；旧单问题 POST 返回 HTTP 410 | 已修复 |
| Q-15 | P1 | Web 与小程序整理过程不能共享，容易覆盖同事内容 | 草稿服务端长期保存，项目 `quality.manage` 人员协作；整体保存使用 `expectedVersion`，冲突 409 且客户端保留本地内容 | 已修复 |
| Q-16 | P1 | 批量问题的照片、负责人和期限可能串项 | 每题稳定键、顺序、1–20 张整改前照片、独立整改人与期限；正式提交后按问题分别迁移附件和流转 | 已修复 |
| Q-17 | P2 | 多轮整改照片无法直接做最新前后对比 | 详情固定展示原始问题照片与最新一轮整改照片，数量不要求相同；全部历史轮次继续保留在时间线 | 已修复 |
| Q-18 | P1 | 草稿照片可能被 24 小时暂存任务误清理，放弃时又可能遗留物理文件 | 已绑定草稿使用专用业务类型长期保留；移除/放弃在事务提交后物理清理，失败进入可重试状态 | 已修复 |
| Q-19 | P1 | 质量问题缺少稳定业务日期和月度含图汇总，按创建/整改时间统计会漂移且人工汇总易漏图 | 新增不可变 `record_date`、最长 366 天日期筛选和异步两 Sheet Excel；问题明细 A:T 20 列中 S/T 分别直接放整改前/最新整改后照片，多张照片各自在列内按原顺序逐行，A:R 同一问题信息纵向合并；图片紧凑缩略图展示但完整原图嵌入工作簿，可在 WPS/Excel 选中后用图片工具放大查看，失败不静默漏图 | 已修复（本地迁移已应用，生产待受控升级） |

## 已确认的正式业务口径

- 质量模块管理的是每周质量检查留档及其问题整改闭环，页面正式名称为“质量周检”。
- 自然周按 Asia/Shanghai 周一至周日计算。允许当前周及往期补录、禁止未来周；逾期提交显示“补录”并保留实际提交时间。
- 草稿允许字段暂不完整；正式提交前一次校验全部问题。任一问题失败整批回滚，已提交重复调用幂等返回。
- 零问题周检必须填写不超过 1000 字结论并上传 1–20 张现场照片；存在问题时现场照片可选。
- 新问题只能从周检产生。旧 `POST /api/v1/quality/issues` 返回 HTTP `410`，历史独立问题仍可查询、整改、复查、改派和作废。
- 周检提交后通常只读；系统管理员可按本文误提交规则退回草稿。提交状态保存 `submitted_issue_count` 快照，个别或全部问题被管理员删除后仍保留周检留档，管理员可按 2026-09-17 新规则仅恢复现存内容。
- `deadline` 统一解释为问题整体闭环期限，待整改和待复查均纳入逾期判断。
- 质量资料由目标项目具备 `quality.manage` 的人员在 PC 端维护，小程序只读查看当前项目的有效资料。
- 工程资料、质量资料和质量问题附件继续保持独立，不自动合并。
- `record_date` 是按日台账和月度汇总的稳定日期；周检问题取检查日期，历史独立问题取原创建日期。
  Web 默认仍查询全部时间，只有成对选择日期时才按最长 366 天闭区间过滤。含图 Excel 复用
  `quality.view`；新文件只保留“按日汇总 / 问题明细”，每条问题后连续展示其整改前与最新整改后
  原图，成功文件保留七天，小程序不增加导出入口。长期本地库已应用质量按日导出迁移并达到 94 表，
  生产库仍待受控升级。
- 普通项目角色可通过细分权限参与质量闭环，不再附加
  `PROJECT_ADMIN / SAFETY_ADMIN` 固定角色白名单。周检草稿创建/保存/提交/放弃、改派、期限调整、作废和质量
  资料管理使用 `quality.manage`；整改必须具备 `quality.rectify` 且为本人被指派或同时
  具有 `quality.manage`；复查使用独立的 `quality.review`。
- 当前质量闭环允许同一位分别具备 `quality.manage`、`quality.rectify` 和
  `quality.review` 的人员代办发起、整改或复查，不强制“发起人、整改人、复查人三者
  分离”；这是为兼容单质量管理员项目而保留的明确业务边界。

## 回归要求

- 后端：`cd backend && mvn test`
- Web：`cd frontend && npm run check && npm run audit`
- 小程序：在 `wechat-miniprogram/site-platform-miniprogram` 执行 `npm run type-check`、`npm run build:h5`、`npm run build:mp-weixin`
- 最终检查：`git diff --check`

## 2026-07-30 回归结果

- 后端全量测试：228 项通过，0 失败、0 错误。
- Web 生产构建：通过，116 个模块完成转换。
- 小程序类型检查、H5 构建、微信小程序构建：全部通过。
- 本地 `dianxinyun` 已在完整备份后执行 `20260730_quality_issue_resilience.sql`，并完成重复执行检查；`request_key`、`version` 和唯一索引均存在。
- 代码差异检查：`git diff --check` 通过。
- 微信真机上的原生文档打开、相机/相册生命周期和窄屏视觉仍属于上线前设备验收，不以编译结果替代。
