# 2026-09-17 最新功能更新包

## 正式执行结果

用户提供服务器终端截图，已显示 `4/4 更新完成`，数据纠错增量、新后端、Web 页面及资源核验通过。随后正式 Web 页面及操作日志读取正常；该 1330 包已正式切换。微信上传/审核/发布仍未取得完成证据。后续“周检问题已删除仍允许回退”属于另一个补丁，不包含在本包内。


本次按用户明确指定的 9 月 15 日正式版准备更新，包含批量项目权限、用户管理界面、质量周检退回、管理员数据纠错及最新周检入口文案。小程序版本 0.1.13（113），构建编号 `0.1.13-20260917-quality-correction`，保留此前隐私自主同意、转发与整改人搜索。

服务器保持 `site-platform.service`、`/opt/site-platform/backend/site-platform.jar` 和 `/opt/site-platform/frontend`；安装器停服前核验原正式 JAR/Web 摘要、应用使用的本机数据库和必要表结构。代码、SQL 和部署工具均来自同一不可变源码快照，旧包保留。

本次唯一增量为 `20260916_administrator_data_correction.sql`：新增纠错日志、附件引用两张表和对应菜单权限，不改写原业务记录或附件，不调整原角色授权。已应用时校验后跳过；不执行历史迁移、全库导入、额外全库备份或隔离恢复。现有转换器、依赖与服务配置保持，8MiB 纠错上传分片继续适配原 50MiB 通用代理限制。

用户自行上传服务器包及同名 `.sha256`，在对应上传目录校验、解压并执行包内 `run.sh`。独立任务完成后显示 `4/4 更新完成`，`UPDATE-RESULT.json` 为 `COMPLETE` 才表示新版本已切换。具体文件名与复制即用命令见最终交付目录的《更新说明.md》。

脚本只准备新文件、停止应用、应用这一项必要增量、切换 JAR/Web、启动并核验后端及 Nginx 返回的新资源。旧静态资源保留，失败恢复原代码；新增表和已有纠错记录保留，不以删表回滚。恢复失败保留维护标记及日志供定位，不从头运行历史安装流程。

小程序以正式 HTTPS API、合法域名校验和关闭 mock 构建，导入独立 0.1.13 发布目录。按用户要求仅打开上传窗口并填写版本说明，上传由用户完成；服务器实际切换、微信上传/提审/发布分别记录，不将构建成功当作上线。

复用既有业务验证：数据纠错 30 项合成真实集成、周检退回 5 项合成集成、Web 196 项及两端最新入口验证。只对本次新增部署编排执行相关离线测试，并核验本次正式产物的源码、版本、接口地址和归档完整性。最新打包及交付结果见 [开发状态](开发状态.md)。

## 最终交付

服务器归档：`/Users/js/Downloads/Dianxinyun-update-20260917-1330/Dianxinyun-update-20260917-1330.tar.gz`，162682874 字节，SHA-256 `51cd0df44171e7f9e094de2b4e2ac49a1ea5ec7d541f4f5ac2010c3ea77290e7`，同名校验文件一并上传。三个产物的源码清单 SHA-256 均为 `5065e3c7a2431a0bd88a5dcdb4ce3c2eea062d560df0fc9de5301039b77f967a`。

将包与校验文件上传到服务器 `/root/releases/20260917-1330/` 后执行：

```bash
cd /root/releases/20260917-1330
sha256sum -c Dianxinyun-update-20260917-1330.tar.gz.sha256 && tar -xzf Dianxinyun-update-20260917-1330.tar.gz && bash Dianxinyun-update-20260917-1330/run.sh
```

小程序目录 `/Users/js/Downloads/Dianxinyun-WeChat-0.1.13-quality/mp-weixin`；397 个产物文件、49 页及正式配置检查通过。已运行至上传前的覆盖体验版确认弹窗，用户继续上传并填写版本 `0.1.13`。服务器正式执行、微信上传/审核/发布尚未确认；本次完成构建交付，不代表上线。
