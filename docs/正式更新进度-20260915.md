# 正式更新进度（2026-09-15）

## 2026-09-15 正式新版已切换（最新状态）

用户最新服务器截图确认：以 `--apply-no-new-backup` 执行的正式切换已从 1/5 至 5/5 全部完成，正式数据库已迁移到 114 表，新 JAR 与 final Web 已切换，本机及公网接口健康、API 文档关闭；独立发布任务最后显示 `Deactivated successfully`。这是用户在正式服务器执行后的反馈，已超出此前仅封包、演练和预检的阶段。

北京时间 13:28，助手从本机只读核验正式 `https://zhihuiyz.xyz`：首页 HTTP 200，并与已核对归档摘要的冻结 final Web 首页逐字节一致（首页 SHA-256 `b59f46a4d03b4518522fc34c07cb1f16ab4e82d89724b258e2d21fbb626a9558`）；正式验证码接口 HTTP 200、业务 code 200。未使用业务账号或代用户提交正式数据。

按用户最新明确要求，本次跳过新备份，并记录 `NEW_BACKUP_SKIPPED_USER_REQUEST`；此前已完成的预备备份保留，不能把它记成本次数据库与附件的完整同点备份。现场状态保存在 `/root/releases/20260915-0912/formal-switch`。

用户在新版上线及正式页面刷新提示后反馈“没有异常了”，本次新版更新与用户使用确认完成；已将该反馈记录为总体使用确认。用户已反馈小程序更新至最新版，但本记录不推定具体微信版本、审核时间或相机真机验收结果。未提交 Git。以下保留此前经过，涉及“待执行/未切换”的描述均为当时状态，以本节为当前结论。

## 13:20 用户要求加速切换

用户最新明确要求只完成新版迁移切换，不再新增备份或重复演练。按本次明确授权保留既有备份，新增快速执行参数跳过完整同点备份及上传目录备份遍历；继续保留固定 12 项增量 SQL、旧行摘要核验、产物一致性和新版启动健康核验。不得把跳过的新备份写成已完成。

用户截图确认重启后已启动正式切换任务，但在 Nginx 预检以“未发现可复用的图纸分片代理”停止，未进入停服/迁移。随后实际 nginx -T 输出显示 `/etc/nginx/sites-enabled/site-platform` 使用通用 `location /api/v1/`，上游 `http://127.0.0.1:8080/api/v1/`，server 上传限制 50m，代理读写超时 120s。修复只给安委会与会议分片复制现有通用代理体并设置专用参数，不改全站 50m 或普通 API 语义。正式修复和迁移结果仍待用户执行反馈。

## 13:30 前新版切换：执行包已准备，服务器待反馈

用户明确目标是 13:30 前新版切换可用，不能把保留旧版作为交付。用户回答微信公众平台版本问题为“已经更新到最新版本了”，据此选择本次冻结 final Web，开放会议创建；没有擅自填写具体微信版本号或平台审核时间。修正版重启是否完成仍待用户反馈，旧 Bash 的“系统软件更新仍在运行”提示只证明旧进程名检查停止，不能当作已重启。

新增本次专用执行工具 `scripts/release-20260915/`，复用冻结 12 项迁移计划及旧行摘要校验，不改已上传业务包。正式停服前核验基线/报告/产物及 Nginx；同点备份后补导入密钥、转换器及 unit Docker 组，复用图纸分片代理体给安委会和会议新增 10MiB 路由；单次正式迁移到 114 表/37 标记，切换新 JAR 与 final Web。当前 InvocationID 启动日志、本机/公网接口、API 文档关闭以及公网首页/JS 内容摘要通过后才恢复自启和解锁。独立 systemd unit 防止 Workbench 断开中止迁移；失败保留维护和现场，不自动回滚或仅换回旧 JAR。单独保留适配 98/25 同点备份的人工恢复工具。

33 项离线测试通过，覆盖数据库身份/文件篡改/数据保护/部分迁移停止，冻结 Web 归档和配置密钥，Nginx 转换/并发变更/动态模块 include，完整编排成功、迁移失败及预检失败。Shell 语法通过，归档逐文件回读一致。未在服务器执行此包，不能记作正式上线。

交付：`/Users/js/Downloads/Dianxinyun-update-20260915-0912/Dianxinyun-switch-20260915.tar.gz`，48011 字节，SHA-256 `5db9c9efd83bbe4e5e67e59cdd03fe264f027c7c8ceec71c054d75b9f1b4f356`，同目录 `.sha256`。上传原 `/root/releases/20260915-0912/` 后解压执行 `Dianxinyun-switch-20260915/run.sh`。任务 `dxy-formal-switch-20260915.service`，状态 `/root/releases/20260915-0912/formal-switch`，完整同点备份目标 `/root/backups/dianxinyun/release-20260915-0912`。原服务仍未因本地封包切换；不提交 Git。

## 用户反馈已确认

- 用户新增时限：2026-09-15 13:30 前需要使用系统；压缩后续交互，已通过的演练和运行验证不重复。北京时间约 12:38 从本机只读检查正式首页 HTTP 200、验证码 HTTP/业务 code 均 200；仅证明公开入口健康，不代表新版业务验收。
- 用户已执行重启前检查，截图末尾为“停止：系统软件更新仍在运行”，并未执行到 `systemctl reboot`。前序维护标记/服务运行/自启检查已通过；原 `pgrep` 规则可能将常驻 `unattended-upgrade-shutdown` 误判为安装进程，尚未取得服务器具体进程证据。下一步粘贴命令改为检查 APT/dpkg 的 POSIX 文件锁并保持到提交正常重启，不删除锁、不杀更新进程。跨进程占锁拒绝/释放后通过已在本机验证，服务器重启结果仍待反馈。
- 上传目录：`/root/releases/20260915-0912/`；外层 `Dianxinyun-update-20260915-0912.tar.gz` 的 SHA-256 校验返回 OK。
- 解压目录：`/root/releases/20260915-0912/Dianxinyun-update-20260915-0912/`；包内 `SHA256SUMS` 全部通过。
- 预检报告：`/root/releases/20260915-0912/preflight-20260915.txt`，生成时间 `2026-09-15T01:33:15Z`。
- x86_64；`site-platform.service` 为 loaded / active / running，工作目录 `/opt/site-platform`，unit 文件为 `/etc/systemd/system/site-platform.service`。
- JAR 入口指向 `/opt/site-platform/releases/20260901-093500/backend/site-platform.jar`；Web 入口指向 `/opt/site-platform/releases/20260901-093500/web-final/dist`。
- uploads 入口指向 `/opt/site-platform/releases/20260810-142355-full-local/uploads`。目录名只是实际部署路径，不据此判断数据环境。
- 本机验证码业务 code=200；MySQL `8.0.46-0ubuntu0.22.04.4`，库 `dianxinyun`，98 张表。截图展示迁移标记到 `20260901_SITE_ACCESS_REENCRYPT_LEGACY_DEVELOPMENT_KEY_V1`；全部表/字段精确兼容核验尚未完成。
- Java 17.0.20；mysql、mysqldump、nginx、python3、sha256sum、unzip 可用，预检时 Docker 缺失；根盘约 79G，剩余约 67G。随后用户执行准备步骤，Docker/Buildx 已能运行，镜像构建停在从 Docker Hub 读取 Debian 清单超时。
- 实际 `/run/reboot-required` 已确认存在，包名输出为 libc6（两行）；`apt-config dump` 未匹配到 `Automatic-Reboot` 配置行，不将“未配置”夸大为已经完整排查所有重启来源。主服务 `is-enabled` 为 enabled，watchdog timer 为 loaded / active。正式维护前仍须按手册处理待重启状态、核验健康与启动保护；此时未执行重启。
- 用户已完成不停服数据库预备备份：`/root/backups/dianxinyun/precheck-20260915-093640/database.sql.gz`。非 InnoDB 表检查通过，mysqldump/gzip 正常退出，`gzip -t` 及同目录 `SHA256SUMS` 验证通过，`du -h` 显示 536K。备份已恢复到下面的隔离库；仍不是数据库/附件同点备份。
- 用户反馈正式库视图、触发器、存储程序和 MySQL 事件均为 0。
- 用户截图确认隔离恢复成功：`dxycheck20260915093640`，98 张表；状态目录 `/root/releases/20260915-0912/validation-093640`。使用仅限该库的 `dxycheck0915093640@localhost` 账号；恢复脚本先验证读取正式库返回 `ERROR 1142 SELECT command denied`，密码只在服务器生成并保留于私有 `client.cnf`。
- 用户最新截图确认脚本 SHA-256 返回 OK，12 项迁移两轮全部通过：114 表、37 个迁移标记，原 98 张表的旧字段记录保留，重复执行后的记录与表结构一致。结果目录 `/root/releases/20260915-0912/validation-093640/migration-rehearsal`；末尾明确“正式库未迁移；服务未停止或切换”。
- 转换环境准备截图：`registry-1.docker.io` 的 Debian `bookworm-slim` 清单请求 `i/o timeout`，Dockerfile 第 1 行失败，未构建出目标转换镜像。命令在开始构建前已核对主服务 active 且 InvocationID 未变；截图中的 needrestart “Services to be restarted” 是 list-only 模式清单，不作为已经重启的证据，失败后的服务健康仍需后续核对。
- 随后用户截图确认离线包外层与全部内层校验通过，`import.sh` 成功载入 `dianxinyun-material-preview:20260915-0912` 并设置兼容 `:local` 标签；受限非 root 容器中的 LibreOffice、FFmpeg、Poppler 和 CMap 检查通过，最后输出“离线预览环境导入成功，原系统健康检查通过”。该脚本同时核对主服务 active、InvocationID 未变与验证码业务 code=200。服务器镜像 ID 保存在 `/root/releases/20260915-0912/converter-image-id.txt`。这确认镜像可运行，尚不代表已接入新后端或完成正式附件转换验收。
- 最新服务信息截图：MainPID 仍为 `245431`；`User/Group=site-platform`，`EnvironmentFiles=/etc/site-platform/site-platform.env`，`WorkingDirectory=/opt/site-platform`。`SupplementaryGroups` 未显式设置（不据此断言账号的完整组成员关系），`ReadWritePaths=/opt/site-platform/uploads`、`ProtectSystem=strict`、`PrivateTmp=yes`；Docker socket 为 `0660 root:docker`。内存总计 3.4 GiB、可用约 1.2 GiB、Swap 为 0。冻结代码确认预览工作目录位于上传根之下，不能误认为它使用宿主无法挂载的私有 `/tmp`。

## 下一步与边界

已完成的 `scripts/rehearse-release-20260915.py` 绑定上传候选包的源码摘要及 12 份 SQL 摘要，固定隔离库/账号/私有连接配置；精确检查 98 表和 25 个原标记，每个连接再次检查当前库和账号，无正式库运行参数或 root 连接回退。两轮逐项执行后为 114 表、37 个标记；对原 98 张表的全部旧字段逐行计算摘要，业务表记录与角色授权必须原样保留，菜单/权限/迁移目录只允许明确的新增集合，校验新字段默认值及索引、项目五模块初值。第二轮比较全部已存储记录和表结构；`INSERT IGNORE` 可能消耗的自增计数单独排除，原始 SHOW CREATE 日志仍保留。

演练资料写入 `/root/releases/20260915-0912/validation-093640/migration-rehearsal`，拒绝覆盖和自动重试。只允许已审阅的重复建表/重复键与特定弃用提示，其他 SQL 错误或警告停止。所有日志、行摘要和结果均保留于服务器私有目录，不输出凭据或业务值。脚本离线 9 项测试通过，覆盖错库/越权拒绝、SQL 篡改、原记录丢失/变化/多余新增、目录旧行保持、警告分类和每连接守卫；随后用户已确认服务器副本实跑通过。此脚本是独立补充文件，没有重建或更改原更新包。

本次副本已验证顺序：会议签到（100 表）、访客复用（103）、访客自动填写（103）、会议资料（106）、用印附件选填（106）、用印合并导出（108）、安委会（111）、项目模块（112）、用户导入（114）、附件旋转（114）、待办入口（114）、导入手动密码（114）。正式库尚未执行。

附件转换依赖准备使用 `scripts/prepare-release-converter-20260915.sh` 对应命令：Ubuntu 软件库安装 `docker.io/docker-buildx`，模拟安装如涉及现有软件升级或删除则停止；needrestart 使用 list-only 模式。Docker/Buildx 安装已完成，构建因上面的网络超时失败。包选择依据 [Ubuntu Docker](https://packages.ubuntu.com/jammy-updates/docker.io)、[Buildx](https://packages.ubuntu.com/jammy-updates/docker-buildx) 与 [needrestart 配置](https://github.com/liske/needrestart/blob/master/ex/needrestart.conf)。

已改为本机构建可上传的 Linux amd64 离线镜像。本机 Docker Desktop 已启动；直接访问 Docker Hub 也超时，经 Docker 官方 AWS 镜像源拉取 `public.ecr.aws/docker/library/debian:bookworm-slim`，使用本机已配置代理读取 Docker Hub 官方清单，确认两者摘要同为 `sha256:88200866dfff7ea7f5cbcb6ec7c8a701889efe6fe859fe64d6990e4b07ea4171`。构建 Dockerfile 与用户已上传包逐字一致（SHA-256 `59c1524274472508beca521f3ea047b3011220860825a08cc3c9d62b24e5a5ed`）；工作目录 `/Users/js/Downloads/Dianxinyun-preview-runtime-20260915`。官方镜像源依据 [Docker 官方说明](https://www.docker.com/blog/news-from-aws-reinvent-docker-official-images-on-amazon-ecr-public/)。

本机离线镜像已完成：标签 `dianxinyun-material-preview:20260915-0912`，Linux amd64 清单 ID `sha256:cb53c9752afda7c304990c00844fa23a6ea9dd3e5b9c38718f2013bbbc922921`。`scripts/verify-material-runtime-offline.py` 在 Mac 的 Docker amd64 仿真中执行原冻结脚本的 Linux 分支，只在测试包装中选择该分支和版本镜像标签，不改原脚本。14 项合成测试通过：PNG/JPG 四角度缩略图、视频封面/90° H.264 转码/转码后解码、非嵌入字体中文 PDF 缩略图、中文 DOCX 转 PDF 和缩略图；原件摘要保持，中文两种缩略图人工查看正常。另以 UID/GID 65534 在禁网、只读根、1GiB/1CPU/128 PID 限制下验证三个工具及 CMap 目录可用。

已由用户上传并导入的独立离线包：`/Users/js/Downloads/Dianxinyun-update-20260915-0912/Dianxinyun-preview-offline-20260915.tar.gz`（427365338 字节），外层 SHA-256 `4065232736b214edbf32f4c0024191dff389600a2599df41cae2f0ef9972dd6d`，同目录有 `.tar.gz.sha256`。包内含 `runtime-image.tar`、`import.sh`、源码/运行清单、14 项验证报告和内层校验文件；镜像逐 blob SHA-256、解压层 diff_id、唯一标签、Linux amd64 配置以及本机 `docker load` 回读均通过，外层归档逐文件与 gzip 校验通过。导入脚本来源 `scripts/import-offline-converter-20260915.sh`，Bash 语法检查通过；只连服务器本机 Docker，拒绝覆盖不同的已有 `:local` 镜像。服务器导入、受限工具运行及原系统健康已由最新截图确认；实际应用转换和正式发布仍待后续验证。

下述 r2 已通过当前 JAR 副本启动及服务账号六项合成附件转换。后续先按手册处理服务器已有的 libc6 待重启状态，确认原系统恢复，再完成正式配置、数据库/附件/JAR/Web/配置同点备份、12 项正式迁移、代码切换和业务验收。当前正式库 98 表不能使用旧发布工具硬编码的 68 表起点；不得按文件名批量执行包内 55 份 SQL。参见 [更新包准备与分步操作](更新包准备与分步操作.md)。

## 副本运行验证：r2 已由用户执行并通过

最新截图确认 `validate-release-runtime-20260915-r2.py` 校验 OK，三阶段全部通过：114 表副本、独立 Redis、正式 prod 配置门禁；新 JAR 完整启动，数据库/Redis 及 API 文档关闭检查通过；停止副本释放内存后，以正式服务账号、PrivateTmp 和只读系统限制完成六项合成附件转换。脚本完成路径清理验证进程及临时凭据，并核验原服务和验证码健康；结果目录 `/root/releases/20260915-0912/validation-093640/runtime-validation-r2`。保留两次运行现场，不重复恢复、迁移演练或运行验证。

输出明确正式服务、正式数据库及配置均未切换，正式批量导入密钥与正式转换器入口均“待配置”。这确认目标服务器上的副本兼容与合成转换，不等于正式附件业务、微信真机或新版上线验收。下一步提供带维护状态、服务自启及软件更新检查的重启命令，由用户执行后反馈；目前尚无重启结果。

以下保留初版故障和 r2 修复经过，以便追溯。

新增 `scripts/validate-release-runtime-20260915.py`，交付于 `/Users/js/Downloads/Dianxinyun-update-20260915-0912/` 下同名文件与 `.sha256`。脚本 24835 字节，SHA-256 为 `d1c1f3f4219b2f06bfd5d2b5585ed2a83b87e741221e1139ba302901669f049a`。仍由用户上传到 `/root/releases/20260915-0912/` 并执行；没有代用户运行服务器命令。

脚本固定绑定已通过演练的 `dxycheck20260915093640`、独立账号和 114 表/37 标记，先通过 TCP/TLS 再验证读取正式库被拒绝；JAR 与转换器摘要必须匹配原上传包。读取当前进程的必要配置但不输出值，临时凭据通过 systemd `LoadCredential` 交接，管理员重置、外部配置与一次性重加密参数不继承。新 JAR 保持 `prod` 和原安全门禁，只在隔离验证中关闭调度；使用独立 Redis 端口 16385、后端回环端口 18085 与独立空上传目录，禁止访问正式部署目录及外部网络。

可用内存少于 1 GiB 拒绝启动；副本 JAR/Redis 分别限制 704/64 MiB。副本达到 Spring ACCEPTING_TRAFFIC、Hikari 与验证码健康，并核验文档入口 404 后先停止、释放内存，再启动临时转换 unit。该 unit 复用 `site-platform`、严格只读系统和 PrivateTmp，仅临时补充 docker 组，使用合成照片、视频和中文 DOCX/PDF 验证 6 项转换。脚本不修改正式 unit、账号组、配置、数据库或入口链接；结束清理验证进程和临时凭据，保留私有结果目录 `/root/releases/20260915-0912/validation-093640/runtime-validation` 与 `/var/lib/dxy-runtime-check-20260915-0912/work` 中的合成文件。已存在验证目录或 unit 时拒绝重跑。

本机 9 项离线保护测试通过，覆盖错库/非本机/无 TLS/重复连接参数、重置参数不继承、正式 prod 与调度隔离、旧报告拒绝、命令行不携带密钥、已有 unit 拒绝和原进程变化。脚本中的 6 项媒体验证也已用本机 amd64 Docker 实跑通过（仅测试包装选择 Linux 分支、版本标签和本机 Docker socket）；新 JAR/转换器本地 SHA 与脚本相符。服务器 systemd 与新 JAR 启动尚未运行，不能记为上线或正式附件业务验收。

用户随后截图反馈脚本校验 OK，输出 `1/3 配置与隔离通过；使用 114 表副本、独立 Redis、正式 prod 门禁` 后以“未完成：独立 Redis 未就绪”退出。该进度证明副本 TCP/TLS、114 表/37 标记与正式库读取拒绝通过，不代表 Redis 已成功启动；新 JAR 和媒体验证尚未进入。失败路径会尝试停止验证 unit 并删除临时凭据；实际退出状态、Redis 日志及原服务失败后健康待只读核对。已生成的 `runtime-validation` 和 `/var/lib/dxy-runtime-check-20260915-0912` 必须保留，不直接重跑、删除或覆盖；下一步先读取该独立 unit 的 journal 与脱敏 `redis.log`，依据实际错误修正验证脚本，不调整正式 Redis。

最新日志截图定位到 `Changing to the requested working directory failed: Permission denied`、`status=200/CHDIR`，Python/Redis 尚未真正执行；正式服务仍为 `MainPID=245431`、`active/running`。补充脚本的 `os.umask(0o077)` 将 `ROOT.mkdir(mode=0o750)` 实际创建为 0700；后续仅 chown 为 root:site-platform，并未恢复组执行位，服务账号无法穿过根验证目录。该错误归属补充验证脚本，无需修改正式 Redis。

已修复目录创建函数：chown 后显式 `chmod(0750)`，内部工作目录仍为服务账号 0700，启动前用 `runuser -u site-platform -g site-platform` 实际检查进入目录与写入资格。修正版使用全新的 `runtime-validation-r2`、`/var/lib/dxy-runtime-check-20260915-0912-r2` 与 r2 unit/container 前缀，保留第一次失败现场，不重新迁移或恢复数据库。11 项离线测试通过，新增 umask 077 下目录权限和访问探针失败回归；在本机 Linux amd64 容器中以 root 创建目录、切换非 root 用户实际复现 0700 拒绝和 0750 通过。此次未重复无关的媒体转换测试，服务器副本完整启动仍待用户执行确认。

r2 交付文件为原 Downloads 更新包目录下 `validate-release-runtime-20260915-r2.py`（25299 字节）及同名 `.sha256`，SHA-256 `e872bd3f68d34b40fcf705f840036919c7def513781bcff3693bd431620195bb`。旧脚本、旧校验和及原更新包均保留。用户上传 r2 两文件到 `/root/releases/20260915-0912/` 后运行新文件；结果目录改为 `/root/releases/20260915-0912/validation-093640/runtime-validation-r2`。
