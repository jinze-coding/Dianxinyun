# 2026-09-16 后端与 Web 更新

> 后续变更说明：本页 1504 归档仍是当时已核验的更新包，不包含之后新增的“管理员将质量周检退回草稿”。该功能当前已交付本地，正式发布前须重新封存包含它的后端/Web 包；小程序和数据库无需随之更新。

## 本次内容

- 修复同项目追加安委会等角色时，误把直接指派的用印审批配置提示为撤权。
- 用户管理支持筛选、跨页选择最多 200 人，批量增加/取消/重设角色，或加入另一个项目并选择是否保留原项目。保存前逐人核对，有责任解除时须确认，整批事务保存。
- 批量操作使用两个入口及实际项目、角色、人数的结果说明。
- 用户列表标题、筛选、表头、底部分页固定，数据区滚动；每页可选 10/20/50/100 条，显示本页实际条数；突出「返回业务工作台」。
- 上线复查补齐审计日志按完整字符分段，避免罕见汉字或表情被截断。

## 检查结论与范围

已逐文件对照 9 月 15 日正式包的冻结源码，后端资源/SQL、配置、依赖和转换器均未变化；新增功能只涉及后端与 Web 代码。本包不执行数据库迁移、不导入数据、不复制本地业务数据，不更改服务器配置或附件，不包含微信小程序更新。

复用已通过的 108 项后端相关单测、9 项独立合成库集成测试、196 项 Web 测试及 lint/类型/浏览器验证。审计修复后重新通过 16 项批量权限单测（含新增 1 项，累计相关单测 109 项）；部署脚本 8 项离线验证通过，涵盖切换、部分失败、启动失败恢复、包篡改、旧页面资源保留。最终包从同一冻结源码构建后端和 final Web，并校验内嵌源码标识及归档逐文件摘要。

正式目标沿用 `site-platform.service`、`/opt/site-platform/backend/site-platform.jar` 与 `/opt/site-platform/frontend`。脚本只接受 9 月 15 日已确认正式 JAR 与 final Web 的摘要，发现目标不同会在停服前停止。配置、数据库、附件均使用服务器现有内容。

## 上传与执行

将 `Dianxinyun-update-20260916-1504.tar.gz` 和同名 `.sha256` 上传到服务器 `/root/releases/20260916-1504/`。在服务器 root 终端执行：

```bash
cd /root/releases/20260916-1504
sha256sum -c Dianxinyun-update-20260916-1504.tar.gz.sha256 && tar -xzf Dianxinyun-update-20260916-1504.tar.gz && bash Dianxinyun-update-20260916-1504/run.sh
```

更新任务会独立运行，终端等待后显示结果；连接断开不会中止服务器任务。脚本先校验目标和包、准备新版，再切换两个代码入口、重启应用，核对后端健康和 Nginx 返回的新首页、JS、CSS。原代码目录保留，新版启动或页面核验失败会恢复原代码；整个过程不重启数据库或服务器。

成功以 `3/3 更新完成` 和 `UPDATE-RESULT.json` 的 `COMPLETE` 为准。刷新浏览器后进入「系统管理 → 用户管理」查看新界面。上传或解压不等于正式更新完成。

如中途断开，查看同一任务：

```bash
journalctl -u dxy-code-update-20260916-1504.service -n 40 --no-pager -o cat
```

若日志为 `ROLLED_BACK`，表示已恢复旧版，本次新版没有上线；若为 `RECOVERY_REQUIRED`，保留现场并提供该任务日志，不重跑历史迁移脚本。

## 交付状态

更新包已封存于 `/Users/js/Downloads/Dianxinyun-update-20260916-1504/`，文件大小 162468054 字节，SHA-256 `455d7226d2fa2e54c777175c3d4475516a777a482ef26347b61bb538636156a4`。源码清单为 `570a954ba79020381f25ac67c60ae9c40bfc9a151fca2cd654d7dd5e76031418`，后端与 Web 来源一致；封包回读逐文件通过。正式 Web 的浏览器专项已通过固定布局、条数/末页、空/错误状态、跨页选择及全部五种操作和责任确认。

当前仅完成本地检查、冻结源码和更新包，正式执行及用户使用确认待服务器反馈。未提交或推送 Git。
