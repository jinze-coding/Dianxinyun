# 访客与会议演示数据

更新日期：2026-09-10。按用户“增加测试数据我看看”建立独立本地演示环境。

## 查看

- 后台：http://127.0.0.1:3008/ ，选择“访客与会议演示项目”，进入“场内管理 → 预约邀请 → 月”。
- 当前主会议大屏：http://127.0.0.1:3008/?meetingScreen=1 。已登录会话可直接打开。
- 演示专用账号访问说明位于本机 `~/.local/state/dianxinyun-visitor-demo-20260910/访问说明.txt`。
  凭证仅在外部私有运行目录保存，不入 Git；普通后台登录仍进入个人待办。

## 数据

| 场景 | 数据 |
| --- | --- |
| 邀请列表 | 10 条：6 场会议、4 条单次预约；包含当前、未来、待填、已填、结束和作废 |
| 质量安全周例会 | 40 名预约人员；另有 5 名现场补录，人员台账总数 45 |
| 当前签到 | 已签 35 = 预约签到 30 + 现场补录 5；预约待签到 10 |
| 大屏 | 24 人/页，共两页，10 秒翻页、5 秒刷新 |
| 近期会议 | 今天、明天、第 4 天和第 7 天；已结束/已取消会议不进入门卫候选 |
| 门卫登记 | 2 组各 2 人：普通来访、同时报名两场未来会议；关联 2 条 |
| 同微信匹配 | 主示例身份同时有 1 条有效单次预约和 1 场当前会议登记，分别展示原始人数 |
| 本人资料 | 单次/门卫/代表性会议提交生成快捷资料；保留命名常用资料示例 |

单位、姓名、手机号、车辆、微信身份和预约数据均为合成测试信息。会议名称和数据带“演示”标记。
大批量预约名单由限定数据库的事务脚本导入并记录演示审计，个人资料与门卫多会议联动走业务接口，
签到和人员更正通过工作人员接口执行。本数据不是实际参会证据或真实微信扫码验收。

主会议本次开放至 2026-09-10 23:59，后续按真实时段显示结束状态。微信 mock 只在本地 Profile
启用，页面会明确显示不能生成真实微信签到码；真实微信扫码仍需获准的正式 AppID 联调环境。

## 隔离与复跑

- MySQL：`dianxinyun_visitor_demo_20260910`，101 表；全新空库基线后应用认证/RBAC 和场内增量。
- 后端：127.0.0.1:18080；Redis：127.0.0.1:16380；Web：127.0.0.1:3008。
- 运行配置、进程记录、上传目录及演示访问说明均在 `~/.local/state/dianxinyun-visitor-demo-20260910/`。
- 长期开发库 `dianxinyun` 保持 98 表，原 8080/3002 服务继续运行；本轮不升级或写入生产环境。

服务运行时，可重复导入并核验本演示日的数据：

```bash
DIANXINYUN_VISITOR_DEMO_CONFIG="$HOME/.local/state/dianxinyun-visitor-demo-20260910/config.json" \
DIANXINYUN_VISITOR_DEMO_CONFIRM=LOCAL_DEMO_ONLY \
node scripts/site-access-demo-data.mjs
```

脚本只接受 `dianxinyun_visitor_demo_` 加八位日期的数据库名和本机 18080 后端。项目名称、标记、
邀请标记和身份唯一键共同限定范围；重复执行复用现有记录，不清库、不覆盖真实记录，不关闭限流。
已验证重复执行后 10 条邀请、40 名预约、35 名签到、2 组门卫登记和 2 条联动关系不增长。
浏览器已核验本月列表、大屏统计和翻页；Node 语法检查与最终人数核验通过。
