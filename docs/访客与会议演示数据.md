# 访客与会议本地测试数据

更新日期：2026-09-10。按用户要求，直接在当前本地开发系统、当前项目中展示，不再使用独立演示环境。

## 查看

- Web：`http://localhost:3002/`，当前项目（本地项目 3）→ 场内管理 → 预约邀请。
- 2026-09-10 默认“日”列表有本次新增的 6 条示例；切到“月”可查看本次全部 10 条邀请。
- 主会议“演示 · 质量安全周例会”所在行提供“签到码”和“签到大屏”入口。
- 大屏直达：`http://localhost:3002/?meetingScreen=19`，沿用当前后台登录态。
- 门卫登记页可查看 2 组新增示例。原项目、账号、15 条原有邀请及门卫二维码保留。

## 数据

| 场景 | 本次新增数据 |
| --- | --- |
| 邀请 | 6 场会议、4 条单次预约，共 10 条 |
| 今日列表 | 当前会议、已结束会议、取消会议，以及待填、已填和作废的单次预约，共 6 条 |
| 主会议 | 40 名预约人员，另有 5 名现场补录，人员台账总数 45 |
| 签到 | 已签 35 = 预约签到 30 + 现场补录 5；预约待签到 10 |
| 大屏 | 每页 24 人，10 秒翻页，5 秒刷新 |
| 近期会议 | 今天、明天、第 4 天和第 7 天；结束/取消会议不进入门卫候选 |
| 门卫 | 普通来访和多会议来访各 1 组，每组 2 人；后者关联 2 场未来会议 |
| 快捷资料 | 10 份记住状态、1 份关闭状态；本人资料不包含同行人 |
| 微信匹配样例 | 同一合成身份具有当前单次预约和会议预约，归属摘要保持用途隔离 |

单位、姓名、手机号码、车辆和微信身份均为合成数据，带“演示”或固定本地测试标记。签到使用
`STAFF_MANUAL / MANUAL`，并明确写入测试审计，不代表真实扫码、定位或参会证据。真实微信
仍使用本地原有配置，没有启用 mock 或调整账号。合成微信身份不能由真实微信号按手机号认领。

主会议开放至 2026-09-10 23:59，之后按实际时间显示结束。扫码验收时应使用本人微信通过业务表单
新建登记；现有合成名单用于查看管理、统计、人工更正和大屏效果。

## 本地更新与重复导入

- 原本地 `dianxinyun` 已顺序应用 `20260902_site_access_meeting_checkin.sql` 和
  `20260910_visitor_reuse_guard_meeting.sql`，各执行两次，当前 103 表；原业务行数保持不变。
- 本次升级前的数据库、上传目录、环境文件及 SHA-256 校验文件位于
  `backups/visitor-local-preview-20260910_164120/`，仅保存在本机且不进入 Git。
- 后端 8080 已重启为当前代码；Web 3002 继续使用当前工作区。当前开发流程不再依赖 3008 演示入口。
- `scripts/site-access-local-demo-data.mjs` 只连接本机 `dianxinyun`，要求指定目标项目并显式确认；
  使用已有访客密钥加密，事务内仅新增本日固定标记的数据；不创建账号、不清库、不替换门卫码。
- 同项目同日重复运行只核验已有数据，不重复导入、不覆盖用户随后编辑的测试内容；数据计数变化时
  返回提示，需先检查实际记录。项目尚未创建门卫码时停止，先从场内管理的门卫页创建即可。

在仓库根目录复跑当前项目：

```bash
bash -c 'set -a; source backend/.env.local; set +a;
DIANXINYUN_LOCAL_DEMO_PROJECT_ID=3 \
DIANXINYUN_LOCAL_DEMO_CONFIRM=LOCAL_DEVELOPMENT_ONLY \
node scripts/site-access-local-demo-data.mjs'
```

已验证脚本语法、重复执行数量不增长、35 人签到/40 人预约/10 人待签到，以及经 3002 代理读取
待填、已填和作废邀请均返回正确状态。后端当前 OpenAPI 包含大屏、门卫状态刷新和近期会议接口。
本轮是本地开发更新，未进行生产发布；微信真机和实际投屏验收另行记录。
