# 系统全局检查修复状态

更新时间：2026-08-01。

本文记录本轮《系统全局检查报告》的修复收口状态。状态含义：

- **已关闭**：仓库内修复完成，并通过对应自动化或最小运行验证；
- **代码已修复，待生产发布**：本地代码和构建已通过，但生产环境仍运行旧版本；
- **已缓解，受上游约束**：已降低风险，当前依赖生态没有可直接替换的兼容版本。

## 一、问题状态

| 问题 | 状态 | 修复摘要 | 当前证据或遗留风险 |
| --- | --- | --- | --- |
| BUG-001 | 代码已修复，待生产发布 | 小程序真实构建强制合法 HTTPS `/api/v1` 地址并关闭 Mock | 本地 `mp-real` 构建通过；生产验证码接口仍是旧后端 500 |
| BUG-002 | 已关闭 | 摄像头、设备接口增加服务端登录态、项目权限和敏感字段防伪造校验 | 授权测试通过 |
| BUG-003 | 已关闭 | 补齐人员编辑、删除、安全教育持久化与校验 | 后端回归通过 |
| BUG-004 | 已关闭 | 统一设备类型别名和状态转换 | 状态/类型测试通过 |
| BUG-005 | 已关闭 | 统一文件与设备状态枚举 | 枚举、序列化和控制器测试通过 |
| BUG-006 | 已关闭 | 数据库、RBAC 和项目多角色文档与当前实现对齐 | 当前本地库 47 表 |
| BUG-007 | 已关闭 | `init.sql` 改为拒绝执行；空库初始化增加空库确认门禁 | 当前有数据环境不会被初始化入口覆盖 |
| BUG-008 | 已关闭 | 禁止真实接口失败时静默切换 Mock | H5、微信构建通过 |
| BUG-009 | 已关闭 | 项目列表错误不再伪装为空列表，默认项目选择增加合法性校验 | Web 单元测试 3/3 通过 |
| BUG-010 | 已关闭 | Web 增加 lint、typecheck、test、build 统一检查并修复未定义引用 | `npm run check` 通过 |
| BUG-011 | 已关闭 | 修复 Web 生产依赖高风险公告 | Web `npm audit` 为 0 |
| BUG-012 | 已关闭 | Web 开发工具链升级至 Vite 8 | Web 构建通过 |
| BUG-013 | 已缓解，受上游约束 | 收敛小程序依赖并限制 H5 开发服务器 CORS，只允许本机/私有局域网 Origin | DCloud 当前编译套件精确锁定 Vite 5.2.8；仍有 1 高、14 中开发依赖公告，禁止暴露公网 |
| BUG-014 | 已关闭 | 升级后端 Spring Boot、Spring Framework、Tomcat、MySQL、Netty、MinIO 等高风险依赖 | 后端运行时依赖 OSV 扫描无严重/高风险命中 |
| BUG-015 | 已关闭 | 生产环境拒绝默认或弱 JWT 密钥 | 配置门禁测试通过 |
| BUG-016 | 已关闭 | 上传增加类型、大小、文件头校验；预览限制脚本格式并防路径穿越 | 文件安全测试通过 |
| BUG-017 | 已关闭 | 生产数据库/Redis/接口文档/错误详情增加安全门禁 | 部署安全、文档访问和异常脱敏测试通过 |
| BUG-018 | 已关闭 | 升级 Logback、Jackson、Commons Lang、POI、Commons Compress 等剩余公告依赖 | 118 个 Maven 运行时坐标 OSV 扫描无命中 |
| BUG-019 | 已关闭 | 验收文档与当前测试、47 表和 148 条 OpenAPI 路径对齐 | 退役接口未重新暴露 |
| BUG-020 | 已关闭 | 新增生产升级、备份、迁移、验收和回滚手册 | 未执行生产写操作；发布仍需明确授权和实际运维信息 |
| BUG-021 | 已关闭 | 将破坏性演示重置 SQL 移出正式迁移目录 | 正式迁移目录无 `DELETE FROM`、`TRUNCATE`、`DROP TABLE/DATABASE` 破坏性语句 |
| BUG-022 | 已关闭 | 摄像头原始 RTSP 地址仅向平台管理员或项目经理返回 | 普通项目成员只能看到是否已配置，不获得可能内嵌的设备凭证 |
| BUG-023 | 已关闭 | 生产启用受信代理客户端 IP 处理，限流与审计不再共享或盲信代理头 | Nginx 必须按发布手册设置转发头，后端端口不得直接暴露公网 |
| BUG-024 | 已关闭 | 登录按 IP 和全局账号独立限流，Redis 限流键使用 HMAC 脱敏 | 更换 IP 不能重置账号额度，Redis 不保存明文账号或客户端标识 |
| BUG-025 | 已关闭 | 本地文件存储的相对键和历史绝对键统一限制在上传根目录 | 根目录外读取、覆盖、回滚清理和删除均被拒绝，根目录内绝对路径保持兼容 |
| BUG-026 | 已关闭 | 项目删除前检查全部项目直接关联表，仅允许删除完全空项目 | 有成员或业务数据时返回 HTTP 409，不会只删主记录留下孤儿数据 |
| BUG-027 | 已关闭 | 工程资料永久删除改为数据库提交后清理物理文件 | 事务回滚保留原文件，提交后清理失败保留逻辑删除元数据供排查 |
| BUG-028 | 已关闭 | 通用附件上传、替换、删除与数据库事务结果联动 | 回滚清新文件，提交后才删旧文件，元数据写入异常返回 HTTP 409 |
| BUG-029 | 已关闭 | 巡检汇总按历史巡检范围、到期日期和箱日去重统一统计 | 公开近 30 天、公开月表和内部月度异常不再计入非巡检范围、未来或重复记录 |
| BUG-030 | 已关闭 | 权限撤销的缓存与会话在事务提交前后双重失效 | 并发请求不能把提交前的旧项目缓存或新签发会话带到授权变更提交之后 |
| BUG-031 | 已关闭 | 统一扫码解析、公开巡检摘要与月表增加真实客户端 IP 限流 | 超限请求在可选登录态解析、巡检范围和记录查询前返回 HTTP 429，限流键继续使用 HMAC 脱敏 |
| BUG-032 | 已关闭 | 标准微信注册临时身份与数据库事务结果联动 | 数据库回滚后恢复 10 分钟微信身份会话，提交后清理一次性占用标记，并发请求仍只能成功占用一次 |
| BUG-033 | 已关闭 | 密码、账号、微信绑定及权限授权写入统一校验数据库影响行数 | 目标记录并发消失或写入未生效时返回 HTTP 409 并回滚，不再继续注销会话、替换权限或记录成功结果 |
| BUG-034 | 已关闭 | 统一扫码在公开访问关闭时停止向无项目权限调用者返回电箱信息 | 匿名或无项目权限账号收到 HTTP 403，且不再查询巡检范围和当天内部记录；正常公开扫码也不查询内部记录 |
| BUG-035 | 已关闭 | 公开近 30 天摘要和逐日月表一次加载整个日期范围的巡检范围历史 | 单个公开请求的范围查询由逐日 28–31 次降为 1 次，历史范围与建箱日期口径保持不变 |
| BUG-036 | 已关闭 | 电箱二维码打印和下载模板统一转义业务字段并限制图片源 | 恶意名称、位置或场景文本只能作为纯文本；打印文档增加 CSP，打印窗口切断 opener |
| BUG-037 | 已关闭 | 当前账号微信绑定、解绑和项目访问申请增加 IP 与账号双维度限流 | 两套兼容解绑入口共享额度，密码校验前最多每账号 10 分钟 5 次，限流主体继续使用 HMAC 脱敏 |
| BUG-038 | 已关闭 | 锁定电箱项目与生命周期字段，校验责任人、字段边界、细分权限和数据库影响行数 | 伪造项目/状态/姓名、无效责任人和权限串线均被服务端拒绝；写入未生效返回 HTTP 409 |
| BUG-039 | 已关闭 | 工程资料目录、资料、版本、回收站和审计写入增加影响行数及字段边界校验 | 任一步写入未生效返回 HTTP 409 并回滚；超长编号、备注和版本说明在物理文件落盘前拒绝 |
| BUG-040 | 已关闭 | 锁定日检模板、来源和六项规范检查项，补齐异常说明、并发唯一和写入影响行数校验 | 电箱行锁串行化同箱提交；模板历史插座编码已在备份后对齐，迁移连续执行两次通过 |
| BUG-041 | 已关闭 | 修复本地服务脚本 Redis 地址变量与全角标点相邻导致的变量误解析 | `bash -n` 和完整 `restart` 通过；缺少 Redis 时会输出真实地址而非未绑定变量崩溃 |
| BUG-042 | 已关闭 | 摄像头和设备 CRUD 增加字段边界、状态枚举和数据库影响行数校验 | 并发消失返回 HTTP 409；项目归属、持久化字段和既有自定义设备类型兼容规则不变 |
| BUG-043 | 已关闭 | 人员、进退场、证件和证件附件增加持久化字段白名单、边界及写入结果校验 | 流程外状态/项目/删除标记不可伪造；附件不能跨项目、跨类型、跨上传人或抢占其他业务绑定 |
| BUG-044 | 已关闭 | 安全教育批次、人员、附件、完成和删除流程增加强类型解析、边界及事务写入校验 | 重复/跨项目人员和跨用户/已绑定附件在首笔写入前拒绝；部分完成或删除返回 HTTP 409 并回滚 |
| BUG-045 | 已关闭 | 项目新增、编辑和定位增加字段白名单、边界、状态/日期枚举及写入结果校验 | 客户端不能覆盖持久化字段；定位主记录与审计日志同事务，任一步失败返回 HTTP 409 并回滚 |
| BUG-046 | 已关闭 | 电箱巡检范围变更增加电箱行锁、字段边界和历史区间写入结果校验 | 同箱并发变更串行；旧区间关闭或新历史新增未生效时返回 HTTP 409 并回滚 |

## 二、2026-08-01 收口验证

### 后端

- `mvn test package`：成功；
- 测试：396 项，0 失败、0 错误、0 跳过；
- JAR：`backend/target/site-platform-1.0.0.jar`；
- SHA-256：`80ad854f371c23cf7d458b03240385aefefdd4b4b010ad91921411ddc6ef9024`；
- 本地 `8080` 正常监听；
- `/api/v1/auth/captcha` 返回 HTTP 200 且业务码 200；
- 未登录访问 `/api/v1/projects` 返回真实 HTTP 401；
- 未登录新增日检、兼容提交和修改巡检设置均返回真实 HTTP 401；
- OpenAPI 当前包含 148 条路径。

### PC Web

- `npm run check`：lint、typecheck、6 项测试和 Vite 8 构建全部通过；
- `npm audit --audit-level=high`：0 个漏洞；
- 本地 `3002` 正常监听；
- `/api/auth/captcha` 经 Vite 代理返回 HTTP 200 且业务码 200；
- 页面标题为“智慧营造综合管理平台”，favicon 返回 HTTP 200；
- 当前单个主 JavaScript 分包约 536 kB，只有构建体积告警，不阻断发布。

### 小程序与 H5

- `npm run type-check`：通过；
- `npm run build:h5`：通过；
- `npm run build:mp-weixin`：通过；
- `npm run build:mp-weixin:real`：通过；
- 本地 H5 `3003` 正常监听，验证码代理返回 HTTP 200 且业务码 200；
- `npm audit`：1 高、14 中、0 低、0 严重，均来自 DCloud 编译链锁定的
  Vite 5.2.8 / esbuild 0.20.2；这些是开发构建依赖，不得把 H5 开发服务器暴露到公网。

### 本地数据库与基础服务

- MySQL 8.4.10；
- `dianxinyun` 共 47 张表；
- 项目成员、项目多角色、角色业务模块关联的孤儿记录均为 0；
- 质量问题状态使用当前合法枚举 `PENDING / RECHECK / CLOSED / VOIDED`；
- 项目专用本地 Redis 6380 端口可连接，当前后端已正常使用，未占用其他项目的 6379；
- 当前日检模板恰好包含六个规范检查项，模板项重复和有效日检箱日重复均为 0。

## 三、仍未完成的生产动作

2026-07-31 只读复测 `https://zhihuiyz.xyz`：

- DNS、HTTPS 与 `/api/v1/**` 转发正常；
- `/api/v1/projects` 仍由旧后端处理；
- `/api/v1/auth/captcha`、统一注册、Web 扫码和微信快捷登录接口不存在；
- 验证码虽然 HTTP 为 200，但响应业务码为 500，不能视为健康；
- 响应特征与旧提交 `89f186d` 一致，生产没有运行当前后端。

因此生产登录问题不是当前仓库的接口前缀错误，而是**生产版本未升级**。执行升级前必须按
`docs/生产升级与回滚手册.md` 完成明确授权、源码归档、生产数据库只读预检、数据库与文件
备份、迁移副本演练和回滚准备。当前工作区包含大量既有未提交变更，禁止直接把整个脏工作区
上传覆盖生产。

## 四、验收边界

本轮已完成代码、自动化、构建、HTTP 和本地数据库只读验证；尚未完成：

- 生产数据库结构与迁移标记核对；
- 生产部署、服务重启和上线后回归；
- 正式微信 AppID 下的真机快捷注册、快捷登录、扫码和文件上传；
- 本轮修复后的完整人工视觉走查。

这些项目不能用本地模拟器或构建成功替代。生产发布完成前，BUG-001 维持“代码已修复，
待生产发布”状态，系统不建议宣告正式上线通过。
