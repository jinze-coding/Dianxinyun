# DCloud 5.14 still declares an exact Vite 5.2.8 peer although the project is
# validated against security-fixed Vite 6.4.3. Keep normal installs reproducible
# until DCloud publishes a compatible peer range.
legacy-peer-deps=true
